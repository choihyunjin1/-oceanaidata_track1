# 반드시 먼저 읽을 메모 - Ocean AI Data P3

> 상태: 2026-08-16 KST 기준
> P3 데이터 열람, 코드 수정, 실험, Git 작업, 제출 전에 이 문서와 배포 `README.md`를 처음부터 끝까지 읽는다.

## 0. 고정 원칙

- 목표는 200개 독립 사례의 6개 리드, 총 1,200개 `hs`에 대한 통합 RMSE 최소화다.
- 각 평가 사례에서는 해당 `case_id`의 과거 48시간만 사용한다. 다른 사례의 행을 현재 사례의 시계열 문맥으로 연결하지 않는다.
- test의 절대 기준시각을 역추적하거나 외부 관측자료와 대조하지 않는다.
- 원본 ZIP/CSV/Parquet/README/score.py는 읽기 전용·로컬 전용이며 Git, 보고서, 제출 패키지에 넣지 않는다.
- 행 단위 무작위 분할을 주 검증으로 사용하지 않는다. 학습 시점의 48시간 context와 24시간 target이 검증 시점과 겹치지 않도록 시간 블록과 embargo를 사용한다.
- Public 66사례 점수만으로 모델을 선택하지 않는다. 로컬 시간 외삽 검증, 정점·리드별 진단, Private 일반화 위험을 함께 본다.
- 사용자가 정확한 파일과 SHA-256을 승인하기 전에는 제출하지 않는다.

## 1. 문제 3 — 종합해양과학기지 유의파고 단기 예측

### 1-1. 배경

유의파고는 항해 안전, 해상 공사, 연안 시설 관리의 핵심 기준이다. 지점 관측 이력을 학습하면 수치파랑모델 없이 단기적인 풍파의 발달·감쇠 특성을 분석할 수 있다. 미래 기상장과 원격 해역 정보가 없으므로 리드타임이 길수록 예측력에 한계가 있고, +18시간과 +24시간은 의도적으로 도전적인 구간이다.

### 1-2. 과제

- 각 익명 평가 사례에 포함된 과거 48시간 관측만 사용한다.
- G-ORS, I-ORS, S-ORS의 +3, +6, +9, +12, +18, +24시간 유의파고를 예측한다.
- 학습기간: 2024-01-01~2025-06-30.
- 평가기간: 2025-07-04~2026-06-30.
- 평가 사례는 기준시각 `hs >= 1.5 m`이고 6개 리드 정답이 모두 유효한 사례다.
- 동일 정점 사례의 기준시각은 최소 78시간 간격이다.
- 200개 독립 사례, 리드별 200개, 총 1,200개를 통합 RMSE로 채점한다.

### 1-3. 변수와 시간격자

| 변수 | 단위 | 설명 |
|---|---:|---|
| hs | m | 유의파고, 상위 1/3 파고 평균 |
| tp | s | 상위 1/3 파의 평균 주기, 첨두주기 아님 |
| hmax | m | 구간 최대파고 |
| wvdir | deg | 파가 오는 방향, 진북 기준 시계방향 |
| wspd, gust | m/s | 풍속, 순간최대풍속 |
| wdir | deg | 바람이 불어오는 방향 |
| caph | hPa | 현지기압 |
| airt, relh | ℃, % | 기온, 상대습도 |

- 파랑은 20분, 기상은 10분 간격이다.
- test context는 10분 격자 `step_minute=-2880,-2870,...,0`의 289행이다.
- 파랑이 없는 중간 10분 행의 파랑 열은 결측이다. 이를 이상이나 타깃 단서로 취급하지 않는다.

### 1-4. 미래정보 차단 구조

- `test_context.parquet`는 각 case의 기준시각 이전 48시간만 포함하고 절대 평가시각을 공개하지 않는다.
- context 48시간 + target 24시간 + buffer 6시간 때문에 동일 정점 사례 사이에는 목표 구간 중첩이 없다.
- 평가 context는 학습기간과 겹치지 않는다.
- 현재 예측에는 같은 `case_id` 그룹만 사용한다.
- case와 실제 기준시각 대응표는 운영자 전용이다. 이를 추론하거나 복원하려 하지 않는다.

### 1-5. 배포 파일

| 파일 | 내용 |
|---|---|
| train_wave.csv | 2024-01-01~2025-06-30 3개 정점 파랑 관측, 118,152행 |
| train_atmos.csv | 같은 기간 3개 정점 기상 관측, 130,896행 |
| test_context.parquet | 200개 사례 x 289행 = 57,800행 |
| test_index.csv | 1,200개 예측 키 |
| sample_submission.csv | 제출 양식 |
| baseline_persistence.csv | 기준시각 hs 유지 기준값 |
| score.py | RMSE와 제출 입력 검증 |

공식 지속성 전체 RMSE는 0.769455 m, 공개 stretch 기준 T는 0.624165 m다. 리드별 지속성 RMSE는 +3h 0.5460, +6h 0.7011, +9h 0.7061, +12h 0.7596, +18h 0.8897, +24h 0.9464다. 이 수치는 hidden 평가 결과이며 로컬 validation 수치와 직접 빼서 비교하지 않는다.

### 1-6. 제출 형식

열 순서와 이름은 정확히 다음과 같다.

```text
case_id,station,lead_h,hs_pred
C0001,G-ORS,3,1.44
C0001,G-ORS,6,1.52
C0001,G-ORS,9,1.61
```

- 정확히 1,200행, 키 중복·누락·초과 0건.
- `lead_h`는 3, 6, 9, 12, 18, 24만 허용한다.
- `hs_pred`는 결측·무한값이 없는 0~30m 수치다.

## 2. 검증과 모델 선택 계약

- 학습 자료에서 공식 사례 조건을 모사하되, anchor의 과거 48시간과 미래 24시간이 fold 경계를 넘지 않게 한다.
- validation anchor는 정점별 최소 78시간 간격으로 분리하고, 시간순 외삽 fold를 주 검증으로 사용한다.
- scaler, imputer, 통계, 특징 선택, 모델·앙상블 가중치는 fold train만으로 정한다.
- 최종 지표는 모든 정점·리드를 합친 RMSE다. 정점·리드별 RMSE, 폭풍 성장/감쇠 상태, 결측률별 RMSE를 실패 원인 진단에 함께 기록한다.
- 첫 기준선은 persistence다. 후보는 같은 validation 사례에서 persistence와 직접 비교한다.
- 절대 hs뿐 아니라 `target_hs - current_hs`를 예측하는 잔차 모델을 반드시 비교한다.
- 방향은 원형 변수이므로 sin/cos 또는 각도 차이로 표현한다.
- 48시간 컨텍스트의 최근값, 변화율, 변동성, 파주기·최대파고 비, 바람-파랑 정렬, 기압·풍속 변화, 다중 시간창 특징을 검토한다.
- 정점별 단독모델과 pooled station-conditioned 모델, lead별 단독모델과 multi-output 모델을 모두 같은 분할에서 비교한다.
- 더 무거운 모델은 단순 모델보다 시간순 validation에서 안정적으로 개선할 때만 승격한다.

## 3. 외부자료·제출 금지선

- 공식 FAQ에 따라 출처를 명시한 외부 공개 데이터는 사용할 수 있다. 2023년 이전 공개 파랑·기상 자료를 storm-life-cycle 사전학습에 쓰는 것이 우선 후보다.
- 익명 test context를 2025-07-04~2026-06-30 실제 관측·재분석·수치파랑자료와 대조해 절대시각을 복원하거나 실제 미래 파고를 가져오는 행위는 금지한다.
- 외부 자료는 라이선스·버전·회수일·SHA-256·가공 이력을 기록하고, 대회 train-only 미세조정 및 external-off ablation으로 증분을 분리한다.
- Public 리더보드에 반복 적합하지 않는다.
- 정확한 후보 파일 경로, SHA-256, validator 결과를 사용자에게 제시하고 명시 승인을 받은 뒤에만 업로드한다.

## 4. 원본 위치 계약

- 환경변수 `P3_DATA_DIR`이 `P3_wave_forecast` 폴더를 가리키게 한다.
- 코드에 사용자명, 드라이브, 한글 다운로드 경로를 하드코딩하지 않는다.
- 원본의 SHA-256과 파일 크기만 manifest에 기록하며 절대경로는 공유 보고서에서 제거한다.

## 5. 공식 우선순위

충돌 시 최신 운영진 공지 또는 서면 답변 > 배포 README > 사용자 제공 문제문 > 이 메모 > 구현 보고서 순으로 적용한다.
