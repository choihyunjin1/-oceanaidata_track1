# P1 v28 prospective cross-quarter transport guard

This guard is sealed for new axes only. It does not re-evaluate, rescue, or retune v24 or v27.

v27 passed the earlier within-prefix guard with both chronological halves, two station-layer identities, and two stations, yet its isolated Q2 additions had precision `4/332 = 0.012048`. Together with v24, this shows that diversity inside one calibration surface does not establish calendar-window transport.

The amended chronological contract trains one candidate ensemble and selects one numeric threshold strictly before Q2. That exact ensemble and threshold must independently pass the full multi-station guard, precision `>0.55`, nonzero TP, and Wilson-90 LCB `>=0.55` in both Q2 and Q3. Those windows are veto surfaces only; no per-window threshold selection or refit is allowed. Q4 labels remain unopened until both receipts are sealed. Only Q4 is the unused performance surface.

The maximum remains nine supervised fits, with three fixed seeds trained once preferred; quarter refits, sweeps, retries, anchor removals, official/test/sample/submission/hidden reads, CSVs, and uploads are zero.
