# P1 v29 prospective duplicate audit

v29 does not rescore or reopen v28. It preserves v28's three frozen logits, logistic calibrator (`C=.1`, lbfgs), chronological 75:25 split, and Saerens EM (`200`, `1e-10`, `1e-6`). The new question is whether two prospectively sealed safety layers prevent cross-group and within-day transport failures: inner-label-only station-layer eligibility (minimum 20 proposed additions, precision above whole-inner anchor F1/2, nonnegative within-group delta) and an outer-label-free KST-day 0.5% cap ranked only by adjusted probability.

This combination was not executed by v28 or v27. It adds no quarter router, model/EM tuning, outer-label threshold, or Public-score row label. Historical execution and lock creation remain disabled until separate authorization.
