# P1 v14 duplicate and leakage audit

## Conclusion

`P1_1_EXACT_VERTICAL_BRACKET_ADD_ONLY` is a literal, deterministic output-level rule not found in the repository's prior P1 candidate receipts. Prior cross-layer work used learned features, residual geometry, synchronous decomposition, or asynchronous peer history; none implemented the exact same-station, exact-timestamp, immediate-depth-neighbor bracket mask.

The rule is sealed from incumbent anchor bits before historical targets are attached. It has zero fitted parameters, no station router, no threshold grid, and no result-dependent trimming. Edge rows, missing adjacent depth ranks, duplicate ranks, unknown ranks, and nonmonotone groups fail closed. After a terminal scientific failure, adjacency and concentration caps may not be retuned.

This remains development evidence on previously reused 2025 Q3/Q4 surfaces, not an independent confirmation.
