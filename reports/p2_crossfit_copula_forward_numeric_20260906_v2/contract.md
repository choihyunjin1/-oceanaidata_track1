# P2 numeric amendment — execution contract

User-approved technical completion of the consumed v1 run, not a restart. Original files, locks, models, arrays and failure are immutable. New ID `p2_crossfit_copula_forward_numeric_20260906_v2`.

First, zero new model fits: verify original source/dependency/environment/model/train-key/augmentation/inner-month provenance; independently derive empirical CDF, mean, covariance and Ledoit-Wolf shrinkage from stored calibration; re-predict all baseline rows and B1/B2 calibration/saved predictions. Exact-hash copies in a new output directory are not new fits. Four copulas were fitted/saved; only three post-assert receipts existed. The fourth receipt is reconstructed with explicit unknown individual fit runtime, never silently counted as a new fit.

Only after this PASS, reuse 36 backbone + 4 copula fits, then finish B3–B8 36 inner backbone + 12 copula fits, total 48 new and 88 logical fits. CPU2, GPU0. Three fixed seeds, 60 epochs, original C3 recipe, 11 physical features, correction strength1.0, 8 original folds, two source-only selected inner months, and double 7-day purge stay unchanged. No outer-trained model enters cross-fit calibration for that fold. No early candidate performance disclosure, no rule/strength/date/feature search.

The original input/key/NaN/baseline exact invariance stays exact. Only different whole-batch missingness-pattern computations of copula final temperature outside the artificial outage allow rtol0/atol1e-12 Celsius. Predictions are not modified to satisfy the assertion. Identical same-shape input in a fresh process must replay exactly. Injected1e-9 error and any bitwise input/key change must fail synthetic checks.

Original execution1809.485s + this completion≤3590.515s = original90-minute research cap. Operational checking at epoch/fit/end boundaries, not a process-killing OS timeout. Point estimate36×46.118s=1660.237s; ×1.25 +300s=2375.296s. Zero-fit provenance and postrun QA/replay/development time are reported separately and are not falsely included in a fresh six-hour final-package claim.

Same eligible166268 keys and pooled SSE/n RMSE Celsius; B3 September–October natural primary, whole8fold pooled and natural T5-missing/T5-present, layer, extra-last17days outage reported separately. B4/B8 zero target outage remains NOT_ESTIMABLE, not error0 or robustness success. Retrospective blocks are not new independent confirmation. CI90 uses original paired7-day bootstrap2000draws/seed20260906; bootstrap fractions are not official improvement probabilities. No .8 or outage-neutral hardgate. Preserve every strict-primary-mean-improvement arm with risks; full materialization only under a separately frozen follow-up contract after independent QA. Research officialinput/CSV/upload0; Git0.
