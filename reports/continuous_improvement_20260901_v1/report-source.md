# P1-P3 continuous improvement cycle — 2026-09-01

## Executive answer

The cycle remains active until 23:00 KST. A gain of `+0.1` competition point is a milestone, not a stopping rule. Each problem keeps its current Public champion while a distinct, preregistered local candidate is trained and tested. A candidate is eligible for official review only after internal metric, competition-point translation, stability diagnostics, uncertainty, hash, and independent QA are available.

Current Public anchors are:

| Problem | Metric | Points | Today's remaining submissions at 04:00 KST |
|---|---:|---:|---:|
| P1 | F1 `0.833548` | `28.909341` | `2/3` |
| P2 | RMSE `0.430194 C` | `27.935464` | `3/3` |
| P3 | RMSE `0.575233 m` | `24.203599` | `3/3` |

Nominal point translations used only for planning are `26.5781209 points/F1` for P1, `-12.5475311 points/C` for P2, and `-15.8707390 points/m` for P3. Every candidate report must show the raw metric delta first and keep the empirical transport error separate from this nominal conversion.

The latest official probes did not improve any champion. P1 v33a fell by `0.010753` F1 and `0.285796` points. P2 bin17 layer-4 removal worsened by `0.000606 C` and `0.007601` points. P3 lead-factor weakening worsened by `0.001356 m` and `0.021529` points. These sign reversals make historical-to-Public transport uncertainty a first-class gate; they do not justify stopping research.

## 04:03 KST checkpoint

- P1 long-event reactivation v1 terminated before claim/state creation with `0` fits. The same-process wrapper performed the readiness import once and then incorrectly measured a second delta-module load as an empty receipt. This is `INVALID_TECHNICAL_ZERO_FIT`, not negative scientific evidence. A new-ID contract repair reuses the first readiness state while keeping the frozen 72-fit science graph unchanged.
- P2 group-balanced raw-residual v8 terminated with `0` fits because timezone-aware timestamps were represented in microseconds on one side and nanoseconds on the other, causing all `69,850` query keys to miss. This is also `INVALID_TECHNICAL_ZERO_FIT`. The v8 lock is retained and a new-ID nanosecond-canonicalization repair is executing without changing the candidate, features, split, seeds, fallback, or gate.
- P3 wind-work residual-axis v20 completed six outer Ridge fits and six feature-only ECDF fits. It produced RMSE `0.777861102 m`, which is `-0.003330423 m` versus the uniform-KMA comparator but `+0.000959902 m` worse than frozen v19. Only `1/6` bimonth blocks improved and both group-bootstrap CI90 intervals against v19 were entirely harmful. The nominal score translation is `+0.052856` points versus uniform, but `-0.015234` points versus the relevant v19 lineage; the candidate is therefore `NO_GO_WIND_WORK_RESIDUAL_AXIS` and is not submission-eligible.

The next P3 cycle is required to be structurally different from v20. It must preserve v19 as a no-op default and preregister a causal selective-information axis before outer scoring; no v20 coefficient or alpha micro-tuning is allowed.

## 04:06 KST completed internal outcomes

### P2 v8r1

The repaired execution completed six fits: three seeds for group-balanced L2 and the preregistered three-seed L1 fallback. Both are closed as `EXPLORATORY_NO_GO_BOTH_PREDECLARED_OBJECTIVES`.

| Candidate | Pooled delta RMSE | Sep-Oct | Jul-Aug | Nov-Dec | Interpretation |
|---|---:|---:|---:|---:|---|
| L2 raw residual | `-1.645609 C` | `-3.955039 C` | `+0.411514 C` | `+1.510580 C` | only 1/3 folds improve |
| L1 raw residual | `-1.770951 C` | `-4.092516 C` | `+0.325290 C` | `+1.322777 C` | only 1/3 folds improve |

The nominal `+24.80/+26.33` point extrapolations are invalid as official expectations: these deltas are far outside the observed small-delta score-calibration range, and the result is dominated by one exposed same-season fold. The next P2 cycle must normalize away absolute seasonal temperature level and test a distinct vertical-curvature/domain-invariant representation; raw-Celsius L1/L2 is closed.

### P3 v20a

The allowed target-free adjustment used a fixed geometric mean of wave-power and train-only wind-support ranks only at lead 24h. It still lost to v19 by `+0.000839221 m`, improved only `1/6` blocks, worsened all stations, and had episode CI90 `[+0.000350,+0.001355] m` and block-station CI90 `[+0.000333,+0.001382] m`. It is `NO_GO_JOINT_WAVE_WIND_SUPPORT`.

More importantly, v19 itself was already uploaded on 2026-08-31 and scored Public RMSE `0.589840 m`, below the actual uniform-alpha champion `0.575233 m`. Consequently v19 and all v19-derived v20/v20a candidates are closed for official submission. The next P3 cycle anchors directly on uniform KMA alpha `0.425`, not v19.

### P1 v1r1 technical receipt

The first same-process repair progressed to `3/72` physical fits and `1/21` materializations, then stopped before any outer score. The pinned execution module expected three namespace attributes absent from the pinned numerical entrypoint: `detect_plateaus`, `detect_singleton_spikes`, and `apply_postprocess`. The functions already exist in the authenticated `p1_qc.rules` and `p1_qc.pipeline` snapshot. This is `INVALID_TECHNICAL_PARTIAL_FIT`, not a model result. The v1r1 lock and failure journal remain immutable; v1r2 adds only exact callable aliases for those three functions under a fresh lock.

### P3 v21 champion-anchored robust fallbacks

The next cycle correctly anchored on the actual uniform-alpha champion, but both sparse target-free fallbacks failed:

| Candidate | Delta RMSE vs uniform | Nominal raw points | Transport-adjusted points | Improved blocks | Changed rows |
|---|---:|---:|---:|---:|---:|
| 25% persistence fallback | `+0.002456835 m` | `-0.038992` | `-0.088578` | `1/6` | `31` |
| three-component median | `+0.001219078 m` | `-0.019348` | `-0.068934` | `1/6` | `21` |

Thus component disagreement plus physical-extreme input diagnostics do not identify rows where the champion should be overridden. This exact sparse fallback family is closed. The next P3 experiment moves to joint six-lead reduced-rank residual dynamics only if a duplication and data-contract audit proves it is distinct from prior direct sequence and per-row residual models.

## 04:17 KST checkpoint

### P1 long-event frozen family closure

The exact callable-only v1r2 repair passed its byte-stability, focused-test, Ruff, and independent-QA checks, then reached `9/72` fits and `4/21` materializations before any outer score. It failed closed because the frozen inner OOS proposal target is empty or single-class. This is `NO_GO_INNER_OOS_PROPOSAL_TARGET_SINGLE_CLASS`: changing the proposal window, target, or gate would define a different scientific experiment, so v1r3 and result-driven retuning are forbidden. No F1 or point estimate exists for this family. P1 now moves to the separately preregistered label-free dependence-aware empirical-null feasibility audit.

The label-free empirical-null audit then completed exactly once with `0` supervised fits and `0` target rows. Q4 held-out block false-alarm rates were `0.01592/0.01637/0.01875` for block lengths `72/144/288`, all within the sealed `0.02` ceiling, so it returned `CALIBRATION_FEASIBLE_RESEARCH_ONLY`. The separately locked sparse add-only historical falsification nevertheless stopped before reading any target or creating predictions because Q2 proposals exceeded the sealed `2%` row-share ceiling. This is `NO_GO_LABEL_FREE_PROPOSAL_SHARE_CEILING_PRETARGET`; no F1, CI, or point estimate exists and threshold retuning is prohibited. P1 now moves to a separate robust cross-layer subspace innovation plus block-conformal/e-BH family.

That robust-subspace/e-BH family passed every label-free ceiling at all three block lengths, then completed the authorized historical falsification. It rejected no blocks and made no additions or removals, so pooled F1 remained exactly `0.860483604` with delta `0`, TP/FP/FN `12989/1146/3066`, and CI90 `[0,0]`. The architecture is valid but powerless at its frozen multiplicity budget and closes as `NO_GO_RESEARCH_ONLY`; its q, e-value transform, and run length will not be retuned. The next P1 cycle audits whether the new subspace representation has supervised nested information value under a simple anchor-negative add-only scorer, with outer quarters unavailable for tuning.

The proposed supervised logistic/precision-LCB head was then stopped at `0` fits by the semantic gate: its anchor-negative regularized logistic scoring, station/layer partial pooling, chronological prefix, one-sided precision LCB, and anchor union already exist in `p1_addonly_hierarchical_event_precision_lcb_20260830_v1`. The decision is `CLOSE_ZERO_FIT_SEMANTIC_DUPLICATE`. P1 now moves to an audited causal masked station-by-layer spatiotemporal graph encoder, provided its architecture and resource contracts remain distinct from MS-TCN, tabular graph features, Group-DRO, and earlier event classifiers.

### P2 v7 ready-candidate recheck and v9 technical repair

The existing `P2_V7_EXTRATREES_PUBLIC_BENEFIT_GATE` pack is still byte-identical at SHA-256 `c6f2a7e02ff3e5064ec653af0a52b117cbf8ae49d80e651a2a96276190f4f620`. It starts from the actual bin17-only Public champion and selectively substitutes a preregistered HGB profile candidate. Its untouched October result is delta RMSE `-0.013071643 C`; all three target layers improve and the block-bootstrap CI90 upper bound is `-0.010794735 C`. The nominal raw point delta is `+0.135447`, but after the fixed empirical transport penalty the planning estimate is only `+0.013765` points. It remains a legitimate low-upside official information candidate, not a guaranteed improvement, and is held while the new v9 family is resolved.

P2 v9 itself stopped before model fitting because some layer-8 feature columns are all missing in the May-Aug training window. This is an `INVALID_TECHNICAL_ZERO_FIT`, not model evidence. v9r1 is allowed to add only deterministic all-missing-column zero handling after presence-indicator semantics; candidate definitions, splits, blending, winsorization, and gates remain frozen.

The repaired v9r1 then completed exactly six fits and closed both candidates as exploratory no-go. The global normalized-curvature Ridge reduced pooled RMSE by `0.012971020 C` with CI90 `[-0.023143523,-0.005600679] C`, but it improved only Sep-Oct (`-0.036490247 C`) and regressed Jul-Aug (`+0.052132376 C`) and Nov-Dec (`+0.036979929 C`); layer 2 also regressed by `+0.005874189 C`. Its nominal raw/transport-adjusted point translations are `+0.330606/+0.208924`, but those are not official expectations because the sign is nontransportable. The layer-month-anomaly candidate likewise improved only `1/3` folds and harmed layer 2. Thus v9r1 is not submission eligible; the byte-stable v7 selective pack remains P2's only current information-probe candidate.

The first data-quality sensitivity cycle v10 stopped before predictions or metrics because its preregistered exact-10-minute layer-8 public-temperature difference has zero supported training rows, while layers 1/5/6/7 have `16211/16161/15159/16221`. This is `INVALID_TECHNICAL_ZERO_FIT`, with its lock retained. A new v11 uses only those four data-supported layers and a fixed cross-layer change-coherence rule; it is a new sealed experiment rather than a v10 retry.

v11 initially stopped at `0` fits on a read-only ndarray view. The fresh v11r1 changed only the buffer copy contract and then completed exactly once. Its fixed supported-layer coherence shrink is the first strong P2 internal signal in this cycle: reference/candidate RMSE `3.085714619 -> 2.945053245 C`, delta `-0.140661374 C`; all three folds, all three target layers, and all six months improve; CI90 is `[-0.308184861,-0.016072198] C`. Nominal raw and transport-adjusted point translations are `+1.764953/+0.219987`. However, this is not yet an official expectation: the historical proxy RMSE is far above the official `0.430194 C`, and the maximum historical action is an unsafe `125.080 C`. The exact candidate therefore enters a label-free deployment-geometry guard before any CSV is allowed, while continuous-depth set-encoder research continues independently.

The exact v11r1 deployment preflight v11m1 then failed closed without creating a CSV. Schema, key uniqueness/order, finiteness, endpoint range, and the profile-projection no-op invariant all passed, and the maximum absolute action `2.034812 C` stayed below the fixed `2.50 C` ceiling. However, the action covered `15,067/26,061 = 57.8144%` of official query rows, above the sealed `50%` ceiling, and its absolute-action p99 was `1.196579 C`, above the sealed `0.50 C` ceiling. The terminal status is `DEPLOYMENT_GUARD_FAIL_NO_CSV`; hidden truth, score-file access, submission rows, and uploads are all `0`. This does not erase the historical v11r1 signal, but it disqualifies that exact deployment action. P2 therefore continues with the separately sealed continuous-depth permutation-invariant set encoder rather than relaxing the failed guard post hoc.

### P3 v22 semantic-duplication stop

The proposed joint six-lead reduced-rank residual cycle passed its data contract with `182` complete cases x `6` leads (`1,092` rows), duplicate/nonfinite count `0`, and six bimonth blocks. It then stopped at `0` fits because the repository already contains executed NLinear station multi-output Ridge and causal-spectral multi-output residual maps with the same causal past-to-joint-six-lead semantics. Rank-1/2 plus fixed winsor/Huber would be a capacity/robustness ablation, not a new information axis. The result is `STOP_SEMANTIC_DUPLICATE`, with prediction/outer score/CSV/upload all `0`. A continuous-path interaction axis is now under a fresh nonduplication audit; the small sample rules out an unconstrained deep network.

### P3 v23 deterministic path-signature result

The continuous-path representation was genuinely new and completed all `12` sealed fits. Both fixed Ridge candidates improved `5/6` bimonth blocks, but neither cleared uncertainty and transport gates. Ridge-256/add-0.15 achieved delta RMSE `-0.002793408 m`, nominal `+0.044333` points, transport-adjusted `-0.005253` points, and episode CI90 `[-0.006280,+0.000943] m`. Ridge-1024/add-0.15 achieved `-0.002790806 m`, nominal `+0.044292`, adjusted `-0.005294`, with block-station CI90 upper `+0.000077 m`. The exact path-signature/Ridge family is `NO_GO_ALL_PATH_SIGNATURES`; no alpha, blend, or signature-level retuning is allowed. P3 now audits a distinct cross-station propagation axis, falling back to directional wind-sea/swell geometry only if station-time support is absent.

The cross-station audit found no deployment-time UTC/simultaneity contract, and the directional wave-age route was semantically duplicated by existing sea-state and forcing-conditioned analog code. The deployable station-coordinate graph-Laplacian alternative completed `12` fits but was harmful: LAP16/LAP64 delta RMSE was `+0.000398422/+0.000191886 m`, nominal points `-0.006323/-0.003045`, and transport-adjusted points `-0.055909/-0.052631`. G-ORS, the May-June block, and lead 12 dominated the regressions. P3 v24 is `NO_GO_ALL_STATION_GRAPH_CANDIDATES`; graph-strength retuning is prohibited. The next audit asks whether case-local reversible instance normalization supplies a genuinely new shape-dynamics axis rather than repeating NLinear or ordinary fold scaling.

RevIN was an exact semantic duplicate of the executed `revin_patch.py` lineage, so it stopped at `0` fits. A genuinely new train-fold-only nonnegative simplex stacker then completed six fits over a fixed heterogeneous base bank, but shrink-0.10/0.20 both regressed: delta RMSE `+0.000200803/+0.000529951 m`, raw points `-0.003187/-0.008411`, adjusted points `-0.052773/-0.057997`, with only `3/6` blocks improving. P3 v25 is `NO_GO_ALL_SIMPLEX_CANDIDATES`; base set, weights, and shrink are frozen closed. The next representation audit targets deterministic causal multichannel ROCKET-style interval convolutions, not another ensemble or alpha blend.

The deterministic causal multichannel ROCKET v26 completed all `12` sealed fits and passed independent QA `16/16`, focused pytest `5/5`, and Ruff. Its best fixed Ridge-2048/add-0.10 candidate reduced internal RMSE by only `0.000121565 m`, equal to nominal `+0.001929` points but transport-adjusted `-0.047657` points. It improved `3/6` blocks, had episode CI90 `[-0.001643,+0.001410] m` and block-station CI90 `[-0.001938,+0.001683] m`, with worst-block and worst-station-lead deltas `+0.007431/+0.005136 m`. The exact kernel bank, Ridge strengths, and blend are therefore closed as `NO_GO_ALL_ROCKET_CANDIDATES`; v27 must audit a new information axis rather than tune v26.

### P1 v5 technical invalidation

Post-run boundary sealing found that the first within-station vertical causal-graph runner compared pandas integer timestamps emitted in microseconds against `Timestamp.value` cutoffs in nanoseconds. The receipt exposed 1970-era boundaries and showed that each fold prefix could include the full 2024-2025 training range. Its `9/9` fits and apparent no-op result are therefore `INVALID_TECHNICAL_TARGET_LEAKAGE_TIME_UNIT`, not scientific evidence. The consumed v5 result and lock remain immutable and must never enter model selection. A fresh v5r1 may change only the timestamp representation to `DatetimeIndex(...).asi8`, add explicit nanosecond assertions and cutoff-boundary tests, and rerun the unchanged sealed science graph under a new lock.

The repaired v5r1 passed the nanosecond boundary contract and completed exactly nine fresh fits in `20.907 s`. The fixed inner Wilson-LCB gate admitted no threshold: the best Q2/Q3/Q4 LCB values were `0.470208/0.144569/0.072981`, all below the registered `0.55`. It therefore made zero additions and removals, leaving pooled F1 exactly `0.860483604` with delta and CI90 `[0,0]` and nominal/transport point deltas `0`. Independent QA and root focused tests/Ruff passed. This is a valid `NO_GO_RESEARCH_ONLY`, distinct from the invalid predecessor. The arbitrary-gate question remains open under a new metric-consistent policy audit; v5r1 itself is closed.

### P2 v12 continuous-depth set encoder

The compact permutation-invariant DeepSets-style encoder completed three frozen-seed fits. It reduced pooled historical RMSE from `3.085714619` to `3.042721096 C`, delta `-0.042993523 C`, with CI90 `[-0.069906,-0.030302] C`; all pooled target layers improved. The canonical current score slope translates this to nominal `+0.539463` points and `+0.417780` after the fixed `0.121682` transport penalty. The result file also contains legacy metric-engine translations `+0.725068/+0.603386`; those are not the canonical current mapping and require an explicit amendment, although the sign/gate decision is unchanged.

Despite the pooled pass, Nov-Dec regressed catastrophically by `+0.179735810 C` while Sep-Oct and Jul-Aug improved by `-0.079386794/-0.007186197 C`. Every one of `69,850` rows changed and p99/max action reached the fixed `0.50 C` clip. Therefore v12 is `FORMAL_EXPLORATORY_PASS_BUT_TRANSPORT_NOT_READY`, not a submission candidate. A source-regime coverage audit must precede any new season-balanced v13; post-hoc Nov-Dec routing or gate relaxation is forbidden.

### P3 v27 fixed multiscale scattering

The fixed dyadic wavelet-modulus/scattering representation completed `12` fits and is the first P3 candidate in this sequence to clear the registered internal stability gate. Ridge-1024/add-0.10 reduced RMSE from `0.781191525` to `0.778751565 m`, delta `-0.002439959 m`; it improved `4/6` blocks, all three stations, and `5/6` leads. Episode CI90 was `[-0.004101,-0.000730] m` and block-station CI90 `[-0.004580,-0.000148] m`; worst block and station-lead deltas were `+0.004587/+0.001802 m`. Nominal gain is `+0.038724` points, but the fixed transport-adjusted estimate is `-0.010862` points. The candidate is preserved for an exact deployment/materialization preflight while research continues; no upload decision follows from the repeatedly exposed 182-case surface alone.

## 05:05 KST checkpoint

### P1 v6/v7 metric audit and causal path cross-moments

The metric-consistency preflight closed at zero fits because the proposed ROCKET route duplicated executed P1 MiniROCKET lineages and the authenticated champion OOF starts after the Q2 inner interval, so an inner-only `F1/2` gate cannot be computed without inventing an anchor. The subsequent distinct causal path cross-moment model completed nine fits under the unchanged `0.55` add-only Wilson-LCB gate. It added seven rows, all false positives, and reduced pooled F1 from `0.860483604` to `0.860284134` (`-0.000199470`, nominal `-0.005302` points). Q2/Q3 were unchanged; Q4 lost `0.000773262` F1. CI90 was `[-0.000587735,0]`, and official/CSV/upload/removal counters were zero. Independent QA, focused pytest `5/5`, and Ruff passed. v7 is closed as `NO_GO_EXPLORATORY_ONLY`; research moves to a distinct change-point or missingness-aware latent-state axis.

### P2 v12a/v13 prefix-safe domain balancing

The zero-fit v12a audit showed that v12 trained only on May-Aug for its earliest fold and that all target folds lie outside that source envelope. v13 therefore kept the same compact DeepSets architecture, blend, and three seeds, but trained each fold only on its chronological prefix with equal mass for layer x calendar-month x KST-day groups. Nine fits reduced RMSE from `3.085714619` to `3.034860044 C`, delta `-0.050854575 C`; all three folds and all six months improved. CI90 was `[-0.081817458,-0.037194536] C`. Canonical nominal and fixed-penalty planning gains are `+0.638099/+0.516417` points. The action affects all `69,850` historical rows with RMS `0.202079 C` and p99/max `0.50 C`; Jul-Aug layer 2/3 and Nov-Dec layer 4 still have local harms. Independent QA `18/18`, focused pytest `5/5`, and Ruff passed. This is the strongest current P2 internal candidate, but it remains `EXPLORATORY_SAFETY_PASS_REQUIRES_FRESH_CONFIRMATION` rather than an automatic upload.

### P3 v27m1 materialization integrity

The exact v27 Ridge-1024/add-0.10 deployment fit produced a `1,200`-row candidate at SHA-256 `f7a469d603d5b4c70090412cf6e36b1938c5051628e2244f6aca4ddc0b1af692`. Schema, keys/order, finiteness, numeric CSV round-trip, source hashes, and all fixed action guards passed independent QA `26/26`; p99/max/RMS action is `0.056542/0.100846/0.018943 m`. Hidden truth and score-file access and uploads remain zero. The pack is `READY_NOT_UPLOADED`, but its internal nominal gain remains only `+0.038724` points and fixed-penalty estimate `-0.010862`; P3 research therefore continues on a separate nonduplicate v28 axis rather than treating materialization integrity as performance proof.

P2 v13 could not be given a genuine fresh confirmation merely by repartitioning the same raw labels. The authenticated comparator commitment exists only for the already exposed `69,850` rows, so v13c closed at zero fits as `HOLD_NO_FRESH_AUTHENTICATED_SURFACE`; it preserved the v13 commitment and read zero target, official, hidden, CSV, or upload rows. P2 therefore moves to a new representation rather than manufacturing freshness.

P3 v28 cross-wavelet relative-phase features completed `12` fits. The best fixed Ridge-512/add-0.10 candidate improved RMSE by `0.001629072 m`, nominal `+0.025855` points, but fixed-penalty `-0.023731` points. It improved `4/6` blocks and passed the fixed worst-block/lead/station-lead/tail bounds, yet both episode and block-station CI90 intervals crossed zero; G-ORS and 6h worsened. Independent QA `20/20`, focused pytest `8/8`, and Ruff passed. v28 is `NO_GO_ALL_PHASE_CANDIDATES`; its scales, ridge, and blend are closed while v29 audits a distinct information axis.

P1 v8 used a genuinely distinct missingness-decay GRU latent state, supported by `16,725` psal and `1,130` depth missing rows with positive elapsed-time variance. It completed nine fits but no inner threshold cleared the unchanged add-only precision gate, so it made zero additions and left pooled F1, long-event interior recall, and offset recall unchanged. Delta F1, CI90, nominal points, and transport points are all zero. Independent lifecycle QA, focused pytest `9/9`, and Ruff passed. v8 is a valid `NO_GO_EXPLORATORY_ONLY`; missingness-decay hyperparameters and the gate are not retuned, and v9 first audits conditional-dependence change semantics against prior CP families.

P3 v29 encoded fixed ordinal-pattern distributions and recurrence-plot topology. Its best Ridge-2048/add-0.10 candidate completed `12` fits and improved RMSE by `0.001830280 m` across `5/6` blocks, nominal `+0.029048` points. Episode CI90 was wholly favorable, but block-station CI90 still crossed zero, 6h worsened by `0.002174 m`, and fixed-penalty points were `-0.020538`. Independent QA `20/20`, focused pytest `7/7`, and Ruff passed. The family closes as `NO_GO_ALL_TOPOLOGY_CANDIDATES`; topology settings are not retuned and v30 must use new information.

P2 v14 added strictly causal, current-row-excluded multiscale domain statistics to the compact set encoder. Nine fits improved pooled RMSE by `0.039812071 C`, nominal `+0.499543` and fixed-penalty `+0.377861` points, with CI90 `[-0.063878,-0.028200] C` and all pooled layers improved. It nevertheless harmed Jul-Aug by `+0.014964 C` and both July/August months, so the preregistered all-fold/month safety gate failed. Independent QA `21/21`, focused pytest `7/7`, and Ruff passed. v14 is `EXPLORATORY_NO_GO_CAUSAL_MULTISCALE_DOMAIN_STATS`; no EWM, blend, or seasonal routing retune is allowed, and v15 audits a distinct pair-interaction set representation.

P1 v9's prefix-fitted covariance, precision, and partial-correlation change representation had strong nonzero feature support, but all three inner precision LCB values stayed below `0.55`. Nine fits therefore made no additions and left pooled F1, long-event interior recall, and offset recall unchanged; delta F1, CI90, and point deltas are zero. Independent lifecycle QA, focused pytest `6/6`, and Ruff passed. v9 is a valid no-op `NO_GO_EXPLORATORY_ONLY`; its dependence estimator and gate are closed.

P3 v30 encoded fixed directed transfer entropy over forcing-to-wave pairs and lags. The best Ridge-2048/add-0.10 candidate completed `12` fits, improved RMSE by `0.001029556 m` across `5/6` blocks, and had nominal `+0.016340` points, but both CI90 intervals crossed zero, G-ORS and 6h worsened, and fixed-penalty gain was `-0.033246`. Independent QA `20/20`, focused pytest `7/7`, and Ruff passed. v30 is `NO_GO_ALL_DIRECTED_INFORMATION_CANDIDATES`; pairs, lags, bins, ridge, and blend are closed.

## 05:33 KST checkpoint

### P2 v15 compact pair-interaction Set Transformer

The fixed one-block compact Set Transformer completed `9` fits and slightly exceeded v13 on the pooled exposed surface: RMSE `3.085714619 -> 3.034482685 C`, delta `-0.051231934 C`, canonical nominal `+0.642834` points and fixed-penalty `+0.521152` points, with CI90 `[-0.082532094,-0.037445076] C`. Permutation-equivariance error was `7.15e-7`, prediction-invariance error `2.98e-8`, and future-isolation error `0`. It nevertheless harmed Nov-Dec by `+0.003842593 C`, November by `+0.010171264 C`, and Nov-Dec layer 4 by `+0.027628915 C`. The family is therefore `EXPLORATORY_NO_GO_COMPACT_SET_TRANSFORMER`; its head, block, blend, and any seasonal router are closed. Independent QA `29/29`, focused pytest `7/7`, and Ruff passed. P2 now tests a fixed physical-depth graph message-passing representation rather than modifying v15.

The ensuing v16 fixed physical-depth graph used one shared message block, a deterministic normalized-depth RBF adjacency, and commutative masked pooling. Nine fits improved RMSE to `3.033552774 C`, delta `-0.052161845 C`, nominal `+0.654502` and fixed-penalty `+0.532820` points, with CI90 `[-0.083833282,-0.038566285] C`; all three pooled layers and five of six months improved. Yet Nov-Dec regressed by `+0.008638613 C` and November by `+0.015874927 C`. This is `EXPLORATORY_NO_GO_FIXED_DEPTH_GRAPH`, not a safer replacement for v13. Independent QA `31/31`, focused pytest `6/6`, and Ruff passed. Bandwidth, block depth, blend, and a November router are closed; P2 continues with a new v17 semantic audit while v13 remains the safety leader.

P2 v17 then used a fixed masked-public-layer denoising auxiliary objective with no external data and no result-driven sweep. Nine fits improved pooled RMSE to `3.033424351 C`, delta `-0.052290268 C`, nominal `+0.656114` and fixed-penalty `+0.534432` points, with CI90 `[-0.084091,-0.038169] C` and all three pooled layers improved. The apparent mean gain did not transport uniformly: Jul-Aug regressed by `+0.008715 C`, Nov-Dec by `+0.027009 C`, and November alone by `+0.040164 C`. All rows changed and action magnitude reached the frozen `0.5 C` cap. Independent QA `31/31`, focused pytest `6/6`, and Ruff passed. v17 is `EXPLORATORY_NO_GO_MASKED_AUXILIARY`; masking, auxiliary weight, blend, and seasonal routing are closed. The repeated pooled-versus-seasonal split motivates a separately preregistered environment-robust objective rather than another architecture-only replacement.

P2 v18 retained the exact v13 representation, prefix, seven-day purge, seeds, epochs, blend, and `0.5 C` action cap, changing only the training objective to a fixed regularized layer-by-calendar-month exponentiated Group-DRO adversary. Nine fits completed in `71.11 s`. RMSE improved from `3.085714619` to `3.034974749 C`, delta `-0.050739870 C`, nominal `+0.636660` and fixed-penalty `+0.514978` points, with CI90 `[-0.081667890,-0.037412713] C`. Unlike v14-v17, every fold, all six months, and all three pooled layers improved; November improved by `0.002040 C` and Nov-Dec by `0.008880 C`. Action p99/max were `0.5/0.5000000000000018 C` over all rows. Root independent QA passed `29/29`, focused pytest `6/6`, and Ruff. v18 is `EXPLORATORY_SAFETY_PASS_REQUIRES_FRESH_CONFIRMATION`: it trails v13 pooled gain by only `0.000114705 C` (`0.001439` nominal points) while providing independently motivated worst-group balance. Both v13 and v18 remain exposed-surface candidates, so neither is a guaranteed official improvement; a distinct fixed risk-dispersion objective is evaluated next without modifying v18.

P2 v19 replaced the minimax adversary with a single fixed V-REx objective: mean layer-by-month risk plus `10.0` times the population variance of those risks. Nine fits completed in `85.06 s`. RMSE improved by `0.047768059 C`, nominal `+0.599371` and fixed-penalty `+0.477689` points, with CI90 `[-0.076213175,-0.035286558] C`; every pooled layer improved. However, Nov-Dec regressed by `+0.000851 C` and November by `+0.008289 C`, so the preregistered seasonal safety rule failed. Root independent QA passed `29/29`, focused pytest `6/6`, and Ruff. v19 is `EXPLORATORY_NO_GO_LAYER_MONTH_RISK_VARIANCE_REX`; the coefficient, regularization, blend, and any router are closed. v13 and v18 remain the two P2 safety candidates while a label-free month-alignment objective is audited separately.

P2 v20 retained the full v13 pipeline and added one fixed Deep-CORAL penalty that aligned latent covariance across calendar months only within the same target layer. Nine fits completed in `518.55 s`. RMSE improved by `0.051840385 C`, nominal `+0.650469` and fixed-penalty `+0.528787` points, with CI90 `[-0.083524701,-0.038050950] C`. All three folds and all three pooled layers improved. Five months improved; November regressed by only `+0.001420 C`, within the preregistered `0.003 C` tolerance, so the registered safety decision is `EXPLORATORY_SAFETY_PASS_REQUIRES_FRESH_CONFIRMATION`. It exceeds v13 by `0.000985810 C` (`0.012369` nominal points) and v18 by `0.001100516 C` (`0.013809` points). Action p99/max remained `0.5/0.5000000000000018 C`. Root independent QA passed `32/32`, focused pytest `6/6`, and Ruff. v20 becomes the P2 internal pooled leader, but the repeatedly exposed surface and small November harm prevent an official performance claim. Its alignment coefficient, architecture, and blend are frozen while another orthogonal objective is researched.

### P1 v10 recurrence-laminar state

The recurrence-laminar add-only model completed `9` fits in `24.469 s`, but no inner proposal cleared the frozen gate. It made zero additions and zero removals, leaving pooled F1 `0.860483604`, long-event-interior recall `0.810713572`, and offset recall `0.647721180` unchanged. Delta F1, CI90, nominal points, and transport-adjusted points are all exactly zero. Root independent recomputation passed all `10/10` checks, focused pytest `6/6`, and Ruff. v10 is a valid no-op `NO_GO_EXPLORATORY_ONLY`; its recurrence thresholds and gate are closed while a distinct v11 axis is audited.

P1 v11 then evaluated a fixed causal first- and second-order Haar-modulus scattering representation. Nine fits made `151` add-only changes, of which `56` were true positives (precision `0.370861`), moving TP/FP/FN from `12989/1146/3066` to `13045/1241/3010`. Long-event-interior recall improved by `0.003664468` to `0.814378040`, but the false-positive cost reduced pooled F1 by `0.000591049` to `0.859892555`; nominal and transport-adjusted point deltas were `-0.015710/-0.004713`, and CI90 `[-0.00424838,+0.00356623]` crossed zero. Root independent recomputation passed all `11/11` checks, focused pytest `6/6`, and Ruff. v11 is closed as `NO_GO_EXPLORATORY_ONLY`; scales, interactions, and gate are not retuned, and a causal recent-versus-prefix kernel two-sample axis is audited separately for v12.

P1 v12 used a fixed causal recent-versus-prefix RBF landmark MMD representation. Although its inner Q2/Q3 precision lower bounds appeared strong (`0.853` and `0.977`), outer evaluation exposed a decisive transport failure: all `447` additions were false positives (`332` I-ORS/L1 and `115` S-ORS/L7). Pooled F1 fell from `0.860483604` to `0.847928975`, delta `-0.012554629`; CI90 `[-0.02285583,-0.00509896]`, nominal `-0.333707` points, and transport-adjusted `-0.100112` points were all unfavorable. Root independent recomputation passed `11/11` checks, focused pytest `5/5`, and Ruff. v12 is closed without threshold or kernel retuning. Its main result is diagnostic: the current P1 bottleneck is inner-to-outer transport of the proposal gate, not simply the absence of another representation. Any v13 mechanism must therefore include a preregistered multi-environment transport-stability veto before outer scoring.

P1 v13 paired a distinct causal horizontal endpoint-visibility representation with that preregistered veto. Environments were fixed station-by-layer-by-chronological-half cells; a threshold required at least two supported environments spanning both halves, precision above `0.55` in every supported environment, and no supported environment with zero true positives. All candidate thresholds were rejected before outer target access. The exactly `9`-fit run therefore made zero additions and zero removals, leaving pooled F1 `0.860483604`, CI90 `[0,0]`, nominal/transport points `0/0`, long-event-interior recall `0.810713572`, offset recall `0.647721180`, and drift recall `0.659506173` unchanged. Runtime was `91.672 s`; root lifecycle QA, focused pytest `5/5`, and Ruff passed. This is a valid `NO_GO_EXPLORATORY_ONLY`: the transport guard prevented v12-style failure, but the visibility signal supplied no promotable action. The exact topology and veto are closed while P1 researches a new event mechanism.

P1 v14 used a causal, non-subsequence-normalized prefix dictionary to approximate Matrix Profile discord scoring without erasing additive offsets. A timestamp-unit defect was caught and corrected during preflight before any fit or lock. The final two preflights were byte-identical and the sealed exactly `9`-fit run completed in `37.218 s`. Q3 pooled inner precision LCB reached `0.77259`, but no threshold passed the unchanged multi-environment transport veto in Q2, Q3, or Q4. The result was another safe exact no-op: additions/removals `0/0`, pooled F1 `0.860483604`, CI90 `[0,0]`, nominal/transport points `0/0`, long-event-interior recall `0.810713572`, and long-event-boundary recall `0.779835391` were unchanged. Root lifecycle QA, focused pytest `4/4`, and Ruff passed. v14 is `NO_GO_EXPLORATORY_ONLY`; windows, prefix dictionary, distances, and veto are closed. A Koopman/DMD innovation proposal was subsequently rejected at zero fits as semantically duplicative of existing causal state-space, latent-GP, RPCA, and robust-subspace innovations, so P1 continues with a different v16 family.

P1 v16 selected causal delay-embedding zero-dimensional persistence after rejecting tail-copula and supervised-shapelet alternatives as semantic overlaps. Fixed Vietoris-Rips component-merger lifetimes were summarized through Euclidean minimum-spanning-tree edges. Two preflights were byte-identical and the exactly `9`-fit execution completed in `75.391 s`, but every Q2-Q4 threshold again failed the prospective station-by-layer-by-half transport veto. The candidate was an exact no-op: additions/removals `0/0`, pooled F1 `0.860483604`, CI90 `[0,0]`, nominal/transport points `0/0`, long-event-interior recall `0.810713572`, and boundary recall `0.779835391` all remained unchanged. Root lifecycle QA, focused pytest `5/5`, and Ruff passed. v16 is `NO_GO_EXPLORATORY_ONLY`; embedding delays, lifetime summaries, and veto are closed. P1 next audits transition surprisal only if it is distinct from the existing soft-symbolic and recurrence families.

P1 v17 closed at zero fits because causal ordinal-transition surprisal is a monotone aggregation of an already executed five-symbol transition-product representation and the same incumbent-negative linear add-only action. No data, target, lock, artifact, official row, CSV, or upload was used. P1 v18 then audited prefix-fitted state-dependent Kramers-Moyal drift/diffusion moments. The scientific mechanism was distinct enough to preflight, but both real preflights identically failed the frozen support gate: nonzero feature share `0.438443` was below `0.5`, although all eight variance checks passed. It closed at zero fits with no target, lock, artifact, official row, CSV, or upload; support, bins, and gate were not relaxed. These closures show that semantic novelty and data support are being enforced before compute, while P1 continues to a separate local-extrema audit.

### P3 v31/v31r1 central quantile residual

The original v31 completed its frozen model fits but terminated before scoring because the reused scorer required its candidate spec in a module-global registry. The source lock and failure receipt remain immutable, and no result was scored. A fresh v31r1 changed only a temporary scorer-registration adapter and preserved the exact quantiles, regularization, features, blocks, purge, blend, and `12`-fit science graph. The recovered result worsened RMSE by `+0.000474982 m`, nominal `-0.007538` points and fixed-penalty `-0.057124` points. Episode and block-station CI90 both crossed zero, and worst reference-tail block harm `+0.019657 m` exceeded the sealed `0.015 m` ceiling. Science changes were `0`; independent QA `23/23`, combined pytest `13/13`, and Ruff passed. v31r1 is `NO_GO_CENTRAL_QUANTILE_CANDIDATE`; the entire quantile family is closed and P3 moves to a distinct v32 information axis.

P3 v32's probability-space Bayesian online change-point likelihood underflowed before any fit or score, so its consumed lock and failure receipt remain immutable. Fresh ID v32r1 changed only the numerical evaluation to log-space/log-sum-exp and retained the same scientific candidate graph. Twelve fits completed in `18.842 s`. The best Ridge-2048 candidate improved RMSE by `0.001950598 m` over `5/6` blocks, nominal `+0.030957` points, but fixed-penalty `-0.018629` points; episode CI90 `[-0.003739,+0.000013]` and block-station CI90 `[-0.003991,+0.000275]` both crossed zero, worst-block harm was `+0.009843 m`, and reference-tail harm `+0.015191 m` narrowly exceeded the frozen `0.015 m` ceiling. Science changes were `0`, numerical-adapter changes `1`; independent QA `23/23`, combined pytest `14/14`, and Ruff passed. v32r1 is closed as robustly insufficient, and P3 proceeds only to a nonduplicate information axis.

P3 v33 used `66` fixed detrended cross-correlation features over four scales and eleven physical channels. Twelve fits completed in `6.213 s`. The best Ridge-2048/add-0.10 candidate improved RMSE by `0.001254214 m`, nominal `+0.019905` points, and `5/6` blocks, while keeping worst block, lead, station-lead, and tail harms within their fixed ceilings. It still failed the uncertainty and transport requirements: episode CI90 `[-0.002609,+0.000161]` and block-station CI90 `[-0.002499,+0.000203]` crossed zero, 6h worsened by `+0.001001 m`, G-ORS by `+0.000069 m`, and transport-adjusted gain was `-0.029681` points. Action RMS/p99/max were `0.016233/0.036676/0.043694 m` over all `1092` rows. Root independent QA passed `20/20`, focused pytest `7/7`, and Ruff. v33 is `NO_GO_ALL_DCCA_CANDIDATES`; scales, channel pairs, ridge, and blend are closed. A quadratic phase-coupling bispectrum/bicoherence axis is reserved for v34 as a separate preregistered family.

P3 v34 encoded normalized third-order quadratic phase coupling with two fixed segment lengths and twelve fixed low-frequency triads. Twelve fits completed in `0.584 s`. The best Ridge-2048/add-0.10 candidate improved RMSE by `0.001446875 m`, nominal `+0.022963` points, and `5/6` blocks; all three stations improved, episode CI90 was wholly favorable at `[-0.002780,-0.000061]`, and all fixed worst block/lead/station-lead/tail ceilings passed. The more conservative block-station CI90 still crossed zero at `[-0.002918,+0.000224]`, 6h worsened by `+0.001469 m`, and transport-adjusted gain remained `-0.026623` points. Action RMS/p99/max were `0.014547/0.028938/0.032175 m` over all `1092` rows. Root independent QA passed `20/20`, focused pytest `7/7`, and Ruff. v34 is `NO_GO_ALL_BICOHERENCE_CANDIDATES`; triads, segment lengths, ridge, and blend are closed. It is a modest robustness improvement over v33 but not a submission promotion, and P3 moves to a separately audited adaptive instantaneous-mode representation.

P3 v35 used a fixed empirical-mode decomposition and Hilbert instantaneous amplitude/frequency representation over four physical paths. The best Ridge-2048/add-0.10 candidate completed within the sealed `12`-fit run and improved RMSE by `0.002682851 m`, nominal `+0.042579` points, and `5/6` blocks. All three stations and all six leads improved; episode CI90 `[-0.004192,-0.001111]` and block-station CI90 `[-0.004330,-0.000898]` were both wholly favorable, and every fixed worst block/lead/station-lead/tail guard passed. Action RMS/p99/max were `0.017514/0.039407/0.047518 m` over all `1092` rows. Fixed transport-adjusted gain was still slightly negative at `-0.007007` points because the historical Public residual penalty is larger than the nominal local gain. Root independent QA passed `20/20`, focused pytest `7/7`, and Ruff. v35 is `PASS_CANDIDATE_AVAILABLE` and becomes the strongest P3 internal research candidate, but its repeatedly exposed 182-case surface and negative fixed transport estimate prevent an automatic upload. The exact mode count, sift count, edge treatment, ridge, and blend are frozen while a separate persistent-homology axis is evaluated.

P3 v36 first terminated before any fit or outer score because SciPy's dense minimum-spanning-tree adapter interpreted exact zero-weight edges in a constant delay cloud as absent. The same ID was not restarted. Fresh v36r1 changed only that adapter to deterministic Kruskal with exact-zero edge retention; random nondegenerate clouds matched the old adapter exactly, science changes were `0`, and numerical-adapter changes were `1`. The recovered fixed `12`-fit result produced another stable candidate. Ridge-2048/add-0.10 improved RMSE by `0.001723140 m`, nominal `+0.027348` points, and `5/6` blocks; both episode CI90 `[-0.003116,-0.000237]` and block-station CI90 `[-0.003102,-0.000138]` were favorable, all stations improved, and every fixed tail/slice guard passed. Fixed transport-adjusted gain remained `-0.022239` points and 6h worsened by `+0.000868 m`; therefore v35 remains stronger. Action RMS/p99/max were `0.015897/0.034992/0.040219 m`. Root independent QA passed `22/22`, focused pytest `7/7`, and Ruff. v36r1 is `PASS_CANDIDATE_AVAILABLE`, immutable and not retuned, while P3 continues with a separate graph-topology axis.

## 06:25 KST checkpoint

P1 v19 closed before data access because causal local-extrema prominence and return-time repeated existing spike/peak, CAPA, visibility, recurrence, and event-proposal geometry. P1 v20 then supplied a genuinely distinct deterministic 16-state causal echo-state reservoir. Its two preflights were byte-identical, feature support was `0.854428`, and all 16 feature variances passed. The exactly `9`-fit run completed in `39.375 s`, but every Q2-Q4 threshold failed the frozen station-by-layer-by-half transport veto; maximum inner Wilson LCBs were only `0.008688/0.061078/0.090943`. It therefore made zero additions/removals and left F1 `0.860483604`, event recalls, CI90, and point deltas unchanged. Root lifecycle QA passed `12/12`, focused pytest `4/4`, and Ruff; result SHA-256 is `7cb398bd87747c7e791694b96cd86add5da270cf01b7f4442a4fd367e98da695`. A subsequent temperature-salinity-depth consistency proposal also closed at zero fits as v21 because the same physical hypothesis already exists in raw/diff/rolling psal-depth features, salinity-gated matched filtering, and the executed temperature-psal path-area family. No target, official row, CSV, or upload was used by the v19/v21 closures.

P3 v37 encoded fixed full horizontal-visibility graph statistics, distinct from P1 endpoint visibility, metric recurrence, and persistence filtration. Ridge-2048/add-0.10 improved RMSE by `0.001767292 m`, nominal `+0.028048` points, and `5/6` blocks. Both episode CI90 `[-0.003140,-0.000323]` and block-station CI90 `[-0.003274,-0.000017]` were favorable; all stations and all fixed tail/slice guards passed, while 6h worsened and the fixed transport estimate remained `-0.021538` points. Root independent QA passed `20/20`, focused pytest `7/7`, and Ruff. v37 is a valid stable research candidate but weaker than v35, so its graph specification, ridge, and blend are closed while the independently preregistered v38 cross-quantilogram tail-dependence axis is evaluated.

P3 v38 then encoded `72` fixed cross-quantilogram tail-hit cross-moments across hs and tp/hmax/wspd, four upper/lower quantiles, three causal lags, and both directions. Synthetic lead-lag orientation, future isolation, two byte-identical preflights, and the sealed `12`-fit contract passed before execution. The best Ridge-2048/add-0.10 candidate improved RMSE by `0.001264047 m`, nominal `+0.020061` points, and `5/6` blocks, but episode CI90 `[-0.002649,+0.000212]` and block-station CI90 `[-0.002800,+0.000517]` both crossed zero; 6h and G-ORS also worsened, and transport-adjusted points were `-0.029525`. Root independent QA passed `20/20`, focused pytest `7/7`, and Ruff. v38 is `NO_GO_ALL_CROSS_QUANTILOGRAM_CANDIDATES`; quantiles, lags, directions, ridge, and blend are frozen and closed while P3 audits a fully separate v39 information family.

P2 v21 changed only the v13 regression likelihood from weighted SmoothL1 to a fixed homoscedastic Student-t negative log likelihood with degrees of freedom `4` and normalized scale `1`; it deleted no rows and learned or swept neither parameter. The exactly `9`-fit run completed in `65.55 s`. RMSE improved by `0.050482138 C`, nominal `+0.633426` and fixed-penalty `+0.511744` points, with CI90 `[-0.081454250,-0.036928943] C`. All folds and pooled layers improved; five months improved and November's `+0.000165 C` regression was inside the preregistered `0.003 C` tolerance. v21 is therefore another `EXPLORATORY_SAFETY_PASS_REQUIRES_FRESH_CONFIRMATION`, but it trails v20 by `0.001358247 C` (`0.017043` nominal points), so v20 remains internal #1. Root independent QA passed `31/31`, focused pytest `6/6`, and Ruff. Degrees of freedom, scale, loss, blend, and cap are frozen while a derivative-supervision v22 is audited only if distinct from earlier curvature and physical-depth graph mechanisms.

P3 v39 used fixed multiscale sample entropy over hs/tp/hmax/wspd, four coarse-graining scales, and conditional template-match summaries. The representation was distinct from ordinal recurrence, cross-covariance, and tail-hit directionality; noise-versus-periodic synthetic separation, affine invariance, finite guards, future isolation, and two byte-identical preflights passed. Both sealed candidates were stable. Ridge-2048/add-0.10 improved RMSE by `0.002099642 m`, nominal `+0.033323` points, and `5/6` blocks; episode CI90 `[-0.003496,-0.000649]` and block-station CI90 `[-0.003754,-0.000194]` were favorable, all three stations improved, and all fixed tail/slice guards passed. Fixed transport-adjusted points remained `-0.016263`, so v35 remains stronger. Root independent QA passed `20/20`, focused pytest `7/7`, and Ruff. v39 is preserved as `PASS_CANDIDATE_AVAILABLE` without retuning while P3 audits a distinct level-crossing/excursion geometry v40.

P1 v22 audited a recent conditional normalizing-flow latent-dynamics compliance method. A faithful flow exceeded the frozen nine-fit scope, while every affordable causal approximation collapsed into already executed state-space innovation, latent-GP, RPCA/subspace, or Koopman closures, so it was rejected at zero fits without data or lock consumption. P1 v23 then changed the learning objective, not the sensor representation: it fit a symmetric positive-negative pairwise logistic AUC surrogate over a fixed causal context basis and kept the multi-environment veto unchanged. Two preflights were byte-identical and the exactly `9`-fit run completed in `20.016 s`. No threshold transported: Q2/Q3 inner precision was zero, Q4 peaked at `0.089247`, and each proposal had only one supported environment versus the required two. The action was therefore an exact no-op with F1 `0.860483604`, additions/removals `0/0`, CI90 `[0,0]`, and point deltas `0/0`. Root lifecycle QA passed `11/11`, focused pytest `4/4`, and Ruff; result SHA-256 is `f109b74b0dbeb2c6cf50ef0bdbdaefb20ecaadb4bda2d31dd9913963447b0d64`. v22/v23 are immutable and P1 proceeds to a distinct v24 axis.

P2 v22 did not execute. The proposed adjacent-depth Sobolev loss was an exact scientific duplicate of the 2026-08-27 continuous-depth challenger, which already penalized squared differences between predicted and target adjacent-layer differences with fixed weight `0.25` and also evaluated a density-gradient physics arm. The previous terminal result was inconclusive, so relabeling the same objective would add neither information nor an independent confirmation surface. The v22 audit closed with fits, predictions, locks, candidate artifacts, official/hidden rows, CSVs, and uploads all zero; independent audit passed `15/15`. P2 therefore moves to a separately fingerprinted public-temperature input-gradient regularization objective rather than relaxing or rerunning the derivative family.

### P3 v40 new internal leader

P3 v40 encoded fixed directional level-crossing rates plus excursion duration, occupancy, and crossing-slope geometry at three median/IQR-normalized levels. It reused no previous prediction, posterior run length, graph, entropy, or router. Synthetic directionality, affine invariance, future isolation, and two byte-identical preflights passed before the sealed `12`-fit run. Ridge-512/add-0.10 improved RMSE by `0.003726943 m`, nominal `+0.059149` points, and fixed transport-adjusted `+0.009563` points—the first recent P3 research candidate to remain positive after the preregistered transport penalty. Episode CI90 `[-0.005562,-0.001795]` and block-station CI90 `[-0.005313,-0.001733]` were favorable; all three stations and all six leads improved, and every worst-block/station-lead/tail guard passed. Action RMS/p99/max were `0.021131/0.054387/0.068457 m`. Root independent QA passed `20/20`, focused pytest `7/7`, and Ruff. v40 supersedes v35 as internal #1, but the 182-case surface is still exploratory and cannot guarantee a Public gain. Its exact Ridge-512 recipe is frozen while v41 tests an independent P3 echo-state trajectory without using v40 outputs.

P3 v41 tested that independent fixed echo-state trajectory. The sealed reservoir had spectral radius `0.9`, fading-memory ratio `0.000596`, and passed two byte-identical preflights plus future isolation. The best Ridge-2048/add-0.10 candidate improved RMSE by only `0.000892511 m`, nominal `+0.014165` points, and `5/6` blocks. Both episode and block-station CI90 crossed zero, G-ORS and 6h worsened, and fixed transport-adjusted points were `-0.035421`. Root independent QA passed `20/20`, focused pytest `7/7`, and Ruff. v41 is `NO_GO_ALL_FIXED_ESN_CANDIDATES`; reservoir size, seed, spectral radius, leak, input scale, ridge, and blend are closed. v40 remains the leader while P3 independently audits conditional increment moments in v42.

P1 v24 introduced a fixed station-adversarial gradient-reversal objective. Its Q2 inner selector appeared unusually strong: `141` proposals, precision `0.957447`, Wilson-90 LCB `0.919830`, with precision `1.0` in both supported halves. Both cells, however, were S-ORS layer 5. On untouched Q2 outer data the action shifted to G-L1, I-L1, and I-L7, and all `332` additions were false positives. F1 fell from `0.860483604` to `0.851123780`, delta `-0.009359824`, CI90 `[-0.013291384,-0.006278462]`, nominal `-0.248788` and transport-adjusted `-0.074636` points. Root lifecycle QA passed `11/11`, focused pytest `4/4`, and Ruff. This valid negative result closes the exact objective and shows that two chronological halves from one station/layer are not sufficient evidence of cross-cell transport; no post-hoc gate change is made inside v24.

### P3 v42 new internal leader

P3 v42 estimated fixed state-dependent conditional increment means and variances over P3 physical paths. A target-free support gate passed with nonzero feature share `0.837019` and `80/80` positive-variance features; synthetic mean-reversion, affine handling, future isolation, and two byte-identical preflights also passed. Ridge-512/add-0.10 improved RMSE by `0.004855893 m`, nominal `+0.077067` points, and fixed transport-adjusted `+0.027481` points, surpassing v40. Episode CI90 `[-0.006939,-0.002700]` and block-station CI90 `[-0.006745,-0.002615]` were strongly favorable; all stations improved and every worst-block/lead/station-lead/tail guard passed. Action RMS/p99/max were `0.025051/0.062141/0.129126 m`; the larger maximum is explicitly retained as a risk descriptor and is not post-hoc clipped. Root independent QA passed `20/20`, focused pytest `7/7`, and Ruff. v42 becomes the immutable internal #1, while the repeatedly exposed 182-case surface and absence of official transport remain material limitations.

### P2 v23 new internal leader

P2 v23 retained the complete v13 pipeline and added only a fixed `0.01` double-backprop penalty on the weighted SmoothL1 loss gradient with respect to the observed public-temperature token. No salinity/depth/context token, masked value, row deletion, perturbation, sweep, router, or ensemble entered the penalty. The exactly `9`-fit run completed in `100.19 s`. RMSE improved by `0.051892467 C`, nominal `+0.651122` and fixed-penalty `+0.529440` points, with CI90 `[-0.083405061,-0.038082520] C`. Every fold, month, and pooled layer improved; November improved by `0.002762 C`. Action p99/max remained `0.5 C`. v23 exceeds v20 by only `0.000052081 C` (`0.000653` nominal points), so it is the arithmetic internal leader but not evidence of a meaningful Public difference. Root independent QA passed `34/34`, focused pytest `6/6`, and Ruff. The exact lambda, channel scope, architecture, blend, and cap are frozen while P2 moves to an independent v24 family.

P1 v25 rejected Deep-SVDD at zero fits because the repository already executed the same normal-embedding-to-center-distance-to-threshold mechanism through a TS2Vec conditional normal prototype. Before v26, a separate prospective guard amendment was sealed: a threshold must now span both chronological halves, at least two distinct station-layer identities, and at least two distinct stations, with precision above `0.55` and nonzero TP in every supported cell. It does not retroactively alter v24. P1 v26 then trained a fixed label-free Barlow-Twins redundancy-reduction encoder and a frozen-encoder linear add-only head. The exactly `9`-fit run completed in `23.032 s`; several quantiles had adequate station/cell diversity, but best precision/LCB was only `0.438596/0.335712` in Q2, `0.25/0.190078` in Q3, and `0.093548/0.079000` in Q4. No threshold passed, so F1, CI, recalls, additions/removals, and points were exact no-ops. Root lifecycle QA passed `12/12`, focused pytest `4/4`, and Ruff; result SHA-256 is `241e050c3a73d908e675be0f748b84e233b08a5adf7d456dfbb0b57af333064f`. The amended guard remains prospective while the v26 objective is closed.

P3 v43 represented the unconditional increment distribution through a fixed empirical characteristic function at three lags and three distribution frequencies. It reused neither v42 state bins nor any prior prediction. Ridge-2048/add-0.10 improved RMSE by `0.001832040 m`, nominal `+0.029076` points, and `5/6` blocks. Episode CI90 `[-0.003233,-0.000342]` and block-station CI90 `[-0.003446,-0.000013]` were favorable, all stations improved, and every fixed guard passed, but transport-adjusted points remained `-0.020510` and several lead/block margins were weaker than v42. Root independent QA passed `20/20`, focused pytest `7/7`, and Ruff. v43 is a valid stable corroborating candidate but its lag/frequency/ridge/blend family is closed; v42 remains the leader.

P1 v27 trained a fixed two-class Dirichlet evidential objective under the amended multi-station guard. Q2 inner selection appeared strong and diverse: precision `0.900709`, Wilson LCB `0.851444`, both chronological halves, two station-layer identities, and two stations. On untouched Q2 outer data, however, the same action made `332` additions with only `4` true positives, outer precision `0.012048`. F1 fell by `0.009097718` to `0.851385886`, CI90 `[-0.012563785,-0.006524519]`, nominal `-0.241821` and transport-adjusted `-0.072546` points. Root lifecycle QA passed `12/12`, focused pytest `4/4`, and Ruff. v27 is closed without uncertainty or threshold retuning. It demonstrates that cell identity diversity alone is insufficient; the next prospective P1 protocol must require one threshold to survive at least two distinct historical windows before a later window is scored.

P3 v44 used fixed contemporaneous and lagged forward/reverse distance-correlation features. Target-free support was `0.801698` with `55/55` positive-variance features, and a nonlinear `x^2` synthetic guard detected dependence despite near-zero Pearson correlation. Ridge-512/add-0.10 improved RMSE by `0.002760403 m`, nominal `+0.043810` points, and `5/6` blocks. Both CIs and all fixed station/lead/tail guards passed, but transport-adjusted points were `-0.005776`, leaving v42 clearly ahead. Root independent QA passed `20/20`, focused pytest `7/7`, and Ruff. v44 is a stable, frozen corroborating family; P3 continues with a separately fingerprinted multifractal axis.

P1 v28 is a prospective validation amendment, not a retrospective rescue. One ensemble and one numeric threshold must now be chosen strictly before Q2, then pass the full multi-station guard, precision `>0.55`, nonzero TP, and Wilson-90 LCB `>=0.55` independently on both Q2 and Q3 without refitting or threshold reselection. Only after both veto receipts pass may untouched Q4 labels be opened for performance scoring. The maximum remains nine supervised fits with three fixed seeds trained once. v24/v27 are not re-evaluated, and official/hidden/test/sample/submission reads, CSVs, uploads, sweeps, retries, and anchor removals remain zero.

P2 v24 added only fixed vanilla SAM parameter-neighborhood optimization (`rho=0.05`) to the v13 architecture, weighted SmoothL1 objective, domain weights, prefix/purge contract, seeds, epochs, blend, and action cap. The exactly `9`-fit run completed in `119.33 s`. RMSE improved by `0.049469328 C`, nominal `+0.620718` and fixed-penalty `+0.499036` points, with CI90 `[-0.078475149,-0.036852402] C`; every fold, month, and layer improved and action p99/max remained `0.5 C`. Root independent QA passed `36/36`, focused pytest `6/6`, and Ruff. v24 is a valid exploratory safety pass but trails v23 by `0.002423139 C`, so v23 remains internal #1 and the exact SAM family is closed without rho or optimizer retuning.

P3 v45 closed at zero fits. Multifractal DFA had already been explicitly rejected in the v33/v35 semantic audits as overlapping the closed wavelet/scattering and global-spectral lineages. Reopening it would duplicate an earlier decision rather than test a new information axis. No model, target, official input, hidden value, CSV, or upload was used. P3 therefore proceeds to a separately fingerprinted higher-order dependence representation.

P3 v46 encoded fixed ternary rank-symbol O-information over four physical triplets and full/recent past-only windows. Synthetic redundant-copy and XOR guards had the expected opposite signs (`+0.693147/-0.693147`), historical nonzero support was `0.756525`, all `16/16` features had positive variance, and two preflights were byte-identical. The sealed `12`-fit run selected Ridge-2048/add-0.10, improving RMSE by only `0.001368149 m`, nominal `+0.021714` points, and `5/6` blocks. Episode CI90 `[-0.002659,+0.000003]` and block-station CI90 `[-0.002775,+0.000263]` both crossed zero, while fixed transport-adjusted points were `-0.027873`. Root independent QA passed `20/20`, focused pytest `7/7`, and Ruff. v46 is `NO_GO_ALL_TRIPLET_OINFORMATION_CANDIDATES`; its symbols, triplets, windows, ridge, and blend are closed without retuning, and v42 remains the P3 leader.

P1 v29 was the first new learning axis evaluated under the sealed v28 cross-quarter contract. A fixed three-seed variational information bottleneck trained once before Q2; two zero-operation preflights were byte-identical and the run used `3` of at most `9` fits in `9.219 s`. All three pre-Q2 quantiles had high pooled precision/LCB, but none met the environment contract: `0.995` included a G-ORS/L1 half with `0/27` TP, `0.9975` collapsed to S-ORS/L5, and `0.999` had only one station/cell/half. No threshold was sealed, so Q2/Q3 transport targets and Q4 labels were not opened, Q4 actions and anchor removals were zero, and points were `0/0`. Root hash/lifecycle validation, focused pytest `6/6`, and Ruff passed. v29 is `NO_GO_CROSS_QUARTER_TRANSPORT_VETO_Q4_UNOPENED`; its objective and thresholds are closed without retuning.

P2 v25 replaced only the v13 final mean head and SmoothL1 loss with a fixed mean-plus-conditional-log-variance head and bounded heteroscedastic Gaussian NLL; inference used the mean only, with no variance router, abstention, blend, sweep, or row deletion. The exactly `9`-fit run completed in `70.67 s`. RMSE improved by `0.045023193 C`, nominal `+0.564930` and fixed-penalty `+0.443248` points, with CI90 `[-0.071872388,-0.033030788] C`; every fold, month, and aggregate layer improved. Jul-Aug layer 2/3 nevertheless regressed by `+0.004093/+0.007908 C`, and the Nov-Dec log-variance head occupied its lower clamp on roughly `57–60%` of rows. Root independent QA passed `36/36`, focused pytest `6/6`, and Ruff. v25 is a valid exploratory safety pass but is `0.006869273 C` weaker than v23, so its head, clamp, NLL, and variance-routing family are closed.

P3 v47 encoded fixed discrete three-dimensional trajectory geometry over four physical triplets and full/recent windows. Line, circle, and helix guards separated zero curvature, nonzero curvature, and nonzero torsion; historical support was `0.931166`, all `72/72` features varied, and two preflights were byte-identical. Ridge-2048/add-0.10 improved RMSE by `0.001727608 m`, nominal `+0.027418` points, and `5/6` blocks. Episode CI90 `[-0.003112,-0.000312]` and block-station CI90 `[-0.003195,-0.000067]` were favorable and all fixed harm ceilings passed, but transport-adjusted points remained `-0.022168`. Root independent QA passed `20/20`, focused pytest `7/7`, and Ruff. v47 is a stable corroborating family but weaker than v42; its geometry, triplets, windows, ridge, and blend are closed without retuning.

P3 v48 represented within-case early-to-late distribution drift using fixed one-dimensional optimal-transport summaries for all twelve transformed channels. Synthetic location, scale, and tail-shift guards, historical support `0.820971`, all `48/48` positive-variance features, future isolation, and two byte-identical preflights passed. Ridge-2048 improved RMSE by only `0.001224835 m`, nominal `+0.019439` points, and `5/6` blocks. Episode CI90 `[-0.002689,+0.000271]` and block-station CI90 `[-0.002518,+0.000252]` both crossed zero, and fixed transport-adjusted points were `-0.030147`. Root independent QA passed `20/20`, focused pytest `7/7`, and Ruff. v48 is `NO_GO`; its half split, transport summaries, ridge, and blend are closed without retuning, while v42 remains the leader.

P1 v30 used a genuinely new causal one-sample-delayed Teager nonlinear-energy representation with fixed three-seed linear probe under the unchanged v28 contract. Two preflights were byte-identical and `3` fits completed in `8.625 s`. Pooled precision/LCB was extremely high at every fixed quantile (`0.921708/0.891147`, `1.0/0.981173`, and `1.0/0.954685`), but every supported proposal collapsed to S-ORS/L5; the strictest quantile covered only the second chronological half. The environment gate therefore rejected all thresholds before Q2. Q2/Q3 transport targets and Q4 labels stayed unopened, Q4 actions and anchor removals were zero, and no Q4 F1 or CI was computed. Root hash/lifecycle validation, focused pytest `5/5`, and Ruff passed. v30 is `NO_GO_CROSS_QUARTER_TRANSPORT_VETO_Q4_UNOPENED`; its representation, probe, quantiles, and threshold family are closed without retuning.

P1 v31 audited the guard itself using only already-open receipts. Its prospective confusion table has one observable pass/nonbeneficial case (v27), while both v28 rejects lack independent outcomes because the contract correctly left their targets unopened. Sensitivity, specificity, and negative predictive value are therefore non-identifiable; v28 acceptance and independent-outcome coverage are both `0/2`. A separately sealed v32 audit-only adjudication attempted to evaluate the fixed maximum-LCB v29/v30 rejects without refitting, but both namespaces lacked persisted model states and label-blind Q2 score/action arrays. It closed as `INVALID_TECHNICAL_PRETARGET_STATE_UNAVAILABLE` before any target read, with fits/refits/reselection/new-label/Q2-Q4/official/CSV/upload all zero. The original decisions remain unchanged. Prospective v33 now requires every new rejected candidate to preserve authenticated label-blind Q2 scores/actions, model/scaler hashes, and bundle hashes before target access; later audit still requires a new diagnostic-only ID and cannot promote or rescue the model.

P2 v26 used deterministic same-layer-and-calendar-month MixUp with fixed `Beta(0.2,0.2)` coefficients, intersection masks, and exact no-op fallback when fewer than two public tokens remained. The v13 architecture, loss, domain weights, prefix/purge, seeds, epochs, blend, and action cap stayed fixed. The exactly `9`-fit run completed in `89.225 s`, improving RMSE by `0.048727632 C`, nominal `+0.611411` and fixed-penalty `+0.489729` points, with CI90 `[-0.078103451,-0.035738076] C`; every fold, month, and aggregate layer improved. Only `6/9` fold-by-layer cells were non-harm: Jul-Aug L2/L3 regressed by `+0.028924/+0.011727 C`, and Nov-Dec L4 by `+0.011108 C`. Root independent QA passed `33/33`, focused pytest `6/6`, and Ruff. v26 is weaker than v23 and its MixUp family is closed without alpha retuning.

A zero-fit P2 gate audit compared the already-open fold-by-layer receipts from v20-v26. Only v24 passed `9/9`; v20, v21, v23, and v26 passed `6/9`, while v25 passed `7/9`. This shows that aggregate fold/month/layer checks can hide localized harm. Without changing any historical conclusion, future v27+ candidates prospectively add a `>=8/9` non-harm requirement and maximum cell regression `<=0.003 C` to the existing gates.

P3 v49 encoded fixed white/black morphological top-hat scale spectra at structuring sizes `3/7/15`. Positive and negative impulse guards separated white and black responses, historical support was `0.921627`, all `72/72` features varied, and two preflights were byte-identical. Ridge-512 improved RMSE by `0.002871612 m`, nominal `+0.045575` points, and `4/6` blocks; both uncertainty intervals and all fixed harm ceilings passed, but transport-adjusted points remained `-0.004011`. Root independent QA passed `20/20`, focused pytest `7/7`, and Ruff. v49 is stable but weaker than v42, so morphology scales, summaries, ridge, and blend are closed.

P3 v50 reused only the generic Teager-Kaiser mechanism—not any P1 object—to construct fixed multiscale nonlinear signal-energy summaries for hs/tp/hmax/wspd at lags `1/3/6`. Constant, sinusoid, and quadratic amplitude-scaling guards passed to numerical tolerance; historical support was `0.921612`, all `60/60` features varied, and two preflights were byte-identical. Ridge-512 improved RMSE by `0.002243412 m`, nominal `+0.035605` points, and `4/6` blocks. Both CIs and fixed harm ceilings passed, but transport-adjusted points were `-0.013981`. Root independent QA passed `20/20`, focused pytest `7/7`, and Ruff. v50 is stable but weaker than v42 and is frozen without lag, summary, ridge, or blend retuning.

P3 v51 measured fixed early-to-late drift on the symmetric-positive-definite correlation manifold for four preregistered channel groups. Identity and common-permutation guards were invariant, a rotated synthetic covariance produced nonzero log-Euclidean distance, historical support was `0.874771`, all `24/24` features varied, and two preflights were byte-identical. Ridge-512 improved RMSE by `0.001452873 m`, nominal `+0.023058` points, and `5/6` blocks. Episode CI90 `[-0.002771,-0.000092]` was favorable, but block-station CI90 `[-0.002802,+0.000262]` crossed zero and transport-adjusted points were `-0.026528`. Root independent QA passed `20/20`, focused pytest `7/7`, and Ruff. v51 is `NO_GO`; the SPD groups, summaries, ridge, and blend are closed without retuning.

P1 v34 applied a fixed causal detrended-fluctuation representation at `24/48/96` rows under v28 plus the v33 auditability amendment. Two preflights were byte-identical and `3` fits completed in `8.641 s`. All fixed pre-Q2 thresholds lacked precision (`0.071170/0.028370/0`) and Wilson LCB (`0.049820/0.012800/~0`), so no threshold was selected. Q2/Q3/Q4 targets remained unread, actions/removals/points were zero, and the exact DFA family closed without retuning. Unlike v29/v30, v34 correctly preserved target-free Q2 score/action arrays for all three thresholds, their model hashes, and bundle/manifest hashes before the veto. Root hash/lifecycle validation, focused pytest `6/6`, and Ruff passed; the first attempt to run tooling from a dependency-incomplete `.venv` was a zero-operation preparation issue and did not consume the science namespace.

P2 v27 spectral normalization terminated technically after the first fold/seed trained and inferred but before any fit receipt, prediction commitment, metric, or result was persisted. The runtime contract reported `v27 training/spectral contract failed`; because its receipt was not yet written, read-only evidence cannot distinguish a finite-value failure from one of five effective norms leaving `[0.98,1.02]`. Operational attempted fits were `1`, committed fits `0`, and the attempt lock was consumed. Root inspection confirmed the exact traceback, lock hash, focused pytest `6/6`, and Ruff. v27 is `TERMINAL_TECHNICAL_INVALID`, not a scientific NO_GO; same-ID restart, tolerance/power-iteration changes, and selective-layer repair are prohibited, and the spectral family is closed.

P3 v52 used fixed variational-mode decomposition with `K=3`, penalty `800`, `40` iterations, and deterministic center initialization. Exact-reconstruction residual features were removed during target-free preflight because they were identically zero; this was a mathematical cleanup before any target/outer score. Two-tone frequency recovery, reconstruction, support `0.934753`, all `72/72` positive-variance features, future isolation, and two byte-identical preflights passed. Ridge-2048 improved RMSE by `0.001550619 m`, nominal `+0.024609` points, and `5/6` blocks. Episode CI90 `[-0.002859,-0.000159]` passed, but block-station CI90 `[-0.002919,+0.000005]` narrowly crossed zero and transport-adjusted points were `-0.024977`. Root independent QA passed `20/20`, focused pytest `7/7`, and Ruff. v52 is `NO_GO` and is frozen without K, penalty, iteration, mode, ridge, or blend retuning.

P1 v35 used the newly preserved v34 artifact to adjudicate one rejected candidate without retraining or reconstructing scores. The maximum pre-Q2 Wilson-LCB rule fixed q=`0.995` and threshold `0.6668935418` before target access; the audit then opened Q2 only. The unchanged action added `332` rows with `0` TP and `332` FP, reducing F1 from `0.778413575` to `0.753725640` (`-0.024687935`). Fits, refits, score recomputation, threshold reselection, Q3/Q4, official/hidden/test/sample/submission, CSV, and upload were zero. Root hash/lifecycle validation, focused pytest `4/4`, and Ruff passed. v35 is `GUARD_TRUE_NEGATIVE_ON_THIS_FIXED_REJECT`: v28 correctly vetoed this one harmful candidate, but one audited reject is insufficient to identify overall sensitivity, so only future predeclared audit-only sampling continues.

P3 v53 encoded fixed realized variation, bipower variation, and jump-share summaries over four channels, full/recent windows, and lags `1/3/6`. Constant and smooth-path guards yielded zero jump share, a single-spike path yielded `0.195446`, historical support was `0.707952`, all `72/72` features varied, and two preflights were byte-identical. Ridge-512 improved RMSE by `0.004607351 m`, nominal `+0.073122` and fixed-penalty `+0.023536` points, with `5/6` blocks. Episode CI90 `[-0.006803,-0.002366]` and block-station CI90 `[-0.006410,-0.002531]` were favorable; all stations and fixed harm ceilings passed. Root independent QA passed `20/20`, focused pytest `7/7`, and Ruff. v53 becomes the stable P3 #2, close to but still weaker than v42 (`-0.004855893 m`, `+0.027481` adjusted), and its exact statistics, lags, ridge, and blend are frozen.

### 09:10 KST continuation checkpoint

P1 v36 attempted a causal generalized-cross-entropy objective with fixed `q=0.7`, but its terminal receipt authenticated only the imported shared v34 engine and omitted the actual wrapper SHA. The wrapper was `1f11885d944131761f0efe6a66449e31f39c0b6ba7a4b0eb90ecbef626b369ee`, while the result recorded the shared engine `a04bcee6...`; the inherited QA therefore could not establish the full executed code identity. Even though three fits completed and all transport targets remained unopened, v36 is classified `INVALID_TECHNICAL_PROVENANCE_WRAPPER_HASH_OMITTED`, and none of its scientific values are used. Same-ID restart is prohibited. A fresh v36r1 may change only provenance capture, must record both wrapper and shared-engine hashes before and after execution, and must keep the v28/v33 science and target-access contract byte-equivalent.

P2 v28 applied PCGrad across the fixed layer-2/3/4 tasks. The exactly `9`-fit result improved RMSE by `0.050893587 C`, corresponding to nominal `+0.638589` and fixed-penalty `+0.516907` points; every aggregate fold, month, and layer improved and the fold-by-date bootstrap CI90 was wholly favorable at `[-0.082006917,-0.037324072] C`. The prospective cell gate still failed: only `6/9` fold-by-layer cells were non-harm and maximum harm was `+0.028618268 C`, with Jul-Aug L2/L3 and Nov-Dec L4 worsening. A report-format `KeyError` happened only after immutable result and prediction commitments; the result was preserved without rerun by a terminal amendment. Root recomputation passed QA `36/36`, focused pytest `7/7`, and Ruff. v28 is `EXPLORATORY_NO_GO_LAYER_TASK_PCGRAD`, remains weaker than v23, and the PCGrad family is closed without task-order or projection retuning.

P3 v54 encoded fixed rainflow cycle-spectrum features. Ridge-based correction improved RMSE by `0.003754359 m`, nominal `+0.059584` and fixed-penalty `+0.009998` points, with `5/6` blocks, both uncertainty intervals, all six leads, all stations, and fixed harm guards favorable. Root independent QA passed `20/20`, focused pytest `7/7`, and Ruff; result SHA-256 is `c289b371a00553b7221af811b8ef00fc202ee740a7f6356d0f701f63f19b1f31`. v54 is a stable candidate but trails both v42 and v53, so its cycle-counting specification, ridge, and blend are frozen while v55 evaluates a separately preregistered Rosenstein local-divergence representation.

P3 v55 used a fixed Rosenstein local-divergence representation with embedding dimension `3`, delay `2`, Theiler exclusion `12`, and horizons `1..6`. Target-free preflight replaced one mathematically constant valid-neighbor feature with a sealed q90 initial-neighbor-distance summary before any outer score; `72/72` final features varied and two preflights were byte-identical. Ridge-2048 improved RMSE by `0.001850093 m`, nominal `+0.029362` points, but fixed-penalty points were `-0.020224`; `5/6` blocks and both CIs were favorable, while its gain was materially weaker than v42/v53/v54. Independent QA passed `20/20`, focused pytest `7/7`, and Ruff; result SHA-256 is `532e6a02c5ef93220849165fd2ab5d38379652ed53d122df94a51057071a0bd8`. v55 is frozen as a stable but weak candidate, and v56 moves to a distinct fixed real-cepstral echo/periodicity representation.

### 07:52 KST official checkpoint

With fresh exact-file approval, P2 v23 and P3 v42 were materialized, independently QA'd, and submitted once. P2 v23 improved the Public result from RMSE `0.430194 C` / `27.935464` points to `0.424976 C` / `28.000939` points: actual deltas `-0.005218 C` and `+0.065475` points. This is a new P2 Public champion, although it captured only a small fraction of the historical nominal `+0.651122`-point estimate; the observed transport shortfall is now part of calibration evidence. P3 v42 reversed sign on Public, moving from RMSE `0.575233 m` / `24.203599` points to `0.576320 m` / `24.186338` points: actual deltas `+0.001087 m` and `-0.017261` points. The P3 uniform-0.425 incumbent remains champion. Both problems retain `2/3` submissions for the day; P1's exact best CSV was already officially scored at F1 `0.833548` / `28.909341` and was not wastefully resubmitted. Research continues because `+0.1` is not a stop rule.

P1 v36r1 then completed the science-neutral provenance recovery under a fresh namespace. It authenticated the wrapper, frozen science module, and shared engine independently before and after execution, did not read or reuse v36's invalid scientific artifacts, and preserved a fresh v33 target-free bundle. Three fits completed in `9.047 s`; all fixed thresholds showed high pooled inner precision but collapsed to a zero-TP environment or one station-layer/half, so the v28 cross-quarter gate rejected them before Q2 target access. Q2/Q3/Q4 targets and official data stayed unopened, actions/removals were zero, and the valid decision is `NO_GO_CROSS_QUARTER_TRANSPORT_VETO_Q4_UNOPENED`. Root post-terminal QA passed all `21` checks, focused pytest `5/5`, and Ruff; result SHA-256 is `bbb5231c0354acbe0e852722ea34bb913e837ca99993597c3736e61f4b7087ca`. The GCE family is closed without retuning.

P2 v29 changed only the optimizer trajectory to fixed Lookahead (`k=5`, `alpha=0.5`) around the unchanged AdamW inner optimizer. Nine fits improved RMSE by `0.048278095 C`, nominal `+0.605771` and fixed-penalty `+0.484089` points, with favorable aggregate folds, layers, and CI90 `[-0.077161551,-0.035437908] C`. The prospective fold-by-layer gate again failed in the same cells: only `6/9` were non-harm and maximum harm was `+0.022354348 C` at Nov-Dec L4, while Jul-Aug L2/L3 also worsened. Root QA passed `35/35`, focused pytest `7/7`, and Ruff. v29 is `EXPLORATORY_NO_GO_LOOKAHEAD_OPTIMIZER`, weaker than v23, and the Lookahead family is closed without k/alpha retuning.

P3 v56 used a fixed real-cepstral echo/periodicity representation. The best Ridge-2048/add-0.10 candidate improved historical RMSE by only `0.001617594 m`, nominal `+0.025672` points, and `5/6` blocks. Episode CI90 was favorable, but the block-station interval crossed zero and fixed transport-adjusted points were `-0.023914`. Root QA passed `20/20`, focused pytest `7/7`, and Ruff; result SHA-256 is `0e9e7d83d6010f614e62beb0e01d75b16bd1f81a39bc2181063c0eac5b084227`. v56 is `NO_GO_ALL_REAL_CEPSTRAL_CANDIDATES`; its cepstral specification, ridge, and blend are closed without retuning.

P3 v57 used fixed rank-tertile LZ76 dictionary-complexity summaries. Ridge-512 improved historical RMSE by `0.002232717 m`, nominal `+0.035435` points, and `5/6` blocks; both episode and block-station CIs were favorable and all fixed harm guards passed. Fixed transport-adjusted points remained `-0.014151`, and the candidate was weaker than v42/v53/v54. Root QA passed `20/20`, focused pytest `5/5`, and Ruff; result SHA-256 is `4ef95bc35169704aa6942f3c9db14f7e92d01548e84312cb8f01d70b5b7ef0d2`. v57 is frozen as stable-but-weaker; its rank alphabet, parser, summaries, ridge, and blend are closed.

P3 v58 tested fixed contemporaneous empirical tail-copula geometry at sealed lower/upper quantiles. Synthetic comonotonic-versus-independent and marginal-monotone guards passed, all `56/60` nonconstant features had positive variance, and two target-free preflights were byte-identical. The historical nonzero share was only `0.486538`, below the sealed `0.55` support floor, so the run closed as `STOP_SUPPORT_GATE_ZERO_FIT` without opening historical targets or fitting an outer model. Official v42 feedback was excluded from features, gates, and selection. Root QA passed `12/12`, focused pytest `5/5`, and Ruff; result SHA-256 is `5236eef93b82db0146c84a4f2f3a88ff0570e6554e0c75a7db4a938e43dd34f8`. No score estimate is valid for v58, and its quantiles/support gate are not relaxed or retuned; research moves to a distinct v59 axis.

P3 v59 estimated fixed delay-cloud intrinsic-dimension profiles from local k-nearest-neighbor distance ratios. Ridge-2048/add-0.10 improved historical RMSE by `0.001806988 m`, nominal `+0.028678` points and `5/6` blocks; episode CI90 `[-0.003195594,-0.000382254] m` and block-station CI90 `[-0.003353740,-0.000024947] m` were favorable, and all fixed harm ceilings passed. Its planning score is approximately `24.232277`, but the fixed transport-adjusted delta is `-0.020908` points and the candidate is materially weaker than v42/v53/v54. Root QA passed `20/20`, focused pytest `5/5`, and Ruff; result SHA-256 is `a7ffc8a0621a44fc4e0836fc73c417048c83255c71ec43fb9c52fdcb8c83701e`. v59 is frozen as stable-but-weaker, not submission eligible, and its embedding, neighbors, windows, ridge, and blend are closed without retuning.

P1 v37 terminated before any optimizer step, fit, or target access because a standardized binary support column was incorrectly compared with the raw `0.5` cutoff. The immutable failure is `INVALID_TECHNICAL_SUPPORT_BIT_STANDARDIZATION`; same-ID restart is prohibited. Fresh v37r1 changed only supported-row identification to the standardized column's binary maximum and added a regression test, while keeping v28/v33, seeds, thresholds, features, and science unchanged. Its two real preflights were byte-identical and exactly `3` fits completed in `8.922 s`, but the fixed temporal-order candidate achieved only `10/460` TP, precision `0.021739`, Wilson-90 LCB `0.013039`, with zero TP in both supported halves of G-ORS/L1. It therefore closed at the pre-Q2 calibration gate with Q2/Q3/Q4 targets unopened, no actions/removals, and zero point delta. Root lifecycle QA passed `22/22`, focused pytest `5/5`, and Ruff; result SHA-256 is `a56c85f9a2867604207d61e53ecca867e60f2430ed1dc0f7e33dee21fe8586e7`. The temporal-order family is closed without retuning, and P1 continues on a distinct v38 axis.

P1 v38 changed only the fixed loss to causal focal loss with gamma `2`, preserving the same v28/v33 validation and add-only contract. Two real preflights were byte-identical and exactly `3` fits completed in `9.156 s`. The best pooled-inner threshold reached precision `0.858156` and Wilson-90 LCB `0.803069`, but the supported G-ORS/L1 environment again had `0/11` TP; the stricter threshold collapsed to a single S-ORS/L5 environment. The run therefore closed validly at `PRE_Q2_CALIBRATION_GATE` with Q2/Q3/Q4 targets unopened, no action/removal, and zero expected point increment. Root lifecycle QA passed `20/20`, focused pytest `5/5`, and Ruff; result SHA-256 is `a8d5f31f580507005e63ae98733d65c7093ade240d9bd4d5d5a0ec44ef9a982a`. The focal family is frozen without threshold or gamma retuning, and P1 proceeds to a distinct v39 axis.

P3 v60 encoded fixed extreme-order absolute-increment tail geometry with Pickands k=`4` statistics. Ridge-512/add-0.10 improved historical RMSE by `0.001692083 m`, nominal `+0.026855` points and `5/6` blocks; episode CI90 `[-0.003347169,-0.000004959] m` and block-station CI90 `[-0.003228795,-0.000124976] m` were favorable, and all fixed harm ceilings passed. Its planning score is approximately `24.230454`, but the fixed transport-adjusted delta is `-0.022731` points and it remains materially weaker than v42/v53/v54. Root QA passed `20/20`, focused pytest `5/5`, and Ruff; result SHA-256 is `96843354edfe0414c53a75a2e6a01f8f16984aa91e06d982d31bde5af5659d5f`. v60 is frozen as stable-but-weaker, not submission eligible, and its k/statistics/lags/ridge/blend family is closed without retuning.

P2 v30 added a fixed IRMv1 scalar-gradient penalty across layer-by-calendar-month training environments while preserving the v13 task architecture and deployment contract. Nine fits completed in `548.935 s`. RMSE improved by `0.049380924 C`, nominal `+0.619609` and fixed-penalty `+0.497927` points; all aggregate folds, months, and layers improved and CI90 was `[-0.079416825,-0.036047467] C`. The prospective fold-by-layer gate nevertheless failed at `6/9` non-harm cells with maximum harm `+0.029760029 C`: Jul-Aug L2/L3 and Nov-Dec L4 regressed. Root QA passed `36/36`, focused pytest `7/7`, and Ruff; result SHA-256 is `8d82a21aa0b41e53b65e8764518e448207dc9106e487f3182f96cfac1fff66d9`. v30 is `EXPLORATORY_NO_GO_IRMV1_LAYER_MONTH_INVARIANCE`; the IRMv1 family is closed without coefficient or schedule retuning, and v23 remains both the internal and official leader while v31 tests a distinct adversarial-invariance objective.

P1 v39 added fixed R-Drop consistency with dropout `0.1` and symmetric KL coefficient `1`, preserving v28/v33 and the add-only validation contract. Two preflights were byte-identical and exactly `3` fits completed in `9.813 s`. The best pooled threshold reached precision `0.929078` and Wilson-90 LCB `0.884864`, but its supported action collapsed to the single S-ORS/L5 identity; the broader threshold retained a G-ORS/L1 cell with `0/31` TP. It therefore closed at the pre-Q2 gate with all Q2/Q3/Q4 targets unopened and zero action/removal/point increment. Root lifecycle QA passed `20/20`, focused pytest `5/5`, and Ruff; result SHA-256 is `bdeed73101ed09c0b2eb268768554f8cdcbe048e5901b7da17839d5c2f31da7c`. The R-Drop family is frozen without coefficient/dropout/threshold retuning, and P1 proceeds to a distinct v40 axis.

P2 v31 used three layer-specific 12-way calendar-month domain classifiers through fixed GRL coefficient `0.1`, leaving the v13 task path unchanged. Nine fits completed in `138.153 s`. RMSE improved by `0.040325950 C`, nominal `+0.505991` and fixed-penalty `+0.384309` points; CI90 was favorable at `[-0.062734971,-0.030964190] C`. The prospective cell gate improved to `7/9`, but still failed because Nov-Dec L3/L4 regressed and maximum harm was `+0.020189326 C`. Root QA passed `39/39`, focused pytest `7/7`, and Ruff; result SHA-256 is `e73dbba22d5e2f3f1b5b15583d32ec2ee4bbfe3dc924af9fa2a252a5003325a7`. v31 is `EXPLORATORY_NO_GO_WITHIN_LAYER_MONTH_ADVERSARY`; the adversarial coefficient/schedule family is closed without routing or ensemble reuse, while v32 tests the distinct score-aligned-loss question.

P3 v61 computed exact Vietoris-Rips H1 loop persistence on fixed sliding-window delay clouds. Synthetic line-versus-circle, scale/translation, future-isolation, determinism, and all `32/32` variance guards passed, but historical nonzero share was only `0.398180`, below the sealed `0.50` floor. It closed as `STOP_SUPPORT_GATE_ZERO_FIT` before historical targets or outer scoring, with QA `14/14`, focused pytest `5/5`, Ruff, and result SHA-256 `c1f341a13bdcd54dce0eb9d0db2fad4f975857f87e5a76bb98d01604cee5c89f`. The cloud/delay/H1/support specification is not relaxed or retuned.

P3 v62 encoded fixed Gramian angular summation/difference lag-diagonal geometry. Ridge-2048/add-0.10 improved historical RMSE by only `0.001167459 m`, nominal `+0.018528` points and `5/6` blocks; both episode and block-station CI90 intervals crossed zero, and fixed transport-adjusted gain was `-0.031058` points. Its planning score was approximately `24.222127`, below the stable P3 alternatives. Root QA passed `20/20`, focused pytest `5/5`, and Ruff; result SHA-256 is `3204fa769c30821004fab441f61c171775fbeb83383fc83711b7458647269889`. v62 is frozen as `NO_GO`, not submission eligible, and P3 proceeds to the distinct v63 volatility-clustering axis.

P1 v41 evaluated whether co-teaching was scientifically supported before any fit. Although the two-peer/three-seed accounting fit within the nine-fit cap and repo execution was novel, the allowed README defines labels as ground truth and supplies no corruption process, noise rate, independent annotators, trusted-clean subset, or target-free noise proxy. Inferring a rate from losses would itself consume targets and make the support decision outcome-dependent. v41 therefore closed as `NO_GO_TARGET_FREE_LABEL_NOISE_SUPPORT_UNIDENTIFIED` with fits, targets, locks, artifacts, actions, removals, official rows, CSVs, and uploads all zero. The co-teaching family is frozen, and P1 proceeds only to an axis whose support is identifiable from train covariates.

P3 v63 encoded fixed autocorrelations of absolute and squared increments at lags `1/3/6/12` as a volatility-clustering profile. Ridge-512/add-0.10 improved historical RMSE by `0.002791993 m`, nominal `+0.044311` points and `5/6` blocks; both CI90 intervals were favorable and all fixed guards passed. Its planning score was `24.247910`, but fixed transport-adjusted gain remained `-0.005275` points, leaving it weaker than v42/v53. Root QA passed `20/20`, focused pytest `5/5`, and Ruff; result SHA-256 is `6f3fb641cb7b3c93415498f60b301aa6e1b38b625716004ce54583dc9053d317`. v63 is frozen as stable-but-weaker without lag/ridge/blend retuning.

P3 v64 measured scale-dependent stability and drift through fixed overlapping Allan and Hadamard variance spectra. Ridge-512/add-0.10 improved historical RMSE by `0.005263867 m`, nominal `+0.083541` points, fixed transport-adjusted `+0.033955` points, and all `6/6` blocks. Episode CI90 `[-0.007554589,-0.003076984] m` and block-station CI90 `[-0.007262062,-0.003121797] m` were strongly favorable; all sealed block/lead/station-lead/tail guards passed. The planning score is `24.287140`, action p99/max are `0.074723/0.143588 m`, and output range is finite. Root QA passed `20/20`, focused pytest `5/5`, and Ruff; result SHA-256 is `8236316322ec2b5027ee2e037c45dce111808a8614e2bc5893651f02902e30e4`. v64 is the new immutable P3 internal leader, but remains exploratory until exact materialization approval because v42's earlier Public sign reversal demonstrates material transport risk.

P2 v33 used fixed virtual-domain MLDG with one differentiable inner step and a cyclic held-out layer-month environment, preserving the exact v13 pipeline. Nine fits completed in `265.94 s`. RMSE improved by `0.050293901 C`, nominal `+0.631064` and fixed-penalty `+0.509382` points; all folds and months improved and CI90 was `[-0.080417608,-0.037116730] C`. The prospective fold-layer gate still failed at `6/9` cells with maximum harm `+0.011366274 C`, concentrated in Jul-Aug L2/L3 plus a small Nov-Dec L4 regression. Root QA passed `38/38`, focused pytest `8/8`, and Ruff; result SHA-256 is `86cb0382bfb4bde9f9583271808c0d72edfd927ea4531de891e9699c36648467`. v33 is `EXPLORATORY_NO_GO_VIRTUAL_DOMAIN_MLDG`; the MLDG family is frozen without inner-step or coefficient retuning, and v34 tests a distinct optimizer transformation.

P1 v42 encoded fixed causal Streaming Half-Space Tree prefix-mass rarity. Two preflights were byte-identical, target-free support was complete, and exactly `3` fits completed in `11.797 s`. The best threshold reached precision `0.744681` and Wilson-90 LCB `0.680067` across three identities/stations, but supported G-ORS/L1 still had `0/19` TP. It therefore closed at the pre-Q2 cross-quarter veto with all downstream targets unopened and zero action/removal/point increment. Root QA passed `17/17`, focused pytest `6/6`, and Ruff; result SHA-256 is `67091d062544f643064ad7153b7e44c49c291bc208bbc9b9cb7c36cc977d8810`. The half-space mass family is frozen without tree/threshold retuning, and v43 first audits reconstructibility before execution.

P2 v34 applied fixed Gradient Centralization to the five Linear weight gradients before AdamW while leaving biases and the v13 task path unchanged. Nine fits completed in `114.73 s`. RMSE improved by `0.051833901 C`, nominal `+0.650387` and fixed-penalty `+0.528705` points; all aggregate folds/months/layers and CI90 `[-0.083445565,-0.038132659] C` were favorable. The prospective gate still failed at `6/9` cells with maximum harm `+0.020429340 C` across Jul-Aug L2/L3 and Nov-Dec L4. Root QA passed `37/37`, focused pytest `6/6`, and Ruff; result SHA-256 is `e08b1e30fcaaa86bbec41657857f541b29e1a7657fa4ab4629d0c390561c46fa`. v34 is `EXPLORATORY_NO_GO_GRADIENT_CENTRALIZATION`; the optimizer transformation is frozen, and v35 tests a distinct fixed RAdam optimizer.

P3 v65 used fixed Hosking L-moment profiles over levels and increments. Ridge-512/add-0.10 improved historical RMSE by `0.002789241 m`, nominal `+0.044267` points and `5/6` blocks; both CI90 intervals were favorable, but fixed transport-adjusted gain was `-0.005319` points and the candidate remained well behind v64. Root QA passed `20/20`, focused pytest `5/5`, and Ruff; result SHA-256 is `dbf0154485aace600c1efdc4d3b3e77f4915a5e970a047ffdb42a0f71bf7d7f6`. v65 is frozen as stable-but-weaker without L-moment/ridge/blend retuning, and v66 tests a distinct sequential record-process axis.

P1 v43 performed the required semantic reconstructibility audit before allocating any fit. It found that v32b had already executed the exact causal random-interval morphology family: a 36-row past-only window, 32 Sobol intervals, eight fixed summaries over five channels, 1,280 features, and ExtraTrees. The sealed v32d continuation had then produced `-0.068800366` pooled delta F1, zero TP additions, 901 FP additions, and 531 incumbent TP removals. Rolling, MiniRocket, and direct-interval banks further reconstruct the same morphology. v43 therefore closed validly as `NO_GO_EXACT_FAMILY_DUPLICATE_CAUSAL_CIF_V32B_V32D` with zero preflights, fits, targets, locks, artifacts, actions, official rows, CSVs, or uploads. Independent QA passed all `11` closure checks; no duplicate execution or result-driven retune was created.

P3 v66 encoded adaptive upper/lower running-record arrival, gap, and terminal-age geometry. Ridge-2048/add-0.10 improved historical RMSE by only `0.001455231 m`, nominal `+0.023096` points and `5/6` blocks. Its episode CI90 was favorable, but the block-station CI90 upper bound was `+0.000026269 m`, fixed transport-adjusted gain was `-0.026490`, and the high-risk `-0.005 m` delta guard failed. Root QA passed `20/20`, focused pytest `5/5`, and Ruff; result SHA-256 is `7874c723f333e0d50d6bc2b1af8a986025e93c6ceb8a8c6d16ff471f2cf795eb`. v66 is frozen as `NO_GO_ALL_RECORD_PROCESS_CANDIDATES`, v64 remains the immutable P3 internal leader, and v67 moves to a distinct fixed rank-based Pettitt change-geometry axis.

P1 v44 preregistered a causal Fishr gradient-variance mechanism, but its label-free support audit found 29 station-layer-quarter environments with only 54 rows in the smallest cell, below the sealed 100-row stability floor. The gate was not relaxed after observation. The run closed as `CLOSE_ZERO_FIT_INSUFFICIENT_STATION_LAYER_PERIOD_SUPPORT` with zero fits, optimizer steps, Q2-Q4 target reads, official rows, locks, artifacts, actions, CSVs, or uploads. Root validation passed all `20` independent checks, focused pytest `6/6`, and Ruff; result SHA-256 is `584459fcb83ab95ada192ccc4b7975574a1ddadd031d12eebe03c318b3572a92`. v45 therefore changes the environment definition prospectively to a target-free, occupancy-justified station×quarter gradient-sign agreement question rather than relaxing v44.

P2 v35 changed only the optimizer to fixed decoupled-weight-decay RAdam. Nine fits improved pooled RMSE by `0.045479955 C`, nominal `+0.570661` and fixed-penalty `+0.448979` points; all aggregate folds and months improved and CI90 was `[-0.072610426,-0.033175691] C`. The prospective gate still failed at `7/9` non-harm cells with maximum harm `+0.023676814 C` in Jul-Aug L2, with Jul-Aug L3 also regressing. Root QA passed `35/35`, focused pytest `6/6`, and Ruff; result SHA-256 is `1e4ebf61b2ef5ac63244bf68a5723a1cffcf81e26e1fe2f0367a5be3d69ca9ff`. v35 is `EXPLORATORY_NO_GO_FIXED_RADAM`, the RAdam family is frozen, and v36 moves to a distinct fixed Fishr final-head gradient-variance axis.

P3 v67 encoded fixed retrospective Pettitt rank-change geometry across every internal split of each past-only window. Ridge-2048/add-0.10 improved historical RMSE by only `0.000976412 m`, nominal `+0.015496` points and `5/6` blocks. Episode CI90 `[-0.002417615,+0.000533973] m` and block-station CI90 `[-0.002523161,+0.000752593] m` both crossed zero, while fixed transport-adjusted gain was `-0.034090`. Root QA passed `20/20`, focused pytest `5/5`, and Ruff; result SHA-256 is `71dca98e1f27370733e8ba59ae1284dc8aea362d858a4f7290966e4645a6c007`. v67 is frozen as a valid NO_GO, v64 remains the leader, and v68 tests a distinct causal missingness-run geometry with explicit transport-shift warning.

P1 v45 evaluated a fixed causal cross-environment gradient-sign agreement mask under a new target-free station×quarter support contract, not a relaxation of v44. Five environments passed the sealed occupancy rule with at least 4,536 rows and two layers. Three fits completed in `8.781 s`; the strictest candidate achieved `46/57` TP, precision `0.807018`, and Wilson-90 LCB `0.707953`, but support collapsed to only S-ORS/L5 half 2, while the broader candidate again had zero G-ORS/L1 TP in both halves. It therefore stopped at the pre-Q2 gate with no chosen threshold, Q2-Q4 target reads, actions, removals, F1/points claim, official access, CSV, or upload. Root QA passed `20/20`, focused pytest `6/6`, and Ruff; result SHA-256 is `82bbb46ad622e1cecaa8280b9130453fc203094ae625cca8d02b924e13f09107`. The AND-mask family is frozen and v46 moves to a distinct causal last-layer spectral-decoupling audit.

P2 v36 applied fixed Fishr-style diagonal per-sample final-head gradient-variance matching across layer×month environments. Nine fits improved pooled RMSE by `0.050436335 C`, nominal `+0.632851` and fixed-penalty `+0.511169` points, with all aggregate folds, months, and layers plus CI90 `[-0.081020868,-0.036992577] C` favorable. The prospective gate nevertheless failed at `6/9` non-harm cells with maximum harm `+0.024712991 C`; Jul-Aug L2/L3 and Nov-Dec L4 regressed. Root QA passed `37/37`, focused pytest `8/8`, and Ruff; result SHA-256 is `cabb73eb3b15051a4cd0d3d00c26ab53bc0301d6cbca1234d82460487d39954f`. v36 is `EXPLORATORY_NO_GO_LAYER_MONTH_FISHR`, frozen without coefficient retuning, and v37 tests distinct layer-conditioned calendar-month Central Moment Discrepancy.

P3 v68 used only causal raw-observation-mask run geometry. Ridge-512/add-0.10 improved historical RMSE by `0.002202562 m`, nominal `+0.034956` points and `5/6` blocks; both episode and block-station CI90 intervals were favorable and all fixed harm guards passed. Its fixed transport-adjusted gain remained `-0.014630`, and RMSE `0.778988963 m` remained materially behind v64's `0.775927657 m`. Root QA passed `20/20`, focused pytest `5/5`, and Ruff; result SHA-256 is `50bc34026f4e8dbc9aa63ef53abd1d7a0fc5ba637c195ae0bf87b4825f99326e`. v68 is frozen as a stable candidate but not promoted; v64 remains the immutable leader and v69 moves to a distinct fixed SAX local-word histogram axis.

P1 v46 applied one fixed causal spectral-decoupling penalty to the last-layer logits. Three fits completed in `5.656 s` with the penalty active. The strictest candidate reached `56/57` TP, precision `0.982456`, and Wilson-90 LCB `0.925111`, but all supported actions still lay in S-ORS/L5 half 2; the broadest candidate retained zero TP in G-ORS/L1. It therefore stopped at the pre-Q2 gate with no Q2-Q4 target reads, actions, removals, F1/points claim, official access, CSV, or upload. Root QA passed `20/20`, focused pytest `6/6`, and Ruff; result SHA-256 is `f3612222da3afa66db9d5cd0aed8c5f8f1eb8cbaf713b7bbce6960598f776822`. Spectral decoupling is frozen and v47 moves to a semantically audited hidden-feature MixStyle axis.

P2 v37 used fixed layer-conditioned calendar-month Central Moment Discrepancy through order five. Nine fits improved pooled RMSE by `0.050382165 C`, nominal `+0.632172` and fixed-penalty `+0.510490` points, with favorable pooled CI90 `[-0.080819083,-0.037110226] C`. It improved `8/9` prospective cells, but Nov-Dec L4 worsened by `+0.035542044 C`, causing the unchanged harm ceiling to fail; the Nov-Dec fold and November month also regressed in aggregate. Root QA passed `37/37`, focused pytest `8/8`, and Ruff; result SHA-256 is `853a20f30c7044826a51bb6f7190a9770deb3c215ee4182ff3fbc3047d5af513`. v37 is `EXPLORATORY_NO_GO_LAYER_CONDITIONED_MONTH_CMD`, frozen without moment/coefficient retuning, and v38 tests a single fixed output spectral-decoupling penalty.

P3 v69 encoded fixed hard-SAX length-three local-word histograms. Ridge-2048/add-0.10 improved historical RMSE by only `0.001546291 m`, nominal `+0.024541` points and `5/6` blocks. Episode CI90 was favorable, but block-station CI90 `[-0.003015753,+0.000173726] m` crossed zero and fixed transport-adjusted gain was `-0.025045`. Root QA passed `20/20`, focused pytest `5/5`, and Ruff; result SHA-256 is `aafccd310d1d5ecf9e1e7089d8789f6a53920001b64add0832499a94544dd05f`. v69 is frozen as a valid NO_GO, v64 remains the leader, and v70 moves to a distinct Burg/Levinson reflection-coefficient memory spectrum.

P1 v47 passed two byte-identical target-free MixStyle preflights with adequate support, but the exactly-once runner failed before any fit because it read the support threshold from the wrong configuration namespace. The immutable decision is `INVALID_TECHNICAL_CONFIG_NAMESPACE_WIRING_ZERO_FIT`; fits, optimizer steps, Q2-Q4 target reads, actions, removals, official/test/sample/hidden rows, CSVs, and uploads all remained zero. The failure receipt SHA-256 is `890df54dbf51e49c18391eb48514822f15574315541de4a7137e4f0f368d9f16`; focused pytest passed `6/6`, Ruff passed, and invalid-closure QA passed. v47 is never rerun. One fresh v47r1 recovery is authorized to change only the namespace lookup and add a regression test while preserving all science.

P2 v38 added one fixed squared-output spectral-decoupling penalty to the exact v13 regression objective. Nine fits improved pooled RMSE by `0.050566638 C`, nominal `+0.634486` and fixed-penalty `+0.512804` points, with all aggregate folds/months/layers and CI90 `[-0.081404055,-0.036991593] C` favorable. The prospective gate still failed at `6/9` cells with maximum harm `+0.027090475 C` across Jul-Aug L2/L3 and Nov-Dec L4. Root QA passed `36/36`, focused pytest `7/7`, and Ruff; result SHA-256 is `a09730cad05b1c3c68125c36aa95c3d3d569c75b87726b40d44a2bb9b3dcff99`. v38 is `EXPLORATORY_NO_GO_SPECTRAL_DECOUPLED_OUTPUT`, frozen without penalty retuning, and v39 tests a distinct target-layer gradient-sign unanimity mask.

P3 v70 encoded fixed Burg/Levinson reflection coefficients and innovation-variance decay. Ridge-512/add-0.10 improved historical RMSE by `0.003807139 m`, nominal `+0.060422` and fixed transport-adjusted `+0.010836` points, with all six leads and both CI90 intervals favorable. It improved `5/6` blocks and had a comparatively large maximum action of `0.147865034 m`; its RMSE `0.777384386 m` still trails v64's `0.775927657 m`. Root QA passed `20/20`, focused pytest `5/5`, and Ruff; result SHA-256 is `2b510592d908bf30730192abb4cc5cf599b552b97ec982f47e2d419a4713b347`. v70 is frozen as a stable but second-tier candidate, v64 remains the leader, and v71 moves to a distinct fixed cross-channel equilibrium-error/mean-reversion axis.

P1 v47r1 repaired only v47's configuration namespace under a fresh exactly-once identity. Two target-free preflights were byte-identical and three fits completed in `9.375 s`. Fixed MixStyle produced precision from `0.871886` to `0.964912`, but the broad candidate again had zero G-ORS/L1 half-0 TP and the strict candidates supported only one station/layer or one cell. It therefore stopped validly before Q2 with no downstream target reads, actions, removals, F1/points claim, official access, CSV, or upload. Root QA passed `23/23`, focused pytest `9/9`, and Ruff; result SHA-256 is `7e00bc9d4847c39760eac51d0607c464da74e272d9e1b19d8cfbc2b7afed5263`. The recovery closes the MixStyle family as a scientific NO_GO rather than a technical failure.

P2 v39 applied a fixed target-layer gradient-sign unanimity mask. Nine fits improved pooled RMSE by `0.048666307 C`, nominal `+0.610642` and fixed-penalty `+0.488960` points, with all aggregate folds/months/layers and CI90 `[-0.077814998,-0.035790267] C` favorable. The prospective gate failed at `7/9` cells because Jul-Aug L2/L3 regressed, with maximum harm `+0.027967604 C`. Root QA passed `37/37`, focused pytest `7/7`, and Ruff; result SHA-256 is `7e8678dd6d6f9e6b6d85c2a726e38f971bd880837b8fe61d1690df6d97b044ce`. v39 is `EXPLORATORY_NO_GO_TARGET_LAYER_GRADIENT_SIGN_UNANIMITY`, frozen without partial-agreement retuning, and v40 moves to a distinct fixed predictive-dropout consistency axis.

P3 v71 encoded fixed pairwise equilibrium residual and error-correction geometry. Ridge-512/add-0.10 improved historical RMSE by `0.003557058 m`, nominal `+0.056453` and fixed transport-adjusted `+0.006867` points, with dual favorable CI90 intervals and `5/6` blocks. Its RMSE `0.777634467 m` was weaker than both v64 and v70, though action p99/max stayed moderate at `0.046329225/0.073264767 m`. Root QA passed `20/20`, focused pytest `5/5`, and Ruff; result SHA-256 is `ca42317fb172c455b53469af7865ffe640b0bec060ef12492cc6c9a302149285`. v71 is frozen as stable-but-weaker, v64 remains the leader, and v72 moves to a distinct fixed 0-1 translation-diffusion diagnostic.

P1 v48 audited an identity-routed mixture-of-experts response to the persistent station-layer collapse. Prefix occupancy itself was adequate at 294,278 rows and at least 1,971 rows per station-layer cell, but direct identity experts exactly duplicated a registered station-routed LightGBM family. Expert-Choice MoE's primary-source guarantee concerns balanced expert capacity, not prospective TP/action coverage, so it could not supply the missing identifiable support claim. v48 therefore closed before READY as a valid semantic-support NO_GO with no runner, lock, preflight, fit, target read, action, removal, official access, CSV, or upload. Focused pytest passed `4/4`, Ruff passed, config SHA-256 is `34c20260626685cab96ecdaf6ed628bd0918dfca1fb7757e3e2e74950fef75c4`, and QA SHA-256 is `8d47b97eb5dd416a61b5a7416212ef7fc4f772289de170552b00387ceeb5f8ca`. The next audit must identify a causal scientific-state observable, not merely expert occupancy.

P2 v40 applied fixed dropout and two-pass predictive-consistency regularization. Nine fits improved pooled RMSE by `0.048630479 C`, nominal `+0.610192` and fixed-penalty `+0.488510` points, with all aggregate folds/months/layers and CI90 `[-0.077453896,-0.035982107] C` favorable. It reached `8/9` non-harm cells, but Nov-Dec L4 still worsened by `+0.019217156 C`, exceeding the unchanged `+0.003 C` ceiling. Root QA passed `38/38`, focused pytest `8/8`, and Ruff; result SHA-256 is `b60534e132c493e76bec94e4a17fcc17b7bc85f30b3f1d8f63be318f3b3cdda5`. v40 is `EXPLORATORY_NO_GO_PREDICTIVE_DROPOUT_CONSISTENCY`, frozen without dropout/coefficient retuning, and v41 moves to distinct all-Linear Weight Normalization.

P3 v72 encoded fixed Gottwald-Melbourne translation-diffusion diagnostics. Ridge-512/add-0.10 improved historical RMSE by `0.003924217 m`, nominal `+0.062280` and fixed transport-adjusted `+0.012694` points, with all leads and all stations plus both CI90 intervals favorable. It improved `5/6` blocks and remained below v64's `0.005263867 m` gain; action p99/max were `0.049029424/0.068813667 m`. Root QA passed `20/20`, focused pytest `5/5`, and Ruff; result SHA-256 is `d028e15f5f4b03120214297f43c37cdbc35a2e6c504505934889850888fa2177`. v72 is frozen as stable-but-weaker, and v64 remains the immutable internal leader while the next P3 axis undergoes a fresh semantic audit.

P1 v49 evaluated whether a causal Thorpe-overturn scientific-state observable could supply the missing cross-station opportunity signal. The prefix contained 53,319 finite profiles with at least three levels, but every such profile belonged to S-ORS; G-ORS had at most one level and I-ORS at most two. The same state was also reconstructible from existing stratification, vertical-rank, and EOS/TEOS families. v49 therefore closed before READY as a precise support-and-semantic NO_GO with no runner, lock, preflight, fit, target read, action, removal, official access, CSV, or upload. Focused pytest passed `5/5`, Ruff passed, config SHA-256 is `297eca4e1f6eb76c00135c4d88fe0dac15e44852d252faaae9b076315c765c1d`, and QA SHA-256 is `b7a2690e10805dc372dbf48ee0dc5f29c0632fb6ca1a7ca7a67b133c7be7933d`. The train-only schema lacks a multi-station vertical state observable, so the next bounded audit checks only already-available and competition-allowed shared external drivers.

P2 v41 reparameterized exactly five Linear layers with Weight Normalization while preserving the initial function. Nine fits improved pooled RMSE by `0.049634805 C`, nominal `+0.622794` and fixed-penalty `+0.501112` points, with all aggregate folds/months/layers and CI90 `[-0.079916402,-0.036212314] C` favorable. The prospective gate failed at `6/9` cells with maximum harm `+0.032314734 C` across Jul-Aug L2/L3 and Nov-Dec L4. Root QA passed `42/42`, focused pytest `6/6`, and Ruff; result SHA-256 is `4c6e3659c3c4635a14a22a1151667990fc92c2ad9d8bc5190bb67e8baad2752c`. v41 is `EXPLORATORY_NO_GO_WEIGHT_NORMALIZATION`, frozen without parametrization retuning, and v42 moves to a distinct observed-token virtual-adversarial consistency axis.

P3 v73 encoded fixed lag-two ordered pairwise predictive-causality variance ratios. Ridge-2048/add-0.10 improved historical RMSE by only `0.001137531 m`, nominal `+0.018053` points and `5/6` blocks. Episode CI90 `[-0.002409735,+0.000211375] m` and block-station CI90 `[-0.002566970,+0.000448081] m` both crossed zero, while fixed transport-adjusted gain was `-0.031533`. Root QA passed `20/20`, focused pytest `5/5`, and Ruff; result SHA-256 is `bbc18963e6c245635a394adaf2d780aad9b0b39c405f9def94d44a15751b28a2`. v73 is frozen as a valid NO_GO, v64 remains the leader, and another semantically distinct P3 axis is being audited.

P1 v50 completed a read-only inventory of already-local, competition-allowed shared causal drivers. Multi-station ERA5 and KMA end on `2023-12-31`, before the 2024-2025 P1 train interval; concurrent NASA POWER and ERA5 blocks are S-ORS-only; historical CTD is I-ORS-only and pre-2024; and tide phase is reconstructible from the already-closed timestamp family. It therefore closed as `EXTERNAL_DRIVER_UNAVAILABLE` with network/download/runner/preflight/lock/fit/target/action/removal/official/CSV/upload all zero. All seven manifest hashes matched, focused pytest passed `6/6`, and Ruff passed; config SHA-256 is `e224cf3873ff13f38ddd42a0942f402276d75fb8a4fe51360f6b57ede1847e80`, report SHA-256 is `c80cc4b23fb34fa2bda782d2530401b87e80d548fe75dfd95c60e859b6abb0a8`, and QA SHA-256 is `0e28e92339fb20bc65e6162076f6c9a153f3592f4ad80336861ebe8a0f7a01ca`. The next P1 audit must find a train-only mechanism with target-free support at two or more stations; it may not fabricate historical-to-current driver alignment.

P2 v42 applied a fixed one-power-iteration virtual-adversarial consistency penalty only to observed continuous tokens. Nine fits completed in `175.188 s` and improved pooled RMSE by `0.051239392 C`, with canonical nominal `+0.642928` and fixed transport-adjusted `+0.521246` points. All three folds, all six months, all aggregate layers, and CI90 `[-0.082247374,-0.037583590] C` were favorable, but the unchanged v26a prospective gate failed at `6/9` cells: Jul-Aug L2/L3 regressed by `+0.025270617/+0.006694684 C` and Nov-Dec L4 by `+0.015828729 C`. Root QA passed `37/37`, focused pytest `7/7`, and Ruff; result SHA-256 is `36f0a441cdaef2b9c129e1fbab129c786bd472940b99b7803d5132ed04a4833d`. v42 is frozen as `EXPLORATORY_NO_GO_MASKED_TOKEN_VIRTUAL_ADVERSARIAL`; v43 tests one distinct identity-initialized target-layer FiLM axis without changing the gate.

P3 v74 encoded fixed ordered lagged codifference through joint-versus-marginal empirical characteristic functions. Ridge-512/add-0.10 improved historical RMSE by `0.002261149 m`, nominal `+0.035886` points, `5/6` blocks, and favorable episode/block-station CI90 intervals `[-0.003823690,-0.000582191]` and `[-0.003969337,-0.000329756] m`. Its fixed transport-adjusted gain was still `-0.013700`, and its RMSE `0.778930375 m` remained weaker than v64. Root QA passed `20/20`, focused pytest `5/5`, and Ruff; result SHA-256 is `731e3e11ad5630acf673af1e1f31a12b356019fad46f8aecf3a0e441a25e52a5`. v74 is frozen as a stable but second-tier candidate; v64 remains the immutable internal leader while v75 audits one new semantically distinct axis.

P1 v51 audited fixed restricted-versus-remote-history predictive causality at the 9,842 exact three-station layer-1 timestamps available before Q2. Incremental relative-MSE gain was `-0.004503/+0.000788/+0.002482` for G/I/S, but positive daily blocks were only `23/61`, `29/61`, and `30/61`; all three median daily gains were negative, so zero stations passed the preregistered strict-majority stability gate. The same Granger-style restricted/unrestricted mechanism also overlapped the P3 v73 registry and prior P1 cross-station contamination warning. v51 therefore closed as `NO_GO_ZERO_FIT_CROSS_STATION_PREDICTIVE_INFORMATION_UNSTABLE_AND_REPOSITORY_OVERLAP` with runner/preflight/lock/fit/target/action/removal/official/CSV/upload all zero. Independent QA passed all `14/14` checks, focused pytest `6/6`, and Ruff; config SHA-256 is `6b90559c4deb6000386a8f0ce90d0716441fe35e5c66ed4aa60338a3187c9ed3`. With v50 and v51 closed, no honest train-only cross-station action mechanism is currently identifiable; v52 audits only whether the prospective guard has measurable false-negative evidence and cannot promote a candidate.

P2 v43 added one identity-initialized, bias-free target-layer FiLM conditioning map to exact v13. Nine fits completed in `70.457 s` and improved pooled RMSE by `0.050199388 C`, canonical nominal `+0.629878` and fixed transport-adjusted `+0.508196` points, with CI90 `[-0.080883738,-0.036766505] C`. The unchanged v26a gate again failed at `6/9` cells: Jul-Aug L2/L3 regressed by `+0.021747648/+0.007218905 C`, and Nov-Dec L4 by `+0.029460496 C`. Root QA passed `39/39`, focused pytest `7/7`, and Ruff; result SHA-256 is `2a825dd41e8df0481295566139b77f3a588e469cb344372877f1fcea5d5ab084`. v43 is frozen as `EXPLORATORY_NO_GO_TARGET_LAYER_FILM`; v44 evaluates one preregistered GradNorm task-balance axis without modifying the architecture, fold-layer gate, or official access policy.

P3 v75 extracted fixed rank-4 matrix-pencil complex-pole radius/angle and residual-rank energy geometry. The better Ridge-2048/add-0.10 candidate improved historical RMSE by `0.001932342 m`, nominal `+0.030668` points, and `5/6` blocks. Episode CI90 was favorable at `[-0.003527069,-0.000353017] m`, but block-station CI90 crossed zero at `[-0.003654335,+0.000062508] m`, G-ORS regressed, and fixed transport-adjusted gain was `-0.018918`. Root QA passed `20/20`, focused pytest `5/5`, and Ruff; result SHA-256 is `6352b1adefbd5f7211d878bfbfb31c1f64a80254be815dfc8e77a11c0297efdb`. v75 is frozen as a valid NO_GO, v64 remains the immutable leader, and v76 audits one new non-spectral, non-Prony, non-codifference axis.

P1 v52 audited one preregistered v33 bundle without refitting, score recomputation, threshold reselection, or candidate promotion. The pretarget maximum-Wilson-LCB rule fixed the v38 focal q=`0.9975` action at 77 additions; opening Q2 only found `4` TP and `73` FP, precision `0.051948052`, and F1 delta `-0.005085464`. This is a second observed guard true negative, but v35 and v52 share the same Q2 target surface and there is still no beneficial observed candidate, so sensitivity remains unidentified. Q3/Q4 and official/test/sample/submission/hidden remained unopened; CSV/upload were zero. Root QA passed all `16/16` checks, focused pytest `6/6`, and Ruff; result SHA-256 is `5fa54dc58dc6193d0469127ab8f560f29f6f31704890668fc8f5ef8ed36b231f`. v28 remains unchanged while v53 audits whether any genuinely unexposed historical transport window exists.

P2 v44 applied fixed GradNorm task weighting to exact v13. Nine fits completed in `178.416 s` and improved pooled RMSE by `0.049641308 C`, canonical nominal `+0.622876` and fixed transport-adjusted `+0.501194` points, with all three folds, all six months, and CI90 `[-0.079990788,-0.036198449] C` favorable. The unchanged v26a gate again failed at `6/9` cells, with maximum harm `+0.043823672 C` in Jul-Aug L2 plus regressions in Jul-Aug L3 and Nov-Dec L4. Root QA passed `38/38`, focused pytest `7/7`, and Ruff; result SHA-256 is `fe1b03997ad5f51b125c04eb0816727295e6bd9bb3869d3d0c5395c98e37eb5f`. v44 is frozen as `EXPLORATORY_NO_GO_TARGET_LAYER_GRADNORM`; v45 tests one preregistered all-Linear DropConnect axis without changing the gate.

P3 v76 tested a fixed moist-air momentum representation, but the real support gate stopped it before fitting. All 182 by 48 features were finite with positive variance, yet 54 historical cases had `caph` pressure identically zero across the prefix, yielding an impossible density minimum `0.001275 kg/m3` below the sealed `0.8` floor. The gate was not relaxed and no substitute pressure was introduced. Fits, outer score exposure, official/test/sample/submission/hidden, CSV, and upload were all zero. Root QA passed `13/13`, focused pytest `5/5`, and Ruff; result SHA-256 is `94c6ef5113e6a5892dd89f5dcc441749df3247fe25cc09b8d8a4ee1852bfe5f9`. v76 is frozen as `STOP_SUPPORT_GATE_ZERO_FIT`; v77 moves to fixed natural-time increment-energy geometry.

P1 v53 audited whether a genuinely unexposed labeled historical transport window remained. Every 2024 quarter had only one station and could not support the v28 multi-station contract; 2025Q1 had three stations but was already used for inner selection and pre-Q2 fit/calibration; 2025Q2-Q4 were globally hypothesis-exposed. The v33 registry contained 11 manifests and 10 distinct bundles, but those preserve pretarget actions rather than fresh truth. v53 therefore closed as `NO_FRESH_HISTORICAL_TRANSPORT_WINDOW` with candidate/runner/preflight/lock/fit/new Q2-Q4/official/CSV/upload all zero. Root QA passed `20/20`, focused pytest `7/7`, and Ruff; QA SHA-256 is `276d48e9f6a4edf0595217e7eea5515d4f7eb1531c65b9c7f0c3679cbe0160fa`. P1 now performs a metadata-only registry audit for any overlooked, reproducible, internally promotable, never-submitted candidate.

P2 v45 applied fixed DropConnect with keep probability `0.9` to the five Linear weights of exact v13. Nine fits completed in `82.322 s` and improved pooled RMSE by `0.049402103 C`, canonical nominal `+0.619874` and fixed transport-adjusted `+0.498192` points, with CI90 `[-0.078695532,-0.036696573] C`. All three folds, six months, three layers, and all `9/9` fold-layer cells improved; the worst cell was still favorable at `-0.023209999 C`. Root QA passed `38/38`, focused pytest `7/7`, and Ruff; result SHA-256 is `24f02df803b6da31df39d63c4cad8f6a2e9006c9c7ee0a36428877c7d1ac6441`. v45 is frozen as `EXPLORATORY_SAFETY_PASS_REQUIRES_FRESH_CONFIRMATION`; a new-ID, exact-frozen three-seed confirmation runs before any official consideration.

P3 v77 encoded fixed natural-time increment-energy geometry. Ridge-2048/add-0.10 improved historical RMSE by only `0.001145434 m`, nominal `+0.018179` points and `5/6` blocks, while fixed transport-adjusted gain was `-0.031407`. May-Jun worsened by `+0.007265598 m`, lead 3h/6h were harmful, and both episode and block-station CI90 intervals crossed zero. Root QA passed `20/20`, focused pytest `5/5`, and Ruff; result SHA-256 is `527129dfdbd90dd4d90a9e0c640acdc99f0d328c6a0325a987b90b9f374c328e`. v77 is frozen as `NO_GO_ALL_NATURAL_TIME_CANDIDATES`, v64 remains the immutable leader, and v78 audits fixed directional-change intrinsic-time geometry.

P1 v54 completed the requested metadata-only registry audit over `470` documents, `155` experiment IDs, `149` result documents, and nine normalized candidate lineages. Three lineages had historical promotion-pass evidence, but all three exact submission SHA-256 values were already present in the official ledger; no unsubmitted lineage satisfied the reproducibility, internal-promotion, never-submitted, and distinctness conjunction. The decision is `NO_OVERLOOKED_PROMOTABLE_P1_CANDIDATE` with candidate/materialization/runner/preflight/lock/fit/official/CSV/upload all zero. Root QA passed `19/19`, focused pytest `7/7`, and Ruff; corpus digest is `1f0343e12fbdfdb9a8a6e9a0f16b9bdfb9c9f2309d91eed17e4bff74032dda13` and QA SHA-256 is `43a353e82b380d13f9fc136175f9a69fb9ffb21aefd4792ba045fd7a4138a4ae`. P1 v55 now requires one support-qualified, semantically distinct train-only mechanism; it must close at zero fits if either condition fails.

P2 v45c independently repeated the exact frozen v45 family with three new seeds (`20260904/05/06`). All nine fits completed in `84.787 s`; pooled delta RMSE was `-0.050578349 C`, nominal `+0.634633` points, fixed transport-adjusted `+0.512951` points, and CI90 `[-0.080779902,-0.037431623] C`. All three folds, six months, three layers, and all `9/9` fold-layer cells improved, with the least favorable cell still at `-0.020268329 C`. Predictions were nearly identical to v45 (Pearson `0.999992365`), so this is a stochastic confirmation of the frozen family rather than a new candidate-selection event; the original v45 commitment remains the family representative and no seed cherry-pick or ensemble is allowed. Root QA passed `43/43`, focused pytest `7/7`, and Ruff; result SHA-256 is `ebb92dade69992eb4a0c6c102eea13fa88c276ee77833d5a6fe6f17a3668c0d3`. P2 v46 now evaluates a separate identity-initialized LayerNorm axis under the unchanged v26a gate.

P3 v78 encoded fixed directional-change intrinsic-time residual geometry and completed all `12` fits. Ridge-512/add-0.10 improved historical RMSE by `0.005260515 m`, nominal `+0.083488` points, fixed transport-adjusted `+0.033902` points, `5/6` blocks, all three stations, and `5/6` leads; both episode and block-station CI90 intervals were favorable. The candidate is stable, but its RMSE remains `0.000003352 m` worse than v64, corresponding to about `0.000053` fewer nominal points, so v64 remains the immutable leader and v78 is frozen as a near-tie second candidate. Root QA passed `20/20`, focused pytest `5/5`, and Ruff; result SHA-256 is `7629d9c4fe18be3de715390ff218923944c63214f06dee8c15217ff4598e4646`. P3 v79 first audits whether BDS embedding-independence is semantically distinct from the prior recurrence/RQA lineage and closes at zero fits if it is reconstructible from v29.

P1 v55 examined exactly one Hawkes self-exciting event-hazard frontier after finding no exact repository implementation. The true past anomaly-onset history needed by the Hawkes intensity is a deployment-time target and therefore unavailable; substituting anchor onsets would reduce the proposal to a memory-kernel variant of already executed since-anchor, signal-run-length, lagged-probability, semi-Markov, and recurrence-return-time features. Semantic and support gates both failed, so v55 closed as `NO_GO_ZERO_FIT_RESEARCH_FRONTIER_EVENT_HAZARD_NOT_IDENTIFIABLE` with executable preflight, fit, Q2-Q4 target, official, CSV, and upload all zero. Root QA passed `17/17`, focused pytest `7/7`, and Ruff; result SHA-256 is `9757f38083e7d6ad246fc800701951cca1ef8e27a511336b5d371edde532b220` and QA SHA-256 is `45f3ba083c2724aeecbe1cf6399c6eea7cb5c54fbe6055a9e8fc3837c06b3c9a`. P1 v56 now audits the next single support-qualified, non-reconstructible train-only observable.

P3 v79 proved that its fixed BDS embedding-independence statistic was not reconstructible from v29, because v29 retained scalar recurrence rate and RQA topology but no delay-embedded `C3` needed for `C3-C1^3`. The exactly-once 12-fit run nevertheless remained far behind v64: Ridge-512 RMSE `0.779400944 m`, delta `-0.001790581 m`, nominal expected score `24.232017`, fixed transport-adjusted gain `-0.021168`, and `5/6` blocks, despite both CI90 intervals being favorable. It is frozen as a stable scientific result but not a submission candidate. Root QA passed `20/20`, focused pytest `5/5`, and Ruff; result SHA-256 is `d95ed34318754a20bb39abeaa0a8f4eb32bab25a9767f7ececaf32594d1a0f80`. P3 v80 now begins another strict negative-fingerprint and support audit; v64 remains immutable.

P2 v46 applied one identity-initialized LayerNorm operation to the hidden activation of exact v13 and completed all nine fits in `82.186 s`. Pooled delta RMSE was `-0.046730641 C`, nominal `+0.586354` points, fixed transport-adjusted `+0.464672` points, and CI90 `[-0.074917163,-0.034504134] C`. Fold, month, and layer aggregates all improved, but the unchanged prospective v26a gate failed because Nov-Dec layer 4 regressed by `+0.006441084 C`, above the sealed `+0.003 C` ceiling; the result was also weaker than both v45 and v45c. v46 is frozen as `EXPLORATORY_NO_GO_HIDDEN_ACTIVATION_LAYERNORM` with no placement, epsilon, affine, seed, or gate retuning. Root QA passed `39/39`, focused pytest `7/7`, and Ruff; result SHA-256 is `c60f05ab2ad9082bd122af68e5fbc382359c606d94e74d6ef1badf5dfe037a96`. P2 v47 now starts a fresh negative-fingerprint and support audit while v45 remains the family representative.

P1 v56 audited causal ADC output-code occupancy, missing-code, and differential-nonlinearity diagnostics as exactly one research-frontier axis. The repository exposes only processed `temp`, not raw integer codes, bit depth, LSB, transition levels, transfer/calibration metadata, serialization, or stimulus contract, so the IEEE 1241 mechanism is not identifiable on this dataset. Decimal-bin substitutes would be reconstructible from existing plateau rules, soft-symbolic features, and v42 state occupancy. v56 therefore closed as `NO_GO_ZERO_FIT_ADC_CODE_DENSITY_SOURCE_CONTRACT_UNAVAILABLE` with executable preflight, lock, train/target/fit/action, official, CSV, and upload all zero. Root QA passed `17/17`, focused pytest `7/7`, and Ruff; result SHA-256 is `029c41047be960b092c3d1736a2636f37b21311557ad1cc56a515b282228563a`. P1 v57 now audits one next authenticated deployment-observable axis.

P1 v57 audited a redundant-thermistor parity residual as exactly one analytical-redundancy axis. The allowed schema contains one `temp` stream per row and no stable probe/channel ID, synchronized colocated redundant channel, calibration/uncertainty contract, or parity relation; layer ordinal is not guaranteed to be a stable sensor identity across years. Treating another layer as a redundant probe would reconstruct existing peer-residual, stratification-coherence, long-event other-layer, and physical-profile families. v57 therefore closed as `NO_GO_ZERO_FIT_REDUNDANT_SENSOR_CHANNEL_UNAVAILABLE` with executable preflight, lock, train/target/fit/action, official, CSV, and upload all zero. Root QA passed `18/18`, focused pytest `7/7`, and Ruff; result SHA-256 is `5226e1eaa86213e0c76f4904a9bafcbbb948bbaed48283a24acee558ac038986`. P1 v58 now audits one next authenticated deployment-observable axis.

P2 v47 added one identity-initialized pooled-context CrossNet layer to exact v13 after rejecting a Fourier-depth idea as semantically duplicate. The exactly-once nine-fit run reduced pooled RMSE by `0.050714628 C`, nominal `+0.636343` and fixed transport-adjusted `+0.514661` points, with CI90 `[-0.081731249,-0.037088284] C`. However, the unchanged v26a fold-layer gate passed only `6/9` cells: Jul-Aug layers 2/3 and Nov-Dec layer 4 regressed, with maximum harm `+0.034562759 C`. Despite a slightly better pooled result than v45/v45c, v47 is frozen as `EXPLORATORY_NO_GO_POOLED_CONTEXT_CROSS_NETWORK`; pooled selection cannot override the preregistered safety gate. Root QA passed `40/40`, focused pytest `7/7`, and Ruff/pycompile; result SHA-256 is `8b30b2b03302e6b2a58e9b7e0a83ad2fb58f4bfbc8062365553016308865daef`. P2 v48 now starts a fresh negative-fingerprint and support audit, while v45/v45c remains the stable family.

P3 v80 encoded fixed historical gust-factor and gust-excess intermittency features, a distinct axis from current-scalar gust excess and generic raw-channel convolutions. Ridge-2048 improved historical RMSE by `0.001662183 m`, nominal expected score `24.229979`, fixed transport-adjusted gain `-0.023206`, and `5/6` blocks. Episode CI90 was favorable, but block-station CI90 crossed zero at an upper bound of `+0.000183262 m`; the result was also far weaker than v64. v80 is frozen as `NO_GO_ALL_GUST_FACTOR_CANDIDATES` with no retuning. Root QA passed `20/20`, focused pytest `5/5`, and Ruff; result SHA-256 is `581be58de43d3712c5395f4e71c38f43e1a5110a5d9b8f0a57d0a1321edf3013`. P3 v81 now starts a fresh semantic and support audit while v64 remains immutable.

## Scope and assumptions

- Deadline: 2026-09-01 23:00 KST.
- Audience: the competition team making limited-upload decisions.
- Primary decision: which new local candidate, if any, deserves each scarce official probe.
- Exclusions: hidden labels, exact-source answer reconstruction, official-test feedback as row labels, source-data redistribution, unbounded result-driven retuning.
- Validation: chronological or blocked historical evaluation with the problem-specific purge and group contracts.
- Submission: action-time user confirmation is still required for the exact file after schema and hash QA.

## Research direction

### P1

The historical audit is closed; Hawkes event history, ADC code-density, and redundant-thermistor parity all failed deployment-observability or source-contract gates at zero fits. The active v58 cycle may inspect only one authenticated, support-qualified, non-reconstructible train-only observable and must stop before executable preflight if any gate fails. Historical gate relaxation, exposed-window retuning, and reinterpretation of already submitted SHA values remain prohibited.

### P2

The DropConnect family passed both the original and frozen-new-seed confirmation with all `9/9` cells. LayerNorm v46 and pooled CrossNet v47 improved aggregate metrics but failed the unchanged cell gate, so both are closed without retuning or pooled-score override. The active v48 cycle again starts from semantic distinctness and block support; v45 remains the family representative, while v23 remains the sole positive official transport anchor.

### P3

v78 remains the near-tie second candidate. v79 BDS and v80 gust-factor intermittency were distinct but materially weaker than v64, and v80 also failed block-station uncertainty. The active v81 cycle again starts with semantic and data-support gates before any fit. No v64/v78 coefficient tuning, blending, or Public-feedback routing is allowed.

## Current limitations

- Public subsets are small and are not exchangeable with exposed historical blocks.
- P1 aggregate receipt geometry is not consistent with a simple pooled micro-F1 interpretation of the recorded 80 changed rows, so row-level utility cannot be inferred from Public deltas.
- P2 and P3 have observed local-to-Public sign reversals; point translations are conditional planning conversions, not guarantees.
- This report remains provisional until all three active local experiments reach terminal QA.
