# P1 28.909341점 원형: 원시 데이터부터 전체 재학습

현재 결론: **빈 모델 폴더에서 7개 모델을 재학습한 뒤 생성한 전체 답안이 과거 28.909341점 파일과 SHA까지 일치했다. 09-07 공식 제출 시도에서도 서버가 기존 채점 답안과 동일하다고 판정했으며 제출 횟수를 차감하지 않았다.** 전체 실행은 2026-09-06 22:56 KST에 시작해 6,323.356초(1시간 45분 23초)에 완료했다.

## 09-07 독립 QA와 공식 서버 확인

- [독립 preupload QA](preupload-qa-20260907.json): 70/70 PASS. 소스/학습 산출물 hash, 7 fit, 6시간 이내 runtime, tree/MS 별도 PID 재생 영수증, 169,011행, 키 순서·중복·결측·binary/finite 및 답안 SHA를 재검사했다. 새로운 학습은 하지 않았다.
- 답안 양성 6,396행, SHA `57844ef235f987059d17bddb035cc0a98b92bb0d53263cf878c68b31bbc53687`.
- P1 문제 화면에서 3/3 기회를 확인하고 이 **새 재학습 폴더의 CSV**를 선택해 `제출하고 채점`을 한 번 클릭했다.
- 서버는 `직전 제출과 내용이 같습니다`, `제출 횟수는 차감하지 않았습니다`, `이전 채점 결과 #388`을 표시했다. 새로운 채점 레코드/새 점수를 얻은 것이 아니다. 중복 우회를 위한 파일 변형이나 추가 클릭은 하지 않았다.
- [공식 제출관리](https://oceanaidata.org/app/submissions)에서 과거 P1 F1 **0.833548**, 점수 **28.909341**을 다시 확인했다. 목록 자체에는 수치 ID가 표시되지 않으므로 #388의 특정 시각을 추정하지 않는다. 과거 exact-SHA 증거와 서버 동일 내용 판정은 별도로 기록한다.
- 오늘 제출 횟수 소모 0, 남은 3/3. 최종 모델 잠금·commit·push 0. [공식 시도 영수증](official-attempt-20260907.json).

이는 **이번 환경에서 원시 배포 데이터→새 모델→과거와 동일한 답안**을 만든 증거이며, 서버의 내용 동일 판정도 확보했다. 다른 GPU에서의 결정론이나 운영진 최종 적격성까지 자동 증명하는 것은 아니다.

## 첫 실측 결과: 트리 코어 재학습 복원

배포 train 776,706행, 원형 80개 특징으로 4 fit를 117.253초에 완료했다. 과거 모델/캐시/답안을 학습 입력으로 읽지 않았다. 별도 프로세스의 O/B 모델 hash 및 TRAIN-only probe 재생 6개 검사 모두 PASS다.

| 모델 | 재학습 파일 SHA256 | 원본과 비교 |
|---|---|---|
| O XGBoost 1개 | `d4b60c543691f51526b3909101763309a6560cc274386a8fb3323773ad469d12` | 정확히 일치 |
| B LightGBM 3개 묶음 | `5f7933a6fa7e2c03a84ce255538a74fa4e697052c546e87af2d8c8a3982f4895` | 정확히 일치 |

새 모델 파일을 별도로 해시 계산해 training receipt와 대조했다. 원본 SHA 근거는 기존 저장 모델 감사에 있다. 이 결과는 단순 답안 재생보다 강한 **트리 코어의 source-to-model 복원** 증거다. 이후 MS-TCN 3개 학습과 별도 PID 재생도 완료됐으며 최종 답안 전체 SHA가 일치했다. MS 학습 단계 시작은 22:58:18 KST, child PID 34368이었다.

## 이미 확인한 것

- 기존 모델 7개를 실제 추론하면 과거 답안 전체 SHA256 `57844ef235f987059d17bddb035cc0a98b92bb0d53263cf878c68b31bbc53687`와 일치한다. 과거 답안 복사나 특정 행 패치가 아닌 일반 조합 규칙으로 복원했다. 근거: [저장 모델 전체 재생 감사](../p1_historical_path_audit_20260906_v1/report-source.md).
- 이번에는 원시 배포 데이터에서 80개 특징을 다시 계산했다. train 776,706행과 test 169,011행 모두 과거 캐시와 값 및 parquet SHA까지 일치했다. 캐시는 비교 증거로만 사용했고 새로운 전체 학습의 입력은 아니다.
- 새 독립 소스 패키지는 모델·데이터·캐시를 복사하지 않고 생성했다. focused pytest 14 PASS / Ruff PASS. Jupyter 입구는 문법·스키마 검사만 완료했으며 실제 전체 실행은 동일한 CLI 진입점으로 완료했다. notebook 자체 실행 완료로 표기하지 않는다.

## 완료한 실행 순서

패키지: `C:/Users/cedis/Documents/OceanFinalCandidates_20260906/P1_original_source_v1`.

1. 원형 O XGBoost 1개와 B LightGBM 3개 재학습.
2. 별도 PID에서 새 트리 모델 numerical replay 검사.
3. 원형 MS-TCN 3-seed e150 재학습 및 별도 PID 재생 검사.
4. 새 모델만 사용해 추론하고 원형 router / MS 합집합 / 일반 GI spike 규칙으로 답안 생성.
5. schema/key/order 검사와 과거 답안 SHA 대조. 다르면 과거 공식 점수를 승계하지 않는다.

실행 영수증: [execution-start.json](execution-start.json). 고정 사전 계획: [plan.md](plan.md).
현재 상태는 패키지 `04_logs/execute.stdout.log`, `03_model/mstcn/progress.json`, 최종 `terminal.json`으로 확인한다.

## 남은 검증과 한계

- 총 7 fit, 전체 6,323.356초, 과거 답안 SHA 일치를 검증했다. GPU MS 학습은 deterministic_algorithms=false인 원형 설정을 유지했으므로 모든 하드웨어/향후 모든 실행의 동일성을 보장하지 않는다.
- 역사적 router 셀 정책은 기존 train OOF 선택 증거가 있는 고정 설정이다. 원래 셀 선택 프로그램 전체가 복원된 것은 아니며 이번 실행이 그것을 재적합하지는 않는다.
- 원형의 offline 관측 특징 계산 규칙을 유지했다. 새 train-fit-depth 변형이나 별도 HPO로 대체하지 않았다.
- 새로운 독립 OOF 개선, 다른 GPU에서의 학습 결정론, 인터넷 차단 환경 설치와 운영진 최종 적격성은 이 실행으로 입증됐다고 주장하지 않는다.
- 원형 모델·답안·실패한 다른 실행은 보존했다. 09-07 사용자 승인으로 답안 제출을 한 번 시도했고 서버 중복 판정을 받았다. 커밋·푸시·최종 잠금은 하지 않았다.
