# P2 v43 target-layer FiLM conditioned DeepSets

## 결론

상태: `EXPLORATORY_NO_GO_TARGET_LAYER_FILM`. pooled delta RMSE `-0.050199388 C`, canonical nominal `+0.629878` points, transport `+0.508196` points.

prospective fold x layer gate: `False`, non-harm `6/9`, max cell `+0.029460496 C`.

Exact v13 element embedding에 target-layer one-hot으로 만든 identity-initialized FiLM affine map 한 개만 추가했다. Perez et al.은 conditioning 동기만 제공하며 P2 성능 근거가 아니다. sweep/router/ensemble/row deletion/Public selection/official/hidden/CSV/upload=0.
