# P1 v21 temperature–salinity–depth physical consistency — zero-fit closure

Decision: `NO_GO_SEMANTIC_DUPLICATE`. No source file, target, fit, lock, or artifact was read or created.

P1 already uses `psal` and `depth` as raw, missingness, difference, and rolling features. More importantly, `ts_matched_filter.py` implements the same task-specific physical premise: synthetic temperature offsets/drifts are discounted when salinity has corroborating entry/exit-boundary evidence. v7 additionally executed causal temperature–salinity signed path area. A density/EOS transform would therefore be another deterministic transform of the same observed variables feeding the same supervised add-only row action, rather than a new representation/action family. The P2 TEOS/profile implementation confirms that this physical mechanism family already exists repository-wide, although its restoration objective differs.

Exact evidence hashes are sealed in the config. This ID is immutable and must not be executed or retuned. Fits, target reads, source-data reads, official/test/sample/submission/hidden reads, CSV creation, and uploads were all zero.

The next safe step is a registry-level audit for a genuinely new information source or intervention layer, rather than another deterministic transform of `temp/psal/depth`. Candidate examples requiring audit are sensor-network metadata or externally licensed causal covariates; neither is authorized by this closure.
