# Optional reader for official OceanSITES NetCDF4/HDF5 archives.
# Install in an isolated target so the frozen competition environment is unchanged:
#   .\.venv-p1\Scripts\python.exe -m pip install --no-deps --target external_data\quarantine\_deps -r requirements-external.txt
h5py==3.14.0
