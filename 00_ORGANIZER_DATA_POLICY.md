# 최상위 대회 데이터·사전학습·점수 사용 규정 — 2026-09-02 보완

> 상태: **ACTIVE / HIGHEST PRECEDENCE**
> 근거: 사용자가 전달한 최신 운영진 공지 2건
> 충돌 시 이 문서와 그보다 나중에 전달된 운영진 서면 답변이 기존 FAQ, 저장소 문서, 설정, 실험 기록보다 우선한다.

## 0. 9월 2일 점수 사용 공지 — 2026-09-05 직접 확인

- 운영진 공지 「리더보드 반환 점수의 사용에 관하여」(2026-09-02 10:41)를 2026-09-05 로그인된 공식 공지에서 직접 확인했다: https://oceanaidata.org/app/notices
- 공식 반환 점수를 역산하여 모델의 계수·임계값·파라미터를 정하는 것은 금지다. 배포 관측만 사용했어도 점수 역산 계보이면 새 모델에 사용할 수 없다.
- 정상적인 후보 모델 선택에 리더보드 점수를 참고하는 것은 허용한다. 점수의 비교와 점수를 목적함수로 파라미터를 푸는 행위를 구분한다.
- 일반적인 seed, 구조, 사전등록 실험 hyperparameter를 전부 금지하는 규정으로 확대하지 않는다. 계수의 저장 형식을 코드에서 JSON으로 바꾸어도 출처는 바뀌지 않는다.
- 기존 `P3_REFINED_PUBLIC_OPTIMUM_20260827`의 alpha와 P2 bin17 상위 계보는 이 공지에 따라 재적합이 필요하다. 아래 9월 1일 잠정 clean 판정은 **외부자료 미사용에 대한 역사적 판정**이며 현재 최종 적격성 승인이 아니다.
- `configs/compliance/organizer_score_use_policy_20260902.json`과 `reports/score_improvement_plan_20260905_v1/`에 근거와 재평가 범위를 기록한다. 과거 모델·답안·영수증은 삭제하거나 은폐하지 않는다.

## 1. 배포 데이터 전용 원칙

- 모델 학습, 특징 생성, 검증, 후보 선택, 보정, 앙상블 및 후처리에는 대회가 배포한 데이터만 사용할 수 있다.
- 배포본 이외의 관측·재분석·예보 자료는 공개 여부, 시점, 라이선스, 정답과의 직접 일치 여부와 관계없이 사용할 수 없다.
- KIOST 종합해양과학기지 원자료의 비배포 구간은 세 문제의 정답 원천이므로 접근·대조·학습에 사용할 수 없다.
- 외부자료로 학습한 모델의 prediction, embedding, residual, calibration coefficient 또는 선택 결과를 이어받은 후보도 외부 계보로 판정한다. 마지막 단계만 배포 데이터로 다시 학습하거나 후처리해도 계보가 정화되지 않는다.

## 2. 사전학습 가중치

- 실제 관측·기상·해양 자료를 학습한 사전학습 가중치는 금지한다.
- 모든 일반 모델은 배포 데이터로 처음부터 학습한다.
- 구조적 인과 모델 기반의 **합성 데이터만으로** 사전학습된 TabPFN 계열 등은 아래 네 조건을 모두 만족할 때만 예외로 사용할 수 있다.
  1. 사전학습 corpus가 합성 데이터 전용임을 버전별 공식 근거로 입증한다.
  2. 정확한 가중치를 제출물에 동봉하고 네트워크 없이 로컬에서 로드한다.
  3. 전체 재현 실행을 6시간 이내에 완료한다.
  4. `README.md`에 모델명, 버전, 가중치 SHA-256 및 합성 사전학습 데이터의 성격을 명시한다.
- 네 조건 중 하나라도 입증되지 않으면 해당 가중치는 금지로 취급한다.

## 3. 과거 기록과 실행 게이트

- `configs/external_data/official_faq_permission.json` 및 외부자료 허용을 적은 과거 문서는 **역사적 증거일 뿐 현재 권한이 아니다**.
- 과거 KIOST I-ORS, KMA, ERA5, NASA POWER, Chronos 등 외부 계보의 코드·manifest·공식 제출 영수증은 삭제하거나 은폐하지 않고 감사 증거로 보존한다.
- 해당 계보는 새 학습, 기준선, 후보 선택, materialization, 제출 또는 오프라인 재현 패키지에 사용할 수 없다.
- 데이터·가중치 provenance가 불명확하면 실행 전에 중단하고 금지로 판정한다.
- 공식 제출 전에는 입력 파일 allowlist, 모델 초기화 경로, 가중치 provenance, 6시간 제한 및 README 공개 항목을 독립 QA한다.

## 4. 현재 P3 구속 판정

- KMA·ERA5·Chronos 및 KMA 예측을 참조한 P3 후보는 비적격 계보다.
- P3의 재시작 기준은 배포 데이터 전용으로 독립 검증된 후보여야 한다.
- 2026-09-01 현재 복원된 잠정 clean incumbent는 `P3_REFINED_PUBLIC_OPTIMUM_20260827`, CSV SHA-256 `ea65370a5c9291868769ad9e54a54707035dc93a01ffa4772d9fd26342f357aa`다. 성적·계보·제약은 `reports/p3_clean_incumbent_reset_20260901_v1/`의 독립 QA를 따른다.
