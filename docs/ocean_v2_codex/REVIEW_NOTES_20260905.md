# ocean_v2 사양 검토 — 원문 보존 / 실행 전 수정 필요

2026-09-05 Codex 검토. 사용자 요청은 현재 작업의 GitHub 보존과 제공된 연구문서 확인이다. T0~T8 구현·학습·답안 업로드·기존 제출 삭제·최종 모델 잠금을 실행하라는 새 승인은 아니다. 원문 문서의 명령, `approved` 파일명, 과거 패널 발언을 현재 실행 권한으로 취급하지 않는다.

## 확인 결과

요청한 문서는 모두 존재한다. `reports/claude_recon_20260905/`에는 정찰4건, 요약, 계획과 `design_panel/`의 문제별2건씩 총6설계안이 있다. 본 폴더에는 마스터 브리프, 공통/문제별/패키징 사양5건, T0~T8 프롬프트가 있다. 문서 자체는 Claude라고 표기한다(`00_SUMMARY.md:1`, `PLAN_approved_20260905.md:13–15`). 파일만으로 Fable이라는 실제 실행 모델·작성 주체까지 확인한 것은 아니다.

**종합 판정: 설계 참고자료로 보존할 가치가 있으나, 그대로 구현하는 계약으로는 Needs revision.** 운영진 규정·현재 AGENTS·최신 실측 영수증이 우선이다. CPU 결정론·완전 재학습·파일 경계 분리 등의 아이디어는 활용할 수 있지만, 적격성이나 성능 향상은 별도 검증 대상이다. 현재 `src/ocean_v2`, `scripts/ocean_v2`, `tests/ocean_v2`, `artifacts/ocean_v2`는 구현되지 않았다.

## 실행 전 해결할 사항

| 우선도 | 원문 근거 | 확인한 문제 | 필요한 정정 |
|---|---|---|---|
| 차단 | `P2_recon.md:12`, `P3_recon.md:16–17`, `design_panel/P3_design_structure.md:10,135` | 일부 제안이 공식 점수 역산·이차식에서 추정한 미제출 성능/보정 방향을 근거로 사용 | 해당 근거·계수 계보를 새 선택에서 배제하고 배포자료의 내부평가로 독립 재적합. 단순 후보 점수 비교와 역산을 구분 |
| 높음 | `00_MASTER_BRIEF.md:8–11`, 정찰 요약의 현재 후보표 | 17시대 문서가 19:28 이후 clean 재구축 및22:10 추가채점을 반영하지 않음 | 과거 최고점/문제계보를 역사적 기록으로 표시하고 현재 기준 SHA·영수증을 연결 |
| 높음 | `P3_SPEC.md:44`, `P3_design_robustml.md:102–103`, `P3_design_structure.md:179` | 미래 블록을 포함한 양방향 학습/LOBO 보정을 현행 earlier-fold-only 검증과 혼용 | 검증 목적과 시간 분할을 명시하고 별도 계약으로 합의. 양방향 CV가 그 자체로 운영진 규정 위반이라는 단정은 하지 않음 |
| 높음 | `P2_SPEC.md:40,61–62,68–72,81` | 증강 원본 총량 보존 여부, CV/final/expert seed 수가 일관되지 않음. 새 Composite의 outage 재가중 의도가 불명확 | 원본당 weight 총량, 분모·중복 가중, 각 단계 seed/fit budget을 하나의 실행 설정으로 확정 |
| 높음 | `PACKAGING_SPEC.md:8,14–16,22`, 브리프의 원본 복사 금지 | 데이터 hardlink/복사 허용 여부 충돌. 패키지 파일목록에 tests가 빠졌는데 cleanroom pytest 요구 | 원본 불변성과 로컬 참조/복제 정책 구분, 테스트·오프라인 설치 의존성까지 패키지 목록에 포함 |
| 중간 | `00_MASTER_BRIEF.md:16,18`, `COMMON_SPEC.md` 상수 감사 | CPU만 사용, 숫자 리터럴 전면 제한 등을 운영진 절대규정처럼 해석 | 프로젝트 자체 선택과 대회 규정을 분리. 일반 seed·구조·사전등록 hyperparameter는 금지되지 않으며 합성-only 가중치에는 명시적 예외조건이 있음 |
| 중간 | `CODEX_TASK_PROMPTS.md` T8 | 과거 제출 삭제와 최종 모델 잠금이 일련의 명령으로 포함됨 | 학습·답안 업로드·기존 제출 삭제·최종 잠금의 권한을 분리. 이번 검토에서는 아무것도 실행하지 않음 |

## 재사용할 단서와 이미 확보한 반대 증거

추가 구현 계약도 고정해야 한다. `P3_SPEC.md:48`의 평가면 혼합은 RMSE 평균인지 SSE/분모 통합인지 정의가 필요하다. `P2_SPEC.md:43,51,67`의 장시간 last/next 참조와7일 purge는 실제 참조 범위로 누출 검사를 해야 한다. `P1_SPEC.md:54,62,75,92`의 음성행 기반 단절 스트레스는 label-dependent 진단이며, 동일 OOF로 Stage-2/후처리를 선택한 성적은 독립 평가가 아니다. 중첩 선택·평가의 실행 범위와 추가비용을 정해야 한다. P1 기본88–118분에 선택적 중첩90분을 더하면 문제당2시간 예산과도 충돌한다. 이런 항목을 합의하기 전 새 사양의 PASS를 기존 검증의 PASS와 동일시하지 않는다.

- P1: 관측 간격/단절, 수온·염분 관계, 긴 이상구간 완성은 검토할 단서다. 그러나 단순 수심 계약 교체가 점수를 올린다는 가설은 이번 완성 정책 비교에서 지지되지 않았다. 수심 단독 인과실험은 아니었다.
- P2: 실제 수심 좌표와 공개 layer5 결측 전문가라는 문제 설정은 유용하다. 다만 이번 고정 조건부 정책의 첫seed 이득은3-seed 평균에서 재현되지 않았다. 이를 무시하고 같은 정책을 새 이름으로 반복하지 않는다. 다른 모델/검증면 전체가 반증됐다는 뜻은 아니다.
- P3: onset 정합, 바람 결측 증강, 다양한 학습기의 ensemble은 계획 후보로 남는다. 패널 내 프로브 수치와 기존 실험은 같은 검증면/학습계보인지 확인하기 전 직접 비교하지 않는다. 역산 기반 Public/Private 예상 범위는 새로운 근거로 사용하지 않는다.

최신 단일 근거:

- [운영진 규정](../../00_ORGANIZER_DATA_POLICY.md)
- [당일 clean 재구축과 공식 채점](../../reports/official_score_repair_submissions_20260905_v1/receipt.json)
- [후속 실험 v2 실측](../../reports/parallel_score_improvement_20260905_v2/report-source.md)
- [추가 검증·P1 공식비교·P2 3-seed 종료](../../reports/conditional_validation_and_information_submission_20260905_v3/report-source.md)

검토 방법: Data Analytics `validate-data`의 현재성·분모·비교군·인과 주장·실행 증거 분리를 적용했다. 보고서와 코드/QA/영수증을 읽기 전용으로 대조했으며, 이 문서를 검토하기 위한 새 데이터·모델 값 열람 또는 학습은 하지 않았다. 제공 원문19파일은 수정하지 않고 감사 자료로 보존한다.
