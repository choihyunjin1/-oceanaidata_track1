# Fable 최종 선택 재검토 — P2 portability v2 (2026-09-07 16:05 KST)

대상: 로컬 정정본. `P2_PORTABILITY_REPAIR_AND_AUDIT_RESPONSE_20260907.md`, `FABLE_FINAL_SELECTION_RECHECK_PROMPT_20260907.md`, `reports/p2_smooth7_portability_repair_20260907_v2/`, 새 P2 최종 ZIP `C:\Users\cedis\Documents\OceanFinalSelected_20260907\P2_v2\P2_FINAL_REPRODUCTION_794268f1_v2.zip`, 외부 선택 문서와 P1/P3 외부 FORM/README. 읽기 전용이며 새 학습·패키지 수정·업로드·삭제·최종 확인·commit/push 0, 실행 중 P3 불변.

증거 구분: **직접 검증** = 로컬 파일을 이 세션에서 해시·내용·코드로 대조했거나 직접 실행한 것. **receipt 의존** = Codex의 실행 기록에만 근거한 것(ZIP 추출 노트북 실행, cold 학습, 별도 PID replay). 포털은 이번 재검토에서 다시 열지 않았다. 마지막 포털 읽기는 15:30 전후 Fable 관찰(P2 미접수로 보임)이며 현재 서버 상태를 단정하지 않는다.

## 결론 먼저

1. **이전 감사의 치명 결함(P2 역사적 해시 게이트)은 해결됐다.** v2 스크립트는 기반 답안 SHA를 `base_sha256`(실제)·`expected_base_sha256`(fee6118b)·`base_exact_reconstruction`(일치 여부)으로 **기록**하고, 대신 **현재 실행의 무결성 QA**(`04_logs/independent-qa.json`: status PASS, checks 전부 True, answer_sha256 == 현재 기반 파일 해시)를 요구한다. 그 QA(`run.py:119-147 final_qa`)의 checks에는 역사적 해시 비교가 없다. 단순 assert 삭제가 아니다. 이 판단은 코드 직접 열람 근거다.
2. **새 cold/saved 결과는 채점 답안 `794268f1`과 같다(receipt 의존)**. ZIP 안의 답안 파일 자체가 `794268f1`, 26,061행인 것은 직접 확인했다.
3. **소스·manifest·ZIP·FORM은 같은 버전이다(직접 검증)**. ZIP SHA `36b4b0e4…`/485,957 B, FINAL_MANIFEST 78항목 전부 멤버와 일치, 두 SMOOTH_SOURCE_MANIFEST 전부 일치, core PACKAGE_MANIFEST는 v1과 동일(core 파일 14개 해시 동일), v2 스크립트 두 사본 동일. FORM·FINAL_SELECTION_MANIFEST의 SHA·bytes와 일치.
4. **옛 상위 문서·중간 답안 진입점 혼용은 해소됐다(직접 검증)**. START_HERE는 "이력용", RELEASE_MANIFEST status는 `HISTORICAL_RELEASE_NOT_CURRENT_FINAL_SELECTION`, P1 FORM은 옛 두 파일을 첨부하지 않도록 정정, FINAL_SELECTION_MANIFEST `do_not_attach`에 등재. P2 상위 README·CURRENT_README는 PROJECT.ipynb와 내부 README의 중간 답안 지시를 "이력용, 실행 금지"로 명시.
5. **한계 기재는 정확하다**: CUDA 필수·타 GPU/CPU/새 venv/OS 차단망 미검증, P3 completion_1 + saved replay만 증거이고 fresh cold 미완·저장 모델 재생은 학습 재현 대체가 아님, 6시간·마감·첨부 개수·재현 대상 답안 미확인이 FORM/README/manifest에 모두 있다.
6. **이전 감사 7번(GitHub 코드 누락)은 정정한다.** 커밋 6ecf287에 `scripts/p2_smooth7_portable_20260907.py`(v1 ZIP 멤버와 해시 동일), `p2_final_day_projection_20260907_v1.py`, `p2_final_day_materialize_20260907_v1.py`가 있고 P3 portable 스크립트도 추적된다. 내가 ZIP 내부 경로명으로만 grep해 잘못 판단했다. 다만 **최종 v2 ZIP에 실제로 들어간 `p2_smooth7_portable_20260907_v2.py`와 수리 테스트는 아직 미추적(untracked)** 이다. 저장소 URL로 보는 심사자는 v1 코드만 본다.
7. **제출 전 필수 수정은 없다.** 권장 사항 2건(v2 코드·테스트 커밋, v2 README의 v1 시각 잔존)과 재개 절차만 남는다. 이 재검토 PASS는 사용자 최종 제출 재개 승인을 대신하지 않는다.

## 재검사 표

형식: `[항목 | 재검사 근거·SHA | 해결/미해결 | 제출 전 필수 수정]`

| 항목 (이전 판단) | 재검사 근거·SHA | 해결/미해결 | 제출 전 필수 수정 |
|---|---|---|---|
| P2 역사적 해시 게이트 (이전: 치명, 검증 환경 차이 시 답안 미생성) | **직접**: v2 ZIP `SOURCE_ONLY/02_code/projection/p2_smooth7_portable_20260907_v2.py` = `SAVED_MODELS/...` 사본, 둘 다 SHA `2489c0db…`. `base_provenance()`: `actual = sha(source)`; `qa.status == "PASS"`, `all(checks)`, `qa.answer_sha256 == actual` 아니면 raise; 반환값에 `base_sha256`(실제), `expected_base_sha256`(BASE 상수, materialize 모듈에서 import), `base_exact_reconstruction`, `base_difference_policy`. `run.py:119-147 final_qa` checks = validate_output(schema/keys/order/finite/중복)·training_pass·training_qa_pass·full_csv_replay(같은 실행 두 PID 일치)·separate_pids·hash(같은 실행 내 일관)·LF·current_models_unchanged·source_unchanged·no_sample_values → **역사적 해시 비교 없음**. `stages.py` PREDICT는 이전과 같이 `exact_existing_candidate_sha_match`를 기록만 함 | **해결**. 잔존 위험: `full_csv_replay`는 같은 머신 두 프로세스의 비트 일치를 요구하므로 CUDA 추론이 실행 간 비결정이면 여전히 실패할 수 있음(Codex도 명시). CUDA 필수(`run.py:52-53`, training QA `device == "cuda"`) | 없음 |
| 옛 BASE 게이트 위치 (호출 그래프) | **직접**: `p2_final_day_materialize_20260907_v1.py:44-45`의 `raise ValueError("base answer is not exact approved L120 cold result")`는 `run()` 안에만 있고, 이를 호출하는 노트북은 `PROJECT.ipynb`(v1/v2 동일, 이력용)뿐. `SMOOTH_PROJECT.ipynb`·`SAVED_PREDICT.ipynb`는 v2 스크립트만 호출(셀 내용 열람). v2 스크립트는 materialize에서 `BASE, endpoints, sha`만 import(`run` 미호출) | **해결** | 없음 |
| 합성 hash-drift·QA 위반 거부 테스트 | **직접 실행**: `.venv-p1 python -m pytest -q -p no:cacheprovider tests/test_p2_smooth7_portability_repair_20260907.py tests/test_p2_smooth7_20260907.py` → **12 passed**(캐시 미기록). repair-qa.json의 12 PASS·Ruff PASS 기록과 일치(Ruff는 receipt 의존) | 해결 | 없음 |
| 새 cold/saved 최종 SHA = 채점 답안 | **receipt 의존**: `candidate-ready.json` v2 — EXTRACTED_COLD TRAIN 179.766 s(3 fit)·PREDICT 39.172 s·SMOOTH_PROJECT 14.672 s, cold 기반 답안 `fee6118b` exact, 후처리 infer PID 39128; EXTRACTED_SAVED 53.360 s, infer PID 41852·replay PID 42160, `base_exact_reconstruction: true`, 최종 `794268f1` 양쪽 동일. **직접**: ZIP 멤버 `ANSWER/submission_p2_L120_s3_smooth7_proj.csv` SHA `794268f1…`, 26,061행(+헤더), 1,253,654 B; 채점 receipt(14:45, 0.395254℃/28.373869)와 같은 SHA | 해결(같은 PC 기준). 타 GPU/CPU·새 venv·OS 차단망 미검증은 그대로 | 없음 |
| 소스·manifest·ZIP·FORM 동일 버전 | **직접**: ZIP 485,957 B, SHA `36b4b0e4a0de61277133a85fefd62a6629aa1ac1c109fc12652b1d1cb745408f`(FORM·p2-final-ready·FINAL_SELECTION_MANIFEST와 일치). FINAL_MANIFEST 78항목 = ZIP 파일 79개(매니페스트 자신 제외) 전부 해시 일치, 누락·초과 0. SOURCE_ONLY/SAVED_MODELS 각 SMOOTH_SOURCE_MANIFEST 전항목 일치. core PACKAGE_MANIFEST 두 사본 `3457f973…` = v1과 동일(status SEALED_SOURCE_ONLY_ZERO_FITS, 17항목 일치). v1↔v2 동일 파일: stages/run/base/boot/core/projection/materialize/requirements/`.pt` 3개/MODEL_MANIFEST. v1 스크립트(`p2_smooth7_portable_20260907.py`)는 v2 ZIP에 없음 | 해결 | 없음 |
| 옛 상위 문서 혼용 (이전: 필수 수정) | **직접**: `OceanFinalRelease_20260907/START_HERE.md` SHA `f160fe0c…`(outer-document-change-sha "after"와 일치) 첫 절 "이력용 묶음 — 현재 최종 선택 안내가 아님"; `RELEASE_MANIFEST.json` SHA `16eca326…`(일치) status `HISTORICAL_RELEASE_NOT_CURRENT_FINAL_SELECTION`; `P1/FORM.md` SHA `49c6c7fb…`(일치) 첨부 목록 = SOURCE_ONLY + 15 part + 재조립 manifest/tool + 이 FORM + `FINAL_SELECTION_20260907.md`/`FINAL_SELECTION_MANIFEST.json`, "옛 RELEASE_MANIFEST/START_HERE는 첨부하지 않습니다"; `FINAL_SELECTION_MANIFEST.json` `do_not_attach`에 두 파일 등재, status `USER_HOLD_PENDING_FABLE_RECHECK_NOT_FINAL_SUBMITTED` | 해결 | 없음 |
| P2 중간 답안 진입점 (이전: 문서 보완) | **직접**: v2 상위 README "내부 원형 README의 중간 답안 제출 지시와 PROJECT.ipynb는 이력용이며 실행하지 마세요"; `SOURCE_ONLY/CURRENT_README.md` "Historical PROJECT.ipynb and original README final-answer instructions are NOT execution entrypoints". 내부 core README(`3334e165…`)는 v1과 동일(핀 유지)이며 27행 "Submit only …_3seed.csv"가 남아 있음 — 상위 문서가 이를 무효화 | 해결(잔존 문구는 핀 호환을 위한 의도적 보존) | 없음 |
| P3 문구·진입점 (이전: 문서 필수) | **직접**: `P3_numeric_cpudet_noshrink_v2/FORM.md` SHA `3b9db543…`, `README.md` SHA `2c5bedea…`(모두 outer-document-change-sha와 일치). 내용: Public 우선·사용자 보류, 증거 = completion_1(29 재사용 + 12 신규) + saved/별도 PID replay, "Fresh cold is still running, not yet proof", "Saved replay is … NOT a replacement for required training reproducibility", "Old TRAIN/PREDICT/UNBOUNDED entrypoints are historical", 6시간·첨부 개수·마감 미확인. ZIP(`c3aed055…`, `f0451b9c…`)은 불변(직접 재해시 일치) | 해결 | 없음 |
| P3 fresh_cold_2 상태 | **직접**: `fresh_cold_2/progress.json` 16:02 기준 backbone **25/36**, stage CPU_TRAINING, pid 26996; `03_model/Q2_2025` 15:41 갱신(진행 중 증거). 프로세스 생존 자체는 Codex 15:46 확인에 의존. 남은 11 fit(전체 데이터 6 포함) → 학습 종료 대략 18:00~18:30, 이후 router·QA. 완료 답안 == `2015b387`, 그 모델의 no-shrink == `70761aff` 대조는 **미수행(진행 중)** | 미완(설계상 대기 항목, 문서에 정확히 기재됨) | 없음. 제출 전 완료되면 결과만 추가 |
| CUDA·타 GPU 미검증 등 한계 기재 | **직접**: P2 FORM/README/CURRENT_README/EVIDENCE/FINAL_AUDIT caveats, `repair-qa.json` `cross_gpu_tested:false, CPU_fallback_added:false, CUDA_required:true, fresh_venv_tested:false, OS_offline_tested:false`; FINAL_SELECTION_MANIFEST `official_deadline_time:null, general_six_hour_scope:UNKNOWN, attachment_count_limit:null`; P1 FORM 셀 선택 프로그램 미복구 문구 유지 | 해결 | 없음 |
| GitHub 코드 추적 (이전 감사 7번: "누락") | **직접**: `git ls-tree -r --name-only 6ecf287 -- scripts` → `p2_smooth7_portable_20260907.py`(작업 트리 해시 `54388c21…` = v1 ZIP 멤버), `p2_final_day_projection_20260907_v1.py`(`bb7a4548…` = ZIP 멤버), `p2_final_day_materialize_20260907_v1.py`(`90535080…` = ZIP 멤버), `p3_numeric_cpudet_noshrink_20260907_v1_portable.py`, `p3_cpudet_unbounded_20260907_v2.py`, builder들 추적됨 → **이전 판단 정정: v1 시점 누락 아님**. 그러나 `scripts/p2_smooth7_portable_20260907_v2.py`(`2489c0db…` = v2 ZIP 멤버), `tests/test_p2_smooth7_portability_repair_20260907.py`, `package_p2_smooth7_20260907_v2.py`, `build_selected_p2_final_20260907_v2.py`, `finish_p2_smooth7_20260907_v2.py`, `finalize_fable_recheck_20260907_v2.py`는 **untracked**(ignored 아님) | 부분 해결. v1 정정 완료, v2 미커밋 | 없음(ZIP 자체가 자족적). **권장**: 사용자 승인 후 v2 스크립트·테스트·리포트 커밋 |
| v2 상위 README의 실측 시각 | **직접**: v2 README 마지막 항목 "TRAIN142.218s / PREDICT34.375s / SMOOTH_PROJECT12.500s, 저장 모델48.047s"는 **v1** 노트북 시각. v2 실측은 179.766/39.172/14.672/53.360 s(candidate-ready v2, P2_v2/FORM.md에는 v2 값 기재) | 미해결(사소). 수치 주장·재현성에는 영향 없음 | 없음. ZIP을 다시 만들면 갱신, 아니면 FORM 설명란의 v2 값으로 충분 |
| P2 접수 상태 | 이번 재검토에서 포털 미열람. 15:30 전후 Fable 읽기: 문제 페이지에 "모델 최종 제출하기" 버튼 존재, 제출관리에 모델 항목 없음 → 당시 미접수로 보임. 현재 상태는 미확인 | 미확인(설계상) | 재개 시 클릭 전 같은 두 화면 재확인, v1 첨부가 남아 있으면 v2로 교체 후 OK |

## 직접 검증과 receipt 의존 요약

- 직접 검증: ZIP SHA/bytes/멤버/모든 manifest 대조, 답안 멤버 SHA·행수, v2 스크립트 전문과 `run.py`·`stages.py`·`base.py`·`boot.py` 관련 구간, 노트북 셀(SMOOTH_PROJECT/SAVED_PREDICT/PROJECT/TRAIN/PREDICT), v1↔v2 파일 해시 비교, 외부 문서 5개 SHA와 내용, git ls-tree/해시, P3 progress 파일, 수리 테스트 12개 실행.
- receipt 의존: 추출 cold(3 fit)·saved 노트북 실행과 그 시각·PID·SHA, Ruff, 합성 검사의 실제 실행 로그, P3 프로세스 생존(15:46).

## 남은 미확인(변동 없음)

모델 마감 시각, 첨부 개수 상한(파일당 50 MB는 UI 실측), 재현 대상 답안 선택 방식과 허용오차, 6시간 규칙의 일반 적용, 문제지 Ⅳ-2, 채점 완료 답안 삭제 가능 여부, P1 여섯 셀 선택 프로그램 미복구, P3 fresh_cold_2 완료·대조. 이 문서는 운영진 적격성 인증이 아니며, **사용자의 명시적 재개 지시 전에는 어떤 최종 확인도 하지 않는다.**
