# Fable 재검토 요청 — P2 portability v2 / 최종 확인 보류

저장소 `C:/Users/cedis/PycharmProjects/PythonProject`, 브랜치 `codex/p1-qc`. 기존 `FABLE_FINAL_SELECTION_AUDIT_20260907.md`는 원문 그대로 보존되어 있다. 먼저 `P2_PORTABILITY_REPAIR_AND_AUDIT_RESPONSE_20260907.md`와 `reports/p2_smooth7_portability_repair_20260907_v2/`의 결과·manifest를 전부 읽고 아래를 재검사한다. 문서나 receipt의 PASS 문구만 반복하지 말고 가능한 범위에서 소스·로컬 파일 해시를 직접 대조한다. 직접 실행하지 않은 검증은 receipt 의존이라고 표시한다.

1. **P2 실제 실행 경로와 변경 범위.** 새 최종 ZIP은 `C:/Users/cedis/Documents/OceanFinalSelected_20260907/P2_v2/P2_FINAL_REPRODUCTION_794268f1_v2.zip`. 정확한 bytes/SHA는 `reports/p2_smooth7_portability_repair_20260907_v2/p2-final-ready.json`을 읽는다. 양쪽 패키지의 `02_code/projection/p2_smooth7_portable_20260907_v2.py`에서 역사적 fee6118b 비교가 중단 조건에서 기록으로 바뀌었는지 확인한다. 동시에 `04_logs/independent-qa.json`의 PASS·모든 checks true·현재 입력 answer SHA 일치를 요구하고 소스/관측/모델 무결성·schema/key/order/finite·scalar PAVA·별도 PID replay가 유지되는지 확인한다. 단순히 모든 assert를 제거한 수정이 아니다. `base_sha256`은 실제값, expected는 별도 값이어야 한다.
2. **수치/재현 증거.** 새 cold TRAIN→PREDICT→SMOOTH_PROJECT와 새 saved 노트북의 최종 SHA가 채점본 `794268f15a0a7ac18ecd4dc99757083e159c49d639d2a8a72df3f835414cc481`인지 확인한다. 3-fit 학습 시간, 별도 PID, 합성 hash-drift 통과/현재 QA 위반 거부 테스트를 대조한다. 타 GPU 실측은 없고 CUDA 요구도 유지된다. 같은 PC exact를 범용 GPU/CPU 재현 보장으로 확대하지 않는다.
3. **manifest/불변 증거.** 새 SMOOTH_SOURCE_MANIFEST와 FINAL_MANIFEST의 모든 파일을 검사한다. 원 core PACKAGE_MANIFEST는 core 파일을 바꾸지 않아 그대로 유지한 것이 맞는지 확인한다. v1 ZIP·P1/P3 ZIP·채점 답안·가중치는 보존되어야 한다. builder의 기대 채점 SHA assert는 로컬 패키지 검증 단계이고 검증 PC의 예측 실행 게이트와 다름을 구분한다. 옛 projection-only main의 BASE gate는 이력용 PROJECT 경로에만 남았는지 실제 notebook 호출 그래프로 확인한다.
4. **문서 혼용 정정.** `C:/Users/cedis/Documents/OceanFinalSelected_20260907/FINAL_SELECTION_20260907.md` 및 `FINAL_SELECTION_MANIFEST.json`을 기준으로 P1 FORM이 옛 START_HERE/RELEASE_MANIFEST를 첨부하지 않도록 바뀌었는지, 옛 상위 문서가 이력용으로 표시됐는지 확인한다. P2 SOURCE_ONLY/SAVED_MODELS는 합치지 않고 P3는 source+model overlay인 차이를 유지한다. P2 옛 PROJECT.ipynb와 중간 답안 지시를 실행하면 안 된다는 안내도 확인한다.
5. **P3/P1 미해결 한계.** P3 fresh_cold_2의 실제 최신 PID/progress/terminal만 읽는다. completion_1 + saved replay를 fresh cold 완료로 잘못 쓰지 않는다. 저장 모델 재생은 필수 학습 재현을 대체하지 않는다. P1 여섯 셀 선택 프로그램 미복구, 일반 모델 6시간 범위, 모델 마감 시각, 첨부 개수, 재현 대상 답안/허용오차는 확인 증거 없으면 미확인으로 둔다.
6. **이전 감사 7번 정정.** `git ls-tree -r --name-only 6ecf2870b8d3a72cd829d58af71ba9f2d854851f -- scripts`로 `p2_smooth7_portable_20260907.py`, `p2_final_day_projection_20260907_v1.py`, `p2_final_day_materialize_20260907_v1.py`가 이미 있었음을 확인한다. P3도 `p3_numeric_cpudet_noshrink_20260907_v1_portable.py`와 `p3_cpudet_unbounded_20260907_v2.py` 등 생성 원본 이름으로 추적된다. ZIP 내 파일명이 다르다고 코드 누락으로 판정하지 말고 builder 복사/변환 경로를 확인한다.

산출물: `docs/ocean_v2_codex/FABLE_FINAL_SELECTION_RECHECK_20260907.md`에 결론 먼저, `[항목 | 변경 전 판단 | 재검사 근거·SHA | 해결/미해결 | 제출 전 필수 수정]`으로 작성한다. 해결된 것과 범용 재현/규정 미확인을 명확히 분리한다. 수정 권고는 최소 범위로 적는다.

권한: 읽기 전용 감사 + 위 감사 문서 작성만. 새 학습·모델/답안/패키지 수정·업로드·삭제·최종 확인·commit/push는 하지 않는다. 실행 중 P3는 절대 중단/재시작/변경하지 않는다. **감사 PASS도 최종 제출 재개 승인이 아니다.** 사용자의 명시적 재개 지시 전에는 모든 최종 확인을 보류한다. 포털을 읽었다면 시각과 관찰 주체를 적고 과거 확인창 상태를 현재 상태로 단정하지 않는다.
