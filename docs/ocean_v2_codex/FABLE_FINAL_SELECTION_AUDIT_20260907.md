# Fable 최종 선택 독립 감사 — 2026-09-07 15:40 KST

검토 기준: 브랜치 `codex/p1-qc`, 커밋 `6ecf2870b8d3a72cd829d58af71ba9f2d854851f`(로컬 HEAD 동일 확인). 검토 대상 P1 `57844ef2` / P2 `794268f1` / P3 `70761aff`. 이번 감사는 읽기 전용이다: 새 학습·답안 수정·업로드·삭제·최종 제출·commit/push 0, 실행 중 P3(PID 26996) 불변.

**검증 범위 구분.** 로컬 파일이 있는 항목은 직접 해시·내용을 확인했다("로컬 직접 확인"). GitHub에는 코드·설정·집계·문서만 있으므로, 모델 가중치·ZIP 내부 실행·cold/saved replay는 **Codex receipt에 의존**한다("receipt 의존"). 로컬에서 ZIP을 실행하거나 재학습하지는 않았다. 포털은 로그인된 브라우저에서 15:30 전후 **읽기만** 했다.

## 결론 먼저

1. **치명적 재현 결함 1건(P2, 제출 전 수정 권고).** 최종 P2 패키지의 마지막 단계 `02_code/projection/p2_smooth7_portable_20260907.py`는 cold·saved 두 경로 모두에서 `assert sha(source) == BASE`(BASE = 기존 L120 답안 `fee6118b…`)로 **기반 추론 결과가 비트 단위로 같지 않으면 중단**한다. 같은 패키지의 `run.py`는 CUDA가 없으면 실행을 거부한다(`same CUDA environment required; no silent CPU fallback`). 따라서 검증 PC의 GPU·CUDA·드라이버가 달라 L120 추론이 한 자리라도 달라지면 평활·투영이 실행되지 않아 **최종 답안이 생성되지 않는다**. 이는 09-02 공지 재현 검증 4항(학습 산출물 제거 후 예측 재생성)에서 "다른 값이 나온다"가 아니라 "출력이 없다"로 귀결된다. 최소 수정은 하드 assert를 "기록 후 계속"으로 바꾸고 manifest·ZIP·replay를 다시 만드는 것(코드 1곳, 재QA 약 5분)이다. 수정하지 않고 제출하려면 이 위험을 알고 결정해야 한다.
2. **버전 혼합 1건(문서, 제출 전 수정 필요).** 상위 `OceanFinalRelease_20260907/START_HERE.md`·`RELEASE_MANIFEST.json`은 P2 = `fee6118b`(28.077280), P3 = `ff42a6a0`(23.741446)로 적혀 있는데, 최종 선택은 P2 `794268f1`, P3 `70761aff`다. P1 `FORM.md`는 "FORM.md 및 상위 RELEASE_MANIFEST.json/START_HERE.md"를 함께 첨부하라고 안내한다. 그대로 첨부하면 운영진이 받는 문서가 최종 P2/P3와 모순된다. 첨부 목록에서 상위 두 파일을 빼거나, 최종 선택(P1/P2/P3 SHA·패키지 위치)을 적은 새 상위 문서로 교체해야 한다.
3. **P2 접수 상태: 포털 읽기 결과 미접수로 보인다.** 15:30 전후 `/app/problems/6`에 "모델 최종 제출하기" 버튼이 그대로 있고, 제출관리 목록에는 답안(채점) 항목만 있으며 모델 제출 항목은 없다. 확인창 OK 미클릭 진술과 일치한다. 다만 서버 측 대기 상태를 배제할 수는 없으므로, 재개 시 클릭 전에 같은 화면을 한 번 더 읽는다.
4. **점수·SHA 연결은 receipt와 화면이 일치한다.** P2 14:45 0.395254 / 28.373869 ↔ `794268f1`(로컬 CSV 해시 직접 확인), P3 14:08 0.595521 / 23.881592 ↔ `70761aff`(직접 확인), P1 08-31 18:14 0.833548 / 28.909341 ↔ `57844ef2`(직접 확인; 09-07 재업로드 시 서버가 "직전 제출과 내용이 같습니다"로 중복 판정한 기록이 서버 측 동일성 근거). SHA는 로컬 파일 값이지 서버 반환값이 아니다.
5. **P3 fresh cold는 아직 증명이 아니다.** 15:26 backbone 23/36. 최종 문서는 "completion_1(29 재사용 + 12 신규) + 저장 모델·별도 PID replay exact"까지만 주장해야 하며, fresh_cold_2 완료·SHA 대조 전에는 "빈 폴더 재학습 일치"를 쓰지 않는다. 또 P3 SOURCE_ONLY의 학습 경로(41 fit, 시간 상한 없음)는 우리 PC에서 약 6시간 규모라 6시간 규칙이 일반 적용될 경우 초과 가능성이 있다. 저장 모델 경로가 실질적 재현 수단이며 이를 FORM에 명시해야 한다.
6. **P1은 패키지 결함 없음, 규정 위험은 그대로.** 답안·ZIP 해시가 FORM과 일치하고, `run.py`는 답안 해시를 기록만 하고 중단하지 않는다(재현에 강건). 여섯 셀 리터럴과 선택 프로그램 미복구는 FORM·README에 정직하게 적혀 있다. 재현 검증 1항(과도한 상수 리터럴) 판정은 운영진 몫이며 우리가 적격을 확정할 수 없다.
7. **GitHub에는 최종 P2 평활 코드가 없다.** `p2_smooth7_portable_20260907.py`는 로컬 ZIP 안에만 있고 저장소에는 config·receipt만 있다. 규정 위반은 아니지만 FORM의 저장소 URL과 실제 코드가 어긋난다. 작은 .py 3개(smooth·projection·materialize)를 커밋하는 것을 권장한다(사용자 승인 필요).

## 감사 표

형식: `[문제 | 근거 파일/SHA | 확인 사실 | 결함·미확인 | 최소 수정 | 제출 전 필수 여부]`

| 문제 | 근거 파일/SHA | 확인 사실 | 결함·미확인 | 최소 수정 | 제출 전 필수 여부 |
|---|---|---|---|---|---|
| **P2** | `OceanFinalSelected_20260907/P2/P2_FINAL_REPRODUCTION_794268f1.zip` 484,699 B, SHA `aab30bbe…054d`(로컬 직접 확인, handoff·p2-final-ready.json과 일치); 내부 `ANSWER/submission_p2_L120_s3_smooth7_proj.csv` 26,061행 SHA `794268f1…`(직접 확인); `FINAL_MANIFEST.json` 멤버 해시 목록 | ZIP 구조 README/FINAL_MANIFEST/ANSWER/EVIDENCE/SOURCE_ONLY/SAVED_MODELS. SOURCE_ONLY 03_model·05_answer 비어 있음(멤버 목록). SAVED_MODELS 03_model에 seed 01/02/03 `.pt` 3개. requirements: numpy 2.3.5, pandas 3.0.1, torch 2.13.0+cu130 | — | — | — |
| P2 | `SOURCE_ONLY/02_code/projection/p2_smooth7_portable_20260907.py`(ZIP 내부, 직접 열람): `assert sha(source) == BASE`; `p2_final_day_materialize_20260907_v1.py:44-45` `raise ValueError("base answer is not exact approved L120 cold result")`; `run.py:52-53` CUDA 필수 | cold 경로(TRAIN→PREDICT→SMOOTH_PROJECT)와 saved 경로(SAVED_PREDICT: RUN_INFERENCE→REPLAY→FINAL_QA→smooth infer/replay) 모두 마지막에 같은 스크립트를 호출 | **결함: 기반 답안 해시 불일치 시 최종 답안 미생성.** `stages.py:74-77`의 PREDICT는 `exact_existing_candidate_sha_match`를 기록만 하도록 설계돼 있는데, 평활 단계가 그 설계를 뒤집는다. CUDA 없는 검증 환경이면 그 전에 중단 | (a) `p2_smooth7_portable`의 BASE assert를 "receipt에 `base_exact_reconstruction` 기록 후 계속"으로 변경(P3 `noshrink.py:116-118`이 이미 이 방식). (b) SMOOTH_SOURCE_MANIFEST·PACKAGE_MANIFEST·FINAL_MANIFEST 재생성, 추출 cold(≈175초)+saved(≈50초) replay 재실행, ZIP 재생성·새 SHA를 p2-final-ready.json과 FORM에 반영. CUDA 요구는 README에 이미 명시돼 있으므로 문서로 두되, CPU 추론 허용은 수치 변동을 검증하지 않았으므로 이번에 넣지 않는다 | **필수(강력 권고)**. 수정 없이 제출하면 검증 환경 차이가 곧 재현 실패다 |
| P2 | `SOURCE_ONLY/README.md`·`SAVED_MODELS/README.md`(원 코어 README): "Submit only `05_answer/submission_p2_L120_3seed.csv`"; `SOURCE_ONLY/PROJECT.ipynb`(옛 투영 전용 노트북, 출력 `submission_p2_L120_s3_proj.csv`) 잔존 | 상위 README와 CURRENT_README는 최종 파일을 `submission_p2_L120_s3_smooth7_proj.csv`로 명시하고 "this guide and new notebook supersede final-output instructions"라고 적음 | 문서 충돌·잔존 진입점. 검증자가 내부 README/PROJECT.ipynb를 따르면 옛 답안(fee6118b 또는 9c5fec38)을 만든다 | ZIP 재생성 시(위 (b)와 함께) 상위 README에 "내부 README의 제출 파일 지시와 PROJECT.ipynb는 이력용, 실행하지 말 것" 한 줄 추가. ZIP을 다시 만들지 않는다면 FORM 요약란에 같은 문장을 넣는다 | 문서 보완(권장). 위 (b)를 할 때 함께 |
| P2 | `reports/p2_l120_s3_smooth7_projection_20260907_v1/{official-receipt,candidate-ready,independent-qa,result}.json`(GitHub 추적) | receipt: 14:45 KST, 0.395254℃/28.373869, 이전 9c5fec38 대비 −0.010666℃; candidate-ready: 추출 cold TRAIN 142초·PREDICT 34초·SMOOTH 12.5초, cold 기반 답안 `fee6118b` exact, saved 5.25초, 두 경로 최종 SHA `794268f1` 동일; result B3 0.463529→0.441854, pooled 1.236732→1.204715 | 창 7은 노출 OOF에서 선택(retrospective) — README·smooth-config에 명시됨. 새 venv·OS 차단망 미검증(문서에 명시) | 없음 | — |
| P2 | Fable 원 코드(검토 문서 `FABLE_ALL_SLOTS_EXECUTION_DESIGN` 부록)와 `independent-qa.json` `boundary_diagnostic` | **경계 차이 설명**: 제 계산은 8개 fold 예측을 하나의 10분 격자에 올려 fold 경계를 넘어 평활했다(전역). Codex 최종은 fold-local. 경계 ±3스텝의 80행에서만 이웃 집합이 다르며 B3 0.4418588866(전역) vs 0.4418539229(fold-local), pooled 1.2046779880 vs 1.2047150555. 배포에는 fold가 없으므로 fold-local이 올바른 아날로그다 | 없음(설명만) | 없음 | — |
| **P3** | `OceanFinalDay_20260907/P3_numeric_cpudet_noshrink_v2/ANSWER/submission_p3_numeric_cpudet_noshrink.csv` SHA `70761aff…`(직접 확인); `SOURCE_ONLY.zip` 107,017 B `c3aed055…`, `SAVED_MODELS.zip` 17,125,495 B `f0451b9c…`(직접 확인, result.json과 일치) | SAVED_MODELS.zip 멤버: `03_model/full/{20260817,18,19}_{single,multi}.cbm` 6개 + `full_router.joblib` + `columns.json` + `noshrink-models.json`(핀). SOURCE_ONLY.zip 52 멤버, 답안·모델 없음 | 핀된 7개 배포 모델은 completion_1 산출(29 재사용 + 7 신규 backbone + 5 router). fresh_cold_2 모델과의 동일성은 완료 전 | 없음 | — |
| P3 | `SOURCE_ONLY/02_code/noshrink.py:36-47, 116-118`(직접 열람) | `expected_base_answer_sha256: None`이면 기반 답안 해시 불일치를 **기록만** 하고 계속(`base_exact_reconstruction`). 단기 리드 불변·후보 SHA는 별도 PID replay로 대조 | — (P2와 달리 강건) | 없음 | — |
| P3 | `reports/p3_numeric_cpudet_noshrink_20260907_v1/official-receipt.json`; `reports/p3_cpudet_mean_router_ablation_20260907_v1/official-receipt.json`; 제출관리 화면 14:08 0.595521 / 14:16 0.600933 | receipt와 화면 일치. no-shrink가 ff42a6a0(0.604351)·2015b387(0.609836)보다 Public 우위 | test-hs0 재가중 OOF는 악화(+0.005)를 예상했으나 Public은 개선. 66사례 Public 잡음, onset 축 미보정 등 원인 후보만 있고 확정 불가. 이 결과로 계수를 다시 적합하지 않는다(receipt에 명시) | 없음 | — |
| P3 | `fresh_cold_2/progress.json` 15:26 backbone 23/36; `SOURCE_ONLY/RUN_TRAINING.ipynb`(prepare→train→qa→adopt-trained, 시간 상한 없음) | fresh_cold_2는 11:56 시작. 남은 13 fit(전체 데이터 6 포함)로 학습 종료 약 17:35~18:00, router·QA 후 18:30 전후 추정 | **미확인**: fresh_cold_2 답안 == `2015b387` 및 그 모델에 no-shrink 재적용 == `70761aff`. SOURCE_ONLY 학습 경로는 우리 PC에서 약 6시간 규모 → 6시간 규칙 일반 적용 시 초과 가능. `START_HERE_NOSHRINK.md`는 "optional, NOT final-designated"라고 적혀 있어 최종 선택 시 문구 갱신 필요 | FORM/README에 (i) 재현 증거 = completion_1 + saved replay + 별도 PID replay, fresh cold "진행 중/미완" (ii) 저장 모델 경로(RUN_INFERENCE→REPLAY_INFERENCE)가 실질 재현 수단 (iii) SOURCE_ONLY 전체 학습 실측 시간과 6시간 미확인을 명시. fresh_cold_2 완료 시 SHA 대조 결과를 추가. 옛 진입점(TRAIN/PREDICT/UNBOUNDED 노트북)은 "실행하지 말 것" 한 줄 | 문서 보완(**필수**: 사실과 다른 "fresh cold 일치" 표현이 남지 않게) |
| **P1** | `OceanFinalRelease_20260907/P1/ANSWER/P1_submission.csv` SHA `57844ef2…`(직접 확인); `P1_SOURCE_ONLY.zip` 138,507 B `ba9fabd4…`, `P1_SAVED_MODELS.zip` 590,000,061 B `61e796c2…`(직접 확인, FORM.md와 일치); `SAVED_MODEL_PARTS/` part001~014 40,000,120 B + part015 30,000,181 B, `REASSEMBLY_MANIFEST.json` original_sha256 `61e796c2…` | 09-07 01:22 이후 ZIP 불변. 분할은 파일당 50 MB 이하 | `portal_limit_live_verified: false`(첨부 개수 상한 미표시). 15개 part + manifest + reassemble.py + FORM = 18개 첨부가 허용되는지 미확인 | 재개 후 모달에서 실제로 파일을 올려 개수 제한을 본다. 안 되면 SAVED_MODELS.zip 대신 SOURCE_ONLY.zip만 첨부하고 저장 모델은 재현 시 재학습(whole-cold 6,323초)으로 대체함을 FORM에 적는다 | 절차 확인(필수, 클릭 전) |
| P1 | `P1/SOURCE_ONLY/02_code/run.py:210-213`(직접 열람): `historical_answer_exact` 기록만 | 답안 해시 불일치 시 중단하지 않음 → 검증 환경 차이에 강건 | MS-TCN bf16·CUDA 재학습의 타 머신 정확 재생은 미보장(이전 시도 ≥110 라벨 차이, 이번 cold는 exact) | 없음 | — |
| P1 | `P1/FORM.md` "셀 정책 출처와 한계", `SOURCE_ONLY/README.md:14-15`, `02_code/tree_recipe.json` `parameter_provenance`, `reports/p1_historical_path_audit_20260906_v1/claim-source-ledger.md` | 여섯 셀은 코드 리터럴(`composition.py:21-22`), 08-26 로컬 OOF 근거, 선택 프로그램 미복구가 모두 명시됨 | **규정 위험(운영진 판정)**: 09-02 공지 1항 "과도한 상수 리터럴". 우리 근거는 리더보드 비사용 문서이지 재실행 가능한 선택 코드가 아니다. 적격성은 확정할 수 없다 | 없음(추가 문서로 해결되지 않음). 사후 선택 알고리즘을 만들어 원본이라 쓰지 않는다 | — (인지하고 제출) |
| P1 | `reports/p1_original_learning_seed_ablation_20260907_v2/{official-receipts,independent-qa}.json`; 제출관리 14:56 0.831622 / 14:57 0.826087 | B5 `cbeb7426` 28.858163, O_slow `c38ace7a` 28.711047, 모두 원형 미만. 내부 O_slow +0.001187(CI90 0 포함), B5 −0.000721. Q3/Q4 2-fold(Q2 MS 증거 없음) | 원형 유지가 옳다. 두 후보 패키지는 보존만 | 없음 | — |
| **공통** | `OceanFinalRelease_20260907/START_HERE.md`(11:11), `RELEASE_MANIFEST.json`(11:11) candidates.P2 `fee6118b` 28.077280 / P3 `ff42a6a0` 23.741446; `P1/FORM.md` "FORM.md 및 상위 RELEASE_MANIFEST.json/START_HERE.md" 첨부 안내 | 상위 문서가 옛 선택을 가리킴 | **버전 혼합(문서)**: P1 첨부에 상위 두 파일을 넣으면 P2/P3 최종과 모순. P2 최종 ZIP·P3 no-shrink 폴더는 각각 다른 루트(`OceanFinalSelected_20260907`, `OceanFinalDay_20260907`)에 있어 한눈에 보이지 않음 | 새 상위 문서 `FINAL_SELECTION_20260907.md` 1개: 문제별 최종 SHA·점수·패키지 경로·ZIP SHA·재현 증거 범위·미확인 항목. P1 FORM의 첨부 목록에서 상위 RELEASE_MANIFEST/START_HERE를 빼고 이 문서로 교체. 기존 파일은 삭제하지 않고 "이력"으로 표기 | **필수(문서)** |
| 공통 | 포털 `/app/problems/6`(15:30 전후 읽기): "모델 최종 제출하기" 버튼 존재, "오늘 남은 제출 0/3 … 26. 9. 8. 오전 12:00 이후 초기화"; `/app/submissions`: 모델 제출 항목 없음, 답안 항목만 | P2 최종 제출 미접수로 판단(확인창 OK 미클릭 진술과 일치) | 서버 대기 상태 배제 불가. 답안 quota 리셋(9/8 00:00)은 모델 마감 시각이 아님. 마감 시각·첨부 개수 상한·재현 대상 답안 선택 규칙·6시간 일반 적용은 여전히 미확인 | 재개 시 순서: 같은 두 화면 재확인 → P2 모달 다시 열어 파일·제목 확인 → OK → 제출관리에 모델 항목이 생기는지 확인 후 다음 문제 | 절차(필수) |
| 공통 | `git ls-files`(HEAD 6ecf287): `configs/experiments/p2_l120_s3_smooth7_projection_20260907_v1.json`, `reports/p2_l120_s3_smooth7_projection_20260907_v1/*`, `reports/p3_numeric_cpudet_noshrink_20260907_v1/*`, `reports/p1_original_learning_seed_ablation_20260907_v2/*` 추적됨 | 최종 후보의 설정·receipt·집계는 GitHub에 있음 | **미추적**: `p2_smooth7_portable_20260907.py`, `p2_final_day_projection_20260907_v1.py`, `p2_final_day_materialize_20260907_v1.py`(P2 ZIP 내부에만), P3 `noshrink.py`/`unbounded.py`(로컬 ZIP 내부). 저장소 URL만 보는 심사자는 최종 후처리 코드를 볼 수 없다 | 위 .py 파일을 `scripts/portable/`로 커밋(사용자 승인 후). 모델·답안·ZIP은 계속 제외 | 권장(비필수) |

## 제출 전 필수 vs 문서 보완 — 요약

**제출 전 반드시 해결(권고 순)**
1. P2 평활 스크립트의 BASE 하드 assert → 기록 후 계속으로 변경, 패키지 재생성·재QA(약 5~10분). 하지 않으면 검증 환경 차이 = 재현 실패.
2. 상위 최종 선택 문서 작성 및 P1 FORM 첨부 목록 정정(옛 START_HERE/RELEASE_MANIFEST 첨부 금지).
3. P3 FORM/README의 재현 증거 문구를 사실대로(completion_1 + saved replay, fresh cold 미완) 고정. fresh_cold_2가 제출 전에 끝나면 SHA 대조 결과만 추가.
4. 재개 절차: 포털 두 화면 재확인 → P2 모달 재확인 → OK → 접수 확인 → P1/P3 순. 첨부 개수 제한은 실제 업로드로 확인.

**문서 보완(권장, 비차단)**
- P2 내부 README·PROJECT.ipynb 잔존 지시에 대한 "이력용, 실행 금지" 안내.
- P3 no-shrink START_HERE의 "optional, NOT final-designated" 문구 갱신, 옛 진입점 안내.
- P2/P3 후처리 .py를 GitHub에 커밋.

**변경하지 않을 것**
- 답안 CSV·모델·계수·창·가중(공식 점수로 재조정 금지). P1 셀 정책·선택 알고리즘(사후 작성 금지). 과거 제출 삭제(별도 사용자 결정). 실행 중 P3.

## 남은 미확인(운영진·사용자 확인 사항)

모델 제출 마감 시각 / 첨부 개수 상한(파일당 50 MB는 UI 실측) / 재현 대상 답안 선택 방식(best·latest·선택형)과 허용오차 / 6시간 규칙의 일반 모델 적용 / 문제지 Ⅳ-2 원문 / 채점 완료 답안 삭제 가능 여부. 위 항목은 이 감사로 확정되지 않았고, 이 문서의 어떤 판단도 운영진 적격성 인증을 대신하지 않는다.
