# 반드시 먼저 읽을 메모 - Ocean AI Data P1

> 상태: 2026-08-21 KST 기준
> 이 문서는 모든 데이터 열람, 코드 수정, 실험, Git 작업, 제출 전에 처음부터 끝까지 다시 읽는다.
> 사용자 제공 문제문, 배포 데이터 README, 대회 홈페이지 공지가 충돌하면 작업을 멈추고 최신 공식 답변을 우선한다.

## 0. 작업 전 고정 순서

1. 이 문서를 전부 읽는다.
2. P1 원본 폴더의 README.md를 전부 읽는다.
3. git status를 확인하고 원본 데이터 또는 제출물이 추적 대상이 아닌지 확인한다.
4. 데이터 경로는 환경변수 P1_DATA_DIR로 주입한다. 코드와 노트북에 개인 PC 절대경로를 넣지 않는다.
5. 외부 공개 데이터는 공식 FAQ의 허용 범위와 아래 출처·권리·누출 게이트를 통과한 뒤 사용할 수 있다. 제출 업로드는 별도 사용자 승인을 받기 전까지 실행하지 않는다. 배포된 전체 시계열을 이용하는 양방향 offline QC가 주력이고, causal QC는 실운영 가능성 비교용 ablation으로 유지한다.

## 1. 작업 대상과 원본 보호

- 로컬 프로젝트: 이 Git 저장소의 루트
- 사용자가 지정한 P1 원본: `P1_DATA_DIR`이 가리키는 로컬 전용 폴더
- 필수 원문: `$env:P1_DATA_DIR\README.md`
- GitHub 백업: https://github.com/choihyunjin1/-oceanaidata_track1
- 배포 README에는 재배포 금지가 명시되어 있다.
- ZIP, CSV, 원본 README, score.py, 제출 CSV, 모델 파일을 공개 GitHub에 올리지 않는다.
- 원본 ZIP과 압축 해제본은 수정, 덮어쓰기, 이름 변경, 이동하지 않는다.
- 문서와 노트북에는 개별 관측 행이나 원자료 값을 복제하지 않고 집계 통계만 남긴다.

PowerShell 세션 예시:

~~~powershell
$env:P1_DATA_DIR = "사용자 PC의 P1_qc_anomaly 폴더"
.venv-p1\Scripts\python.exe -m jupyter execute --inplace --timeout=600 --kernel_name=python3 notebooks\00_p1_reconnaissance.ipynb
~~~

## 2. 사용자 제공 문제문 보존본

### 문제 1. 종합해양과학기지 관측 수온 자동 품질관리

#### 1-1. 배경

해양 관측 기지의 센서는 장기간 해수에 노출된 상태로 운영되므로 여러 원인으로 비정상 신호를 낸다. 센서 거치 수심의 변동, 커넥터 접촉 불량, 전원 계통의 불안정, 교정 주기 경과에 따른 편차 누적이 대표적이다. 이러한 이상은 하나씩 순차적으로 나타나기보다 여러 원인이 같은 시기에 겹쳐 복합적으로 발현되는 경우가 많다.

이 과제는 실제 품질관리의 전 과정을 재현하는 것이 아니라, 정상으로 판정된 관측에 대표적인 센서 이상을 합성하여 주입하고 그 자동 탐지 능력을 평가하는 교육용 문제다. 주입 비율과 지속시간 범위는 실제 장애 통계가 아니라 대회 운영을 위해 설정한 값이다.

생물 부착은 주로 전도도 센서의 교정 상수를 변화시켜 염분 값 자체를 틀어지게 만들며, 수온 센서에서는 응답 지연으로 나타날 뿐 평형값은 대체로 보존된다. 따라서 본 과제가 판정 대상으로 삼는 수온 이상의 직접 원인으로는 다루지 않는다.

#### 1-2. 과제

- 배포된 수온 시계열의 각 시점에 대해 센서 이상 여부를 판정한다.
- 판정 대상은 temp다. psal과 depth는 참고 변수다.
- 이상은 원자료의 품질 플래그가 1(good_data)인 구간에만 주입되었다.
- 전체 유효 관측의 약 4%가 이상 구간이며, 일부는 두 가지 이상의 유형이 같은 시기에 중첩된다.

#### 1-3. 주입된 이상

| 유형 | 지속시간 | 양상 |
|---|---:|---|
| spike | 10분 | 단일 시점의 급격한 이탈 |
| noise | 3~58.8시간 | 분산이 비정상적으로 커진 구간 |
| flatline | 2~47.2시간 | 값이 변하지 않고 고정된 구간 |
| offset | 8~86.5시간 | 일정한 크기만큼 치우친 구간 |
| drift | 9~86.5시간 | 시간에 따라 편차가 선형으로 누적되는 구간 |

#### 1-4. 접근 참고사항

- 같은 기지의 층간 수온 관계는 유용하지만 항상 유효하지 않다.
- 조석 혼합이 지배하는 시기에는 층간 상관이 높지만, 수온약층이 발달하면 약층 상하 상관이 사라져 정상 성층을 이상으로 오탐할 수 있다.
- 계절 성층과 내부파로 한 층만 단시간 급변하는 정상 현상이 있으므로 시간창 길이를 계절 조건과 함께 조절해야 한다.
- 기지별 변동 특성이 다르므로 정점·층 단위의 강건한 표준화가 전 구간 공통 임계값보다 안전하다.
- spike는 1차 차분, flatline은 연속 동일값 길이, drift는 장기 이동 기준선과의 편차에 민감하다.

#### 1-5. 배포 데이터

| 파일 | 내용 |
|---|---|
| train.csv | 2024~2025년 관측과 정답 라벨, 776,706행 |
| test.csv | 2026년 관측, 라벨 비공개, 169,011행 |
| sample_submission.csv | 제출 양식 |
| baseline_rule.csv | 규칙 기반 기준 예측 |

공식 예외:

- P2 정답 보호를 위해 S-ORS 2025년 9~10월 layer 2·3·4는 P1 학습에서 제외되었다.
- 정점·층별 운영기간과 결측률은 서로 다르다.
- G-ORS 2026년 depth는 전부 결측이다.
- I-ORS 2026년 layer 3은 미운영이다.

#### 1-6. 제출과 평가

- 필수 열 순서: station, year, layer, time, label
- 선택 열: anomaly_type
- label은 이상 1, 정상 0인 정수이며 결측과 무한값을 허용하지 않는다.
- anomaly_type은 순위에 반영되지 않고 본선 심사 참고용이다.
- 평가지표는 이상 행에 대한 binary F1이다.
- 주최측 규칙 기반 전체 평가 기준 F1은 0.548255다.
- 종합점수는 각 리더보드 분할에서 동일한 베이스라인 제출을 다시 채점한 값으로 계산된다.

## 3. 공식 일정과 재현성 요구

2026-08-13에 최신 대회 UI와 KIMST 공고 제2026000111호의 초기 첨부 PDF를 대조했다. 두 화면의 날짜가 다르므로 **운영 기준은 최신 대회 UI를 우선**하고, 정확한 마감 시각과 결과물 업로드 시점은 운영진의 서면 답변을 받아야 한다.

| 출처 | 단계 | 표시 일정 | 운영 판정 |
|---|---|---|---|
| 최신 대회 UI | 문제 공개 | 2026-08-13 | 현재 기준 |
| 2026-08-12 참가자 전용 제출 안내(수정) | 예측 답안 제출 시작 | 2026-08-25 | 8월 20일 업로드분은 25일 함께 채점 |
| 최신 대회 UI | 제출 마감·최종 모델 | 2026-09-07 | 현재 기준; 정확한 시각 확인 필요 |
| 초기 공고 PDF | 대학부 온라인 해커톤 | 2026-08-10 ~ 2026-09-04 | 변경 전 일정으로만 보존 |
| 초기 공고 PDF | 예선 결과물 제출 | 2026-09-04 10:00 이전 | 변경 전 일정으로만 보존 |
| 초기 공고 PDF | 본선 진출자 발표 | 2026-09-11 | 최신 UI 재확인 필요 |
| 초기 공고 PDF | 본선 자료 제출 | 2026-09-29 | 최신 UI 재확인 필요 |
| 초기 공고 PDF | 본선 발표 심사 | 2026-10-08 | 최신 UI 재확인 필요 |
| 초기 공고 PDF | 수상자 선정 | 2026-10-12 | 최신 UI 재확인 필요 |
| 초기 공고 PDF | 시상식 | 2026-11-04 ~ 2026-11-06 | 최신 UI 재확인 필요 |

초기 공고 PDF의 대학부 예선 주의사항은 별도 변경 공지가 없는 한 재현성 안전 기준으로 유지한다.

- 결과물은 코드·데이터셋 zip으로 제출한다.
- 실시간 리더보드, 코드·데이터 재현성 검증, 전문가 평가를 종합한다.
- 초기 PDF에는 2026-09-04 10:00 이후 수정 불가라고 적혀 있으나, 최신 UI는 2026-09-07을 제출 마감·최종 모델일로 표시한다. 실행 전 최신 UI와 서면 답변을 다시 확인한다.
- 코드·데이터 재현 불가, 환경정보 누락, 한글 파일명·경로 오류 등으로 실행할 수 없으면 실격될 수 있다.
- 성능·정확도 70점, 방법론 타당성 20점, 창의성 10점이다.

중요한 미확정 충돌:

- 공고는 코드·데이터셋 zip을 요구하지만 배포 README는 원자료 재배포를 금지한다.
- 운영진의 서면 답변 전에는 제출 묶음에 원본 ZIP/CSV를 포함하지 않는다.
- 재현 패키지는 우선 코드, requirements, 환경정보, 입력 위치 계약, 해시와 실행 문서로 설계한다.

## 4. 2026-08-13 로컬 정찰로 검증한 사실

- 원본 ZIP CRC 검사가 통과했다.
- ZIP 내부 6개 파일과 압축 해제본의 SHA-256이 모두 일치한다.
- train/test 키 결측, 키 중복, 정확 중복 행은 모두 0건이다.
- 모든 time은 파싱되며 +09:00 접미사를 갖는다.
- train 양성은 32,126/776,706 = 4.1362%다.
- test 결측은 psal 798건(0.4722%), depth 16,368건(9.6846%)이다.
- test depth 결측 중 16,331건은 G-ORS 전체다.
- baseline 양성은 4,402/169,011 = 2.6046%다.
- baseline 양성 연속 구간 1,199개 중 699개는 1행 singleton이다.
- sample/baseline 키와 행 순서는 test와 완전히 일치한다.
- 근거 보고서: reports/P1_RECONNAISSANCE_2026-08-13.md
- 재현 노트북: notebooks/00_p1_reconnaissance.ipynb

### 4-1. 구현·로컬 검증 체크포인트

- 전용 Python 3.12.10 환경 `.venv-p1`, 고정 의존성, CUDA 13.0/RTX 5090 smoke test가 준비되었다.
- `audit`, `cv`, `train`, `predict`, `validate`, `reproduce` CLI와 시간 gap segmentation, 피처, 트리 모델, 후처리, 제출 검증, 딥러닝·SSL 구성요소가 구현되어 있다.
- 7일 purge를 둔 세 rolling-origin outer fold에서 각 fold의 inner split으로 모델 반복 수와 후처리를 선택했다.

| 모드 | honest nested outer micro F1 | test 정점·층 비중 재가중 F1 |
|---|---:|---:|
| 양방향 offline 주력 | 0.816737 | 0.768804 |
| causal 비교 | 0.757248 | 0.703250 |

이 값은 train 라벨로 만든 로컬 추정치이며 test 정답이나 공식 리더보드 점수가 아니다. 전체 OOF에 임계값을 다시 맞춘 resubstitution 수치(offline micro 0.837877, causal micro 0.761137)는 배포 설정 선택 참고값일 뿐 honest outer 성능으로 보고하지 않는다. 공식 0.548255도 다른 평가 집합에서 산출된 주최측 기준값이므로 숫자를 직접 차감하지 않고, 같은 로컬 fold에서 후보와 로컬 기준선을 비교한다.

환경·CLI 진입점:

~~~powershell
powershell -ExecutionPolicy Bypass -File scripts\bootstrap_env.ps1
$env:P1_DATA_DIR = "사용자 PC의 P1_qc_anomaly 폴더"
.venv-p1\Scripts\python.exe -m p1_qc audit --config configs\p1.toml
.venv-p1\Scripts\python.exe -m p1_qc cv --config configs\p1.toml --mode offline --backend lightgbm
~~~

## 5. 절대 금지선

### 라벨·시계열 누출

- test label은 끝까지 미지 상태로 유지한다.
- 행 단위 무작위 분할을 주 검증으로 쓰지 않는다.
- 이상 구간, rolling window, 구간 후처리 범위를 고려한 시간 블록 분할과 purge/embargo를 사용한다.
- validation 시점 이후의 데이터를 학습, 정규화 통계, 임계값 선택에 사용하지 않는다.
- 이 문제는 배포된 전체 시계열을 일괄 판정하는 offline QC를 주력으로 실행한다. centered rolling, 이전·다음 차분과 양방향 이웃은 동일한 연속 segment 안에서만 계산하고, rolling 의존 범위보다 긴 purge로 fold 간 누출을 차단한다.
- causal 모드는 미래 행을 전혀 보지 않는 독립 비교 실험으로 유지한다. 공식 서면 규칙이 offline 문맥을 금지한다고 확인되면 causal 결과로 전환한다.
- test 양성률을 train 4% 또는 baseline 예측률에 맞추지 않는다.
- 리더보드 점수에 맞춰 반복적으로 수작업 임계값을 조정하지 않는다.

### 외부 데이터

- 공식 공개 FAQ API(id=9)는 외부 공개 데이터 활용을 허용하며 출처 명시를 의무화한다. 참가자 FAQ의 대학부 답변도 외부 데이터 사용 가능을 확인한다.
- 별도 서면 승인 대기는 해제한다. 공개 접근, 사용 가능한 라이선스, URL/DOI·버전·회수일·SHA-256·가공 이력을 모두 기록한 자료만 격리 경로에서 사용한다.
- P1 2026 clean 수온, P2 2025-09~10 S-ORS layer 2·3·4 실제 수온·염분, P3 익명 case의 실제 미래 파고·절대시각처럼 숨은 정답 자체를 복원하거나 대조하는 자료는 외부 데이터 일반 허용과 무관하게 사용하지 않는다.
- 시간대가 겹쳐도 ERA5·CMEMS 같은 독립 재분석의 비정답 공변량은 출처·동화자료·누출 감사를 통과하면 후보가 될 수 있다. 외부자료 제거 ablation과 동일 로컬 split 비교를 반드시 남긴다.
- 권리가 불명확한 S-ORS ScienceWatch 자료와 provenance가 불명확한 사전학습 checkpoint는 별도 권리·학습자료 검토 전 사용하지 않는다.
- 외부 원자료는 GitHub와 공개 제출 패키지에 포함하지 않고 재현 다운로드 코드와 manifest만 제공한다.

### 원본·Git

- 데이터셋 원본 폴더는 읽기 전용이다.
- artifacts, outputs, submissions와 모델은 Git에 추가하지 않는다.
- 커밋 전 git status와 git diff --cached를 확인한다.
- 사용자가 명시하지 않은 커밋, push, 제출 업로드를 실행하지 않는다.

## 6. 문제별 하루 3회 제출 게이트

2026-08-20 갱신된 공식 인터페이스는 예측 답안을 문제별 하루 3회까지 허용한다. 2026-08-12 참가자 전용 수정 공지는 답안 채점을 8월 25일부터 시작하며 20일 업로드분은 25일 함께 채점한다고 안내한다. 실제 업로드 직전 계정 화면에서 문제별 당일 제출 이력과 초기화 기준을 다시 확인한다.

사용자가 정확한 후보 파일의 절대경로를 지정하고 최종 승인하기 전에는 업로드하지 않는다. 승인 전에 다음을 모두 기록한다.

1. 실험 ID, Git commit, seed, 피처·모델·후처리 버전
2. validation 분할 정의와 purge 길이
3. 전체·정점·층·유형별 precision, recall, F1
4. 공식 0.548255 참고값과의 관계, 동일 로컬 split 기준선 대비, 누출·과적합 검토
5. 제출 열 순서, 169,011행, 키 유일성·동일성, 유한 binary label 검증
6. 제출 CSV 절대경로, 파일명, 바이트, SHA-256
7. 해당 날짜의 기존 제출 여부와 일일 기회 초기화 시간대

검증 명령:

~~~powershell
.venv-p1\Scripts\python.exe scripts\validate_submission.py <candidate.csv>
~~~

## 7. 공식 출처

- KIMST 공고: https://www.kimst.re.kr/u/news/notice_01/board.do?bno=153421765145711&searchDiv=&searchKeyword=&type=view
- 대회 홈페이지: https://www.oceanaidata.org
- 대회 공식 FAQ API: https://oceanaidata.org/api/faqs
- KORS 자료 포털: https://kors.kiost.ac.kr/eng/dataPortal
- KORS 다운로드 페이지: https://kors.kiost.ac.kr/data/accessData
- I-ORS 2014~2023 CTD, DOI 10.22808/DATA-2024-6: https://sciwatch.kiost.ac.kr/handle/2020.kiost/46422
- I-ORS 2004~2023 1시간 자료, DOI 10.22808/DATA-2025-2: https://sciwatch.kiost.ac.kr/handle/2020.kiost/46771
- S-ORS 2015~2023 CTD, DOI 10.22808/DATA-2024-7: https://sciwatch.kiost.ac.kr/handle/2020.kiost/46423

## 8. 운영진 확인 전 미확정 사항

- 공개 사전학습 checkpoint의 학습자료 provenance와 허용 범위
- centered/bidirectional 특징을 포함한 사후 자동 QC 범위의 공식 확인. 현재 공개 문제문에는 online 제약이 없으므로 승인된 실행 계획에서는 offline을 주력으로 사용하되, 추후 공식 답변이 금지하면 causal로 전환한다.
- 문제별 하루 3회 제출의 초기화 시간대, 실패 제출 차감, 취소·재제출 규정
- public/private 리더보드 분할과 최종 평가 절차
- 코드·데이터셋 zip에서 원본 배포자료를 제외하는 정확한 방법
- 외부 DOI, 라이선스, 가공 이력을 제출물 어디에 공개해야 하는지
